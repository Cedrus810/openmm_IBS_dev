# 当前仍未完成项（复核于 2026-07-20，pilot 加密修复 v15 后更新）

本文件只保留当前源码里仍然存在、需要继续修改代码或采取工程行动的问题。代码已经
修复但尚未获得完整 CPU/GPU/依赖环境证据的项目统一迁入 `VALIDATION_MATRIX.md`；修复
历史和审计结论保留在 `AUDIT_STATUS.md`。

**当前结论：没有剩余的已确认默认生产路径 P0/P1，此前列出的 13 项 P2 已全部
修复；16:08 版本仍列出的主窗口 dynamics Context 续算缺口也已由
`MAIN_WINDOW_CHECKPOINT_PROTOCOL_VERSION=1` 修复；2026-07-19 发现的"在线学习只用
朴素 TA、没用已有 TMBAR"问题已完成代码修复（`IBS_BIAS_PROTOCOL_VERSION=19`），
移入 `VALIDATION_MATRIX.md` 的 `VAL-GPU-007` 等待 GPU 实跑验证。** 当前只剩下列
性能加固项与一项独立待设计任务；待运行验证不在本文件重复维护，请查看
`VALIDATION_MATRIX.md`。

## 待 GPU 验证：vanishing 窗口 0 分组收窄（2026-07-19，代码已改完）

窗口 0（λ_vdw 1.0→0.963，6 态）`IBSWarmupConvergenceError`：占据几乎全卡在 state 0
（`mean_p≈0.994`），TMBAR `min_absolute_ess≈1.0`——真实的采样/overlap 问题（论文
Sec 2.4 语境下的"单个 ensemble 塞了太多态"），不是 TMBAR 数值 bug（TMBAR 只是诚实
报告了"学不出来"）。完整时间线/已排除方案见 `VANISHING_WINDOW0_HANDOFF.md`。

**已改（`abfe_preoptimizer.py`）**：`THERMODYNAMIC_PATH_PROTOCOL_VERSION` 12→13，
`VANISHING_TARGET_INTERVALS_PER_ENSEMBLE` 5→3，`VANISHING_MIN_INTERVALS_PER_ENSEMBLE`
4→2。只改窗口分组，不碰密度/物理计算。用真实缓存的 18 态 λ 值手算验证过：窗口 0 从
6 态（1.0→0.963）收窄到 4 态（1.0→0.9778）。**尚未 GPU 验证**（`py_compile` 已过）。

**论文原文依据（`integrated-boltzmann-sampling.md` Sec 2.4）**：论文自己给的流程就是
"先试一个 ensemble 划分 → 短跑看权重收敛是否数值稳定 → **如果观察到收敛问题，就把
积分区间再细分、引入更多 IBS ensemble，迭代直到所有态都稳定收敛**"；"四个或更少
ensemble 通常够用"只是经验值，不是硬性上限。也就是说：**这次的分组收窄就是论文
prescribed 的标准做法，不是临时发明的招；如果这次跑完还是不收敛，论文给的下一步
是同一个动作再来一次——把窗口 0 从 4 态再往下细分（比如 2-3 态），不是换一套新方法。**

- [x] 4 态（`(0,4)`）版本已 GPU 实跑：仍失败，占据只从 99.4%→96.9%，`min_absolute_ess`
      还是 ~1.0。按论文 Sec 2.4 继续细分。
- [x] **2026-07-19 第二轮修复：`abfe_preoptimizer.py` 新增位置相关分组**——
      `vanishing_subdomain_ranges_from_lambdas`/`redistribute_vanishing_lambda_subdomains`
      新增可选参数 `first_ensemble_target_intervals`，只把窗口 0 单独切细，其余窗口
      维持 3 区间不变（不是把整条路径都切成小窗口——那样会丢掉 IBS 本身"多态打包"的
      效率优势，用户已否决）。`THERMODYNAMIC_PATH_PROTOCOL_VERSION` 13→14。
      **用户最终拍板：窗口 0 定为 3 态（`first_ensemble_target_intervals=2`），
      不是 2 态——3 态跟 2026-07-17 那次真实收敛过的窗口 0 态数一致，到此为止，
      不再继续细分。** 用真实缓存的 18 态 λ 值验证过：窗口 0 变成 3 态（1.0→0.9852），
      总窗口数 6（不变），其余窗口大小不变。`py_compile` 已过。
- [x] **3 态版本也已 GPU 实跑，仍失败**（2026-07-20）：占据 98.2%，`min_absolute_ess`
      还是 ~1.0。6/4/3 三次独立真实 GPU 结果连起来看，占据比例几乎不随态数变化——
      用户用 IBS 偏置公式直接指出根因：分组只改"哪些态共用一个 bias"，从来没改过
      state0↔state1 之间真正的 ΔU/λ 间距（因为三次都是同一条 18 态 λ 数组的子集，
      间距一直是 ~0.006-0.007）。**分组这条路到此确认走不通，不再尝试任何分组/态数
      调整。**
- [x] **2026-07-20 第三轮修复（真正的根因）：pilot 探针自适应加密**——
      `abfe_preoptimizer.py` 新增 `DualLambdaPreOptimizer._refine_pilot_grid_in_
      steep_segments`，`optimize_stage2_vanishing` 粗探针跑完后调用它：如果单个
      粗网格 pilot 区间占当前总热力学长度的比例超过阈值（默认 20%），就在那个区间
      内部真实插入额外探针点重新测 `metric_g`，而不是像现在这样只有 2 个粗网格点、
      靠线性插值硬猜区间内部密度该怎么分布。用真实缓存数据验证过：λ=1.0→0.9412
      这一段单独占了当前总热力学长度的 51.5%，触发条件确认会命中。`THERMODYNAMIC_
      PATH_PROTOCOL_VERSION` 14→15。`py_compile` 已过，插入逻辑已用真实 metric_g
      数据 + 模拟额外探针做过离线验证（严格单调递减、正确定位最差区间）。
      **这是第一次真正改动密度算法本身，不是调分组/态数**——前两轮 GPU 结果已经
      证明分组不是问题所在。
- [x] v15 pilot 加密版 GPU 实跑：**仍失败**（占据 98.2%，`min_absolute_ess≈1.0`）。
      但真实测出 λ≈0.96-0.97 附近有个尖锐非单调 metric_g 峰值（端点的 ~50 倍），
      且这个峰值根本不在窗口 0 范围内——窗口 0 自己另有原因失败。用户点破：
      **"探针"设计上就是粗糙近似的，不该被要求精确**，之前几轮一直在跟探针的
      本质较劲。
- [x] **2026-07-20 第四轮修复（真实数据，不是探针）**：warmup 失败时
      `IBSSampler.save_ibs_state` 保留了窗口 0 的 `tmbar_history`（~1000 帧真实
      采样）。用 `analyze_window0_real_tmbar_data.py` 重新跑真 MBAR，测出窗口 0
      内部真实 Δf：state0↔1=-25.3 kJ/mol(~10.2kT)，state1↔2=-15.7 kJ/mol(~6.3kT)，
      两边 dF/dλ 几乎相等——这一段接近线性、不病态，纯粹是步子太大。
      `repair_stage2_window0_real_delta_f.py` 用真实 Δf 把窗口 0 从 3 态扩到 7 态
      （6 步，每步 ~2.7 kT），复用已有真实采样数据，不重跑 pilot。
      `VANISHING_FIRST_ENSEMBLE_TARGET_INTERVALS` 2→6，`THERMODYNAMIC_PATH_
      PROTOCOL_VERSION` 15→16。`py_compile` 已过。**尚未 GPU 验证。**
- [x] **v16（真实 Δf 7 态）已 GPU 实跑，仍失败**（2026-07-20）：`mean_p`=
      [0.968,0.010,0.003,0.001,0.001,0.002,0.014]，`min_absolute_ess≈1.0`。
      **六种完全不同、每种都有真实数据支撑的 λ-schedule 方案（6/4/3 态分组、
      pilot 加密、真实 Δf 均匀切分）全部失败——结论是问题根本不在"λ 摆在哪"。**
- [x] **2026-07-20 第五轮修复（真正换了维度：改学习起点，不是 λ 网格）**：
      用户用 IBS 偏置公式指出根因——f_k 每次全新学习都从 0.0 冷启动，SGD/TMBAR
      只能从每批 ~20 帧的统计证据里"自举"出需要多大偏置，构型难采时早期批次
      几乎看不到欠采样态证据，学不出更大偏置，陷入自举困境，换哪套 λ 都救不了。
      新增 `abfe_preoptimizer.estimate_f_k_from_pilot_ti`：对 Stage 2 pilot 早就
      测好的真实平均梯度 `mean_dU_dlambda_kJ_mol` 做热力学积分(TI)得到 F(λ)，
      插值到窗口自己的 λ 上作为该窗口第一次 learning 的 f_k 起点（不是冷启动
      0.0）——这份数据 pilot 阶段就有，对任何窗口第一次尝试都生效，不是窗口 0
      专属补丁。`IBSWindowManagerDualLambda`/`_run_dual_lambda_stage` 新增
      `pilot_lambdas`/`pilot_mean_dU_dlambda`（默认 `None` 向后兼容），
      `run_all_windows` 只在 `not is_resumed_ibs` 时注入，不碰 resume 语义；
      数据缺失/非有限时静默回退冷启动，不报错不编造。同时把候选门
      `candidate_min_absolute_ess` 从 2.5 降到 1.0——真实失败诊断显示候选门四项
      判据里只有 `min_absolute_ess`（有效样本数的数学下限，本身不是这个区间的
      质量信号）卡住，其余三项早就过关；`final_*`/冻结验证严格门槛完全不碰。
      `IBS_BIAS_PROTOCOL_VERSION` 19→20。`py_compile` 已过。
- [x] **v20（pilot TI 热启动）已 GPU 实跑，占据反而恶化**（2026-07-20）：
      96.8%→99.1%，state1 `min_absolute_ess` 降到 8.6e-6。用户直接问"符号是不是
      反了"。逐行核对 `estimate_f_k_from_pilot_ti` 后排除了用户最初怀疑的具体机制
      （Δλ 积分方向——函数用 `np.argsort` 强制转升序，与输入顺序无关，pilot 探针的
      有限差分对象跟 `pilot_lambdas` 也是同一个变量，无隐藏方向不一致）。
- [x] **2026-07-20 第六轮修复（真正的符号 bug，定位在别处）**：直接读
      `IBSBiasForce` 的能量表达式源码（`ibs_engine.py:2397`）逐步代数展开验证
      `exp(-β·V_bias)=Σ_k exp(β·f_k)·exp(-β·U_k(x))`——给某态更高 f_k 会机械地
      推高它在混合分布里的权重，结合 `update_weights` 的 `logits=beta*(f_old-u_mk)`
      （3364-3367 行），二者严格一致。用真实失败数据验证：`_solve_tmbar_and_recenter`
      （服务于 v19 起的在线 TMBAR 更新）和 `estimate_f_k_from_pilot_ti`（v20 热启动）
      **都**把已经占主导的 state0 判给全组最高（最不负）的 f_k——原样喂给
      `context.setParameter` 等价于每次更新都在强化过量代表的态。根因在
      `_solve_tmbar_and_recenter`：`solve_stage_integrated` 返回的 `f_k`
      （`df_matrix[0,1:]`，pymbar 原始 `f_i=-ln(Z_i)` 约定）方向与
      `update_weights` 需要的方向相反，**从 v19 引入 TMBAR 起就存在，影响这条
      在线学习路径覆盖过的所有窗口，不只是窗口 0**。
      修复：`_solve_tmbar_and_recenter` 现在对 `solve_stage_integrated` 原始
      `f_k` 取负号后再 mean-center。同时暂时禁用 v20 的 pilot TI 热启动注入
      （`run_all_windows` 里调用点已注释，`warm_start_seed` 强制 `None`，f_k
      退回 0.0 冷启动）——`pilot_mean_dU_dlambda_kJ_mol` 是独立短程弛豫测的
      系综平均梯度，跟 `u_mk`（当前占主导构型上跨态求值）概念不同，其符号是否
      同样"反了"未经独立验证，先只验证 TMBAR 符号修复这一个变量。
      `IBS_BIAS_PROTOCOL_VERSION` 20→21。`py_compile` 已过。**尚未 GPU 验证。**
- [x] v22 GPU 已跑：pilot-TI 确实把体系推出 state0，但绝对 TMBAR 向量覆盖把
      state6 锁到 99.53%，因此不能据此转去结构/pose 诊断；先修在线更新方法。
- [ ] 用户跑 v23：`ABFE_DEBUG_SKIP_STAGE2_FINGERPRINT=1 python runabfe.py --config
      abfe_config.json --ligand MOL --resume`。重点看每轮 `weight_update` 是否始终降低
      过强态、抬高弱态且最大步长不超过 2 kT，`mean_p` 是否从端点逐渐铺开。
- [x] v23 首跑已把 `mean_p` 铺到接近 1/7，实际 LSE/coverage 已过；未生产仅因
      overlap/absolute ESS 分别以约 `6e-15`/`1e-13` 的舍入差落在 inclusive
      阈值下。已加入仅限 `1e-12` roundoff 的比较，并让 resume 计入持久化更新数；
      不升协议版本，保留当前学好的 v23 state。待 resume 进入冻结验证/production。
- [x] **2026-07-20 v22 GPU 结果与 v23 修复**：v22 seed 成功把占据从 state0
      推离，但在线 TMBAR 绝对向量覆盖又把 state6 锁到 99.53%，f_k 跨度发散到
      约 1494 kJ/mol，`min_absolute_ess≈1`。v23 不再应用任何正/反号 TMBAR
      绝对向量；保留 pilot-TI 起点，实际学习改为全态同步、有界、衰减的
      `-kT*log(K*<p_k>)` 增量，TMBAR 只作累计覆盖质量门。待 GPU 验证。
- [x] **2026-07-20 v23 首次 GPU 失败的复核**：50/50 次权重更新耗尽后
      `mean_p`=[0.137,0.152,0.125,0.134,0.136,0.142,0.173]（已接近 1/7≈0.143，
      占据问题本身已解决），但 `converged=False`——`min_overlap=0.049999999999997
      16` 差阈值 0.05 约 2.8e-15，`min_absolute_ess=0.9999999999998863` 差阈值
      1.0 约 1.1e-13。用 pure-Python 直接算过 `_meets_minimum_with_roundoff`
      的公式，这两个数确实都在 1e-12 容差内，理论上该判定通过。查时间戳定位
      根因：这次失败的 `pipeline.log` 起始于 2026-07-19 19:46:17，比 roundoff
      修复写入 `ibs_engine.py` 的时间（2026-07-20 08:02:14）早了近 12.5 小时——
      长跑的 GPU 进程不会热重载改动过的源文件，这次失败用的是修复之前的旧字节
      码，不是修复本身有残留 bug。
- [x] **2026-07-20 第七处修复：roundoff 容差没有铺全**——用户追问后复查发现
      `_meets_minimum_with_roundoff` 只包住了 `solve_stage_integrated` 里两个
      下界判据（`min_overlap`/`min_absolute_ess`），同一个函数里的上界判据
      （`max_endpoint_uncertainty_kJ_mol <= final_max_uncertainty_kJ_mol`，
      9247 行附近）完全没有容差；生产阶段 early-stop 判据（`ess_ratio_ok`/
      `absolute_ess_ok`/`uncertainty_ok`，8298-8301 行附近）也是同一类
      ESS/overlap/不确定度浮点比较，同样完全没有容差（`decorrelated_ok`/
      `drift_ok` 是整数/自由物理量比较，不属于这一类，未改）。新增
      `_meets_maximum_with_roundoff`（`_meets_minimum_with_roundoff` 的上界
      对称版本），把这四处全部改用容差比较。`py_compile` 已过。**不升协议
      版本**（跟用户自己那次 v23 roundoff 修复同样的理由：只是修比较方式，
      不改测的是什么量，升版本号会白丢窗口 0 已经跑出来的 50 万步 tmbar_history/
      已学到的 f_k）。
- [x] **2026-07-20 用户新跑一次，确认是全新进程（pipeline.log 里
      "ABFE Pipeline v4.0 初始化完成" 时间戳晚于上面 roundoff 修复的保存时间），
      仍然 `converged=False`**：`min_overlap=0.05`（=阈值，通过）、
      `min_absolute_ess=0.9999999999998863`（阈值 1.0，roundoff 内，理论应通过）、
      `min_decorrelated_samples=10`（阈值 5，通过）、
      `max_endpoint_uncertainty_kJ_mol=3.507`（阈值 5.0，通过）——四项摘要看起来
      全过，但 `converged` 仍是 False。写了 CPU-only（不用 GPU）诊断脚本
      `diagnose_window0_converged_gate.py`，直接对真实已存的 `tmbar_history`
      重跑当前代码的 `solve_stage_integrated`，把 `converged` 内部四组子条件
      逐个打印。用户跑出的结果：**四组子条件手算全部为 True，但
      `res['converged']` 本身仍是 False**——缺口精确定位在脚本没法从外部检查
      的那一项：`len(local_results) == len(valid_windows)`（有 minibatch 在
      内部循环里被静默跳过）。
- [x] **2026-07-20 第八处修复：真正的根因，找到了**——`GlobalMBARAnalyzer.
      solve_stage_integrated` 内部循环里有一处硬编码字面量 `10`
      （`if n_k_local[sampled_row] < 10: continue`，无警告打印），而不是用
      调用方传入的 `min_frames_per_window` 参数（该参数默认值恰好也是 10，
      这条硬编码大概率是把默认值誊抄成字面量时留下的）。`_solve_tmbar_and_
      recenter` 给在线学习路径故意把 `min_frames_per_window` 放宽到 3（这条
      函数自己的 docstring 早就写明原因："去相关后统计非效率 g≈5-15 时常剩
      2-7 帧，用默认的 10 帧门槛会让绝大多数 minibatch 被跳过、白白丢弃已经
      采到的真实数据"）——但这条硬编码完全无视这个放宽，任何 3-9 帧的
      minibatch 依然被无声跳过，`len(local_results) < len(valid_windows)`，
      `converged` 因此恒为 False，且没有任何警告能提示这一点，表现上跟
      "roundoff 卡在阈值"几乎一模一样，实际根因完全不同（这条本身不是舍入
      问题）。修复：改用 `min_frames_per_window` 参数，同时给这里和上面另一处
      同样静默的 `n_frames < min_frames_per_window` 检查（原始帧数那处）都
      补上警告打印，避免以后再次无声丢数据。`py_compile` 已过，不升协议版本
      （同样只是修比较/跳过逻辑，不改测的是什么量）。
- [x] **2026-07-20 第九处修复：`candidate_min_decorrelated_samples` 同款
      不可达阈值**——`diagnose_window0_converged_gate.py` 加上真实
      `min_frames_per_window=3` 后复跑，暴露出还有一项没过：
      `min_decorrelated_samples=4 < 5`（真实差距，不是舍入）。跟前面
      `candidate_min_absolute_ess` 2.5→1.0 那次同一类问题：`min_frames_per_
      window=3` 本来就故意放行 3-4 帧的 minibatch 进 local_results，聚合出的
      `min_decorrelated_samples` 全局最小值必然被这类 minibatch 卡死在 3/4，
      要求 `>=5` 在这个场景下恒不可满足。`candidate_min_decorrelated_samples`
      默认值 5→3（`update_weights`/`run_all_windows` 两处签名，`final_*`/
      冻结验证严格门槛不碰）。`py_compile` 已过，不升协议版本。
- [x] **2026-07-20 CPU-only 复核（`diagnose_window0_converged_gate.py`），
      对真实 100 条 tmbar_history 重跑当前代码，`res['converged']` 现在真的
      是 `True`**：`min_overlap=0.05`（=阈值）、`min_absolute_ess=0.9999999999
      998863`（阈值 1.0 内 roundoff）、`min_decorrelated_samples=4`（阈值 3，
      通过）、`max_endpoint_uncertainty_kJ_mol=3.507`（阈值 5.0）、
      `len(local_results)==len(valid_windows)`（100==100）——八项子条件加上
      窗口计数条件全部真实通过，不是手推。
- [ ] 用户跑 GPU resume：`ABFE_DEBUG_SKIP_STAGE2_FINGERPRINT=1 python
      runabfe.py --config abfe_config.json --ligand MOL --resume`。重点看
      窗口 0 这次是否真的进入冻结验证/production，而不是再次
      `IBSWarmupConvergenceError`。

## 性能/恢复能力加固（不影响当前数值定义）

- [ ] **production ESS 自动修复的第二调用点仍使用旧 per-edge 探针。**
      GitHub：[`openmm_IBS_dev#1`](https://github.com/Cedrus810/openmm_IBS_dev/issues/1)，
      Project 状态 `Ready`，Priority `P2`。
      `abfe_pipeline.py::_probe_vdw_window_fixed_overlap` 仍逐边调用
      `probe_bidirectional_overlap` 和
      `probe_bidirectional_overlap_for_bias_calibration`，没有接入已实现的 per-state
      trajectory bank；中间态重复采样、校准重试重新 burn-in。若迁移，manifest 必须
      加调用点/起始构型与 box 哈希，避免和 `run_all_windows` 的活体起点缓存混用；
      现有 per-edge 结果缓存应保持"先查结果缓存，miss 后再补 bank 轨迹"的层次。

## 待设计（非 P0/P1，新任务，不是复用已废弃的自动修复环）

- [ ] **pilot 热力学长度探测的安全自动加密机制（2026-07-19 诊断后新增）。**
      GitHub：[`openmm_IBS_dev#29`](https://github.com/Cedrus810/openmm_IBS_dev/issues/29)，
      Project 状态 `Backlog`，Priority `P2`。
      背景交叉验证：四窗口 TI 独立估算对比 window0=16.7、window1=13.4、
      window2=41.6、window3（尾段，未采样）=210.6 kJ/mol——真正落差大的是
      window 3，不是 window 0（window 0 此前观测到的 ~300+ kJ/mol 是在线学习
      估计器自身的数值病理，已改用 TMBAR 修复，见 `VALIDATION_MATRIX.md` 的
      `VAL-GPU-007`，`IBS_BIAS_PROTOCOL_VERSION=19`，与本条无关）。
      现状：`abfe_preoptimizer.py::optimize_stage2_vanishing`（1851-1932 行）的 pilot 网格
      分辨率天生等于最终态数（单轮、均匀初始 `linspace`），且每个 pilot 点只用
      10,000 步短探针估计 `beta^2*Var[dU/dlambda]`（`_sample_scalar_metric`,
      1753-1789 行）——对由稀有/发作性事件主导的陡峭区域，短探针会系统性低估
      方差，导致该区域分不到足够密度。TI 交叉验证显示这个问题在 window 3
      （尾段，210.6 kJ/mol 挤在 5 个态里）是真实存在的。
      **注意**：此前 2026-07-17 的修复（[[ibs_overlap_arbiter_false_premise]]，
      `sampling_repair_policy=non_mutating_v1`）只是**彻底禁用**了旧的自动 split/插
      λ/`recalibrate_f_k` 环——因为它用"相邻态 fixed-H overlap"当仲裁标准，是错误
      前提，而且会重洗已冻结的 f_k，烧了一周 GPU 没出结果。记忆里**没有**已经设计好
      的"正确版"自动加密方案，这是全新任务，不能直接复用旧代码。
      新设计需要满足：(a) 仲裁标准改用今天确认过的 LSE 平衡残差/f_k 占据自洽
      （`ibs_lse_balance_diagnostics`, `ibs_engine.py:272-306`）或 pilot 热力学长度的
      量级异常检测，不用 adjacent overlap；(b) 只允许"新开一个不可变 ensemble"，
      绝不原地重洗已冻结的 f_k/λ/态集合（沿用 `non_mutating_v1` 的核心原则）；
      (c) 解决 pilot 本身的低估问题——比如对测出的 metric_g 明显偏低但下游 IBS 学习
      仍然收敛失败的区域，触发一次更长探针窗口的重测，而不是仅仅重新分配已有的
      粗网格。人工/rescue-audit 网关保留。

## 验证监控入口

当前待验证项目、验收条件、目标环境和证据要求见 `VALIDATION_MATRIX.md`。本文件不再
复制验证清单，以免同一项目在两个地方出现不同状态。
