# 存档

## 2026-07-26 GitHub issue / TODO 同步

来源文件：`docs/TODO.md`

目标仓库：`Cedrus810/openmm_IBS_dev`

本轮目的：

- 读取 `docs/TODO.md` 中 ATT-01 到 ATT-26 的当前状态。
- 将已在 TODO 中标为 `[x]` 的 GitHub issue 更新正文并关闭。
- 将仍为 `[ ]` 的 GitHub issue 保持 open，并把正文从旧的“未验证审查发现”改为当前 TODO 描述。
- 由于 Codex GitHub connector 可读但更新 issue 返回 `403 Resource not accessible by integration`，最终通过本机已登录的 GitHub CLI 执行。

GitHub CLI 路径：

```powershell
C:\Program Files\GitHub CLI\gh.exe
```

认证状态：

- GitHub account: `Cedrus810`
- Token scopes: `gist`, `read:org`, `repo`, `workflow`

执行过的临时脚本：

- `update_github_issues_from_todo.ps1`
- `continue_github_issues_from_todo.ps1`

这些脚本只用于本轮一次性同步 GitHub issues；同步完成后不再保留为根目录工作文件。

### 已关闭 / 已完成

- `#42` ATT-01：`scan_boresch_1d_pes` 不是孤立顶层 `self` 问题。
- `#43` ATT-02：`VANISHING_TARGET_INTERVALS_PER_ENSEMBLE` 常量拼写与导入路径已验证。
- `#44` ATT-03：单 GPU Context 无界常驻风险已消除。
- `#46` ATT-05 / P0-5：IBS energy/bias/base 三文件 manifest 与 fail-closed 验证已实现。
- `#48` ATT-08：溶剂腿 `topology` 参数有一致性 hard gate 语义。
- `#50` ATT-10：IBS base-energy failures 已 hard-gate。
- `#52` ATT-12：vanishing 路径/子域范围契约已明确。
- `#53` ATT-13：TMBAR history 已有界。
- `#55` ATT-15 / P1-9：PBC-aware bulk-water 反离子选择已修复。
- `#56` ATT-16 / P1-10：全局 TMBAR 误差已传播 covariance。
- `#57` ATT-17 / P1-11：预平衡 fingerprint 已绑定 pose/box/steps。
- `#58` ATT-18 / P1-12：溶剂盒已改用 OpenMM padding 语义。

### 保持 open / 仍待处理

- `#45` ATT-04：parallel stage worker spawn 安全与导入期 CUDA 初始化边界。
- `#47` ATT-06/ATT-07 / P0-6/P0-7：DEXP 拟合模型、生产 Hamiltonian、`offset_c0` 契约。
- `#49` ATT-09：热力学循环符号需要集中到单一函数并加 toy-cycle 测试。
- `#51` ATT-11：`GeometricRestraintEstimator` 单一 0.22 nm 几何键阈值需替换。
- `#54` ATT-14 / P1-8：IBS 中 DEXP cutoff/switch 配置传递需按新 DEXP 版本复审。
- `#59` ATT-19：核心物理单元测试矩阵不足。
- `#60` ATT-20：缺少公开 ABFE benchmark 端到端集成验证。
- `#61` ATT-21：用户文档、安装/API/输入格式和 thermodynamic-cycle 文档不足。
- `#62` ATT-22：缺少 CI/CD、静态检查、typing 和 formatting。
- `#63` ATT-23：运行恢复与资源保护能力不足。
- `#64` ATT-24：CLI 与物理输入验证不足。
- `#65` ATT-25：协议版本注册表和迁移工具缺失。
- `#66` ATT-26：`IBSWindowManagerDualLambda.run_all_windows` 仍需拆分。

### 备注

GitHub Project `https://github.com/users/Cedrus810/projects/1/views/1` 是否需要额外更新，取决于 Project 自定义字段是否自动跟随 issue closed/open 状态。Issue 本身已经同步；Project 的自定义 `Status`/`Priority`/`Iteration` 字段若没有自动化，可能仍需手动或用 `gh project item-edit` 单独处理。

## 2026-07-27 GitHub issue / TODO 同步

来源文件：`docs/TODO.md`

执行脚本：`sync_github_issues_20260727.ps1`（一次性临时脚本，执行后删除）。

本轮同步内容：

- 关闭 `#45` ATT-04：import 期 CUDA 初始化已消除，原归因已更正。
- 关闭 `#49` ATT-09：热力学循环已统一到 `abfe_core.combine_binding_free_energy()`，并修复 traditional/API 路径漏 APBS。
- 更新 `#52`：记录 ATT-12 仍关闭，但旧 v20 Fisher/square-anchor 描述已退役，当前为 v21 blended-metric schedule。
- 更新 `#62`：补入 `run_offline_tests.sh` 入口脚本 bug 与 CI/测试入口要求。
- 更新 `#64`：补入显式输入文件和物理 preflight fail-closed 缺口。
- 更新 `#33` 标题与正文：从旧 v18/v20 Fisher schedule 改为 v21 blended-metric lambda schedule CUDA 验证。
- 更新 `#66`：保留 run_all_windows 拆分待办，同时记录 2026-07-27 对旧行数描述的更正。

新建 GitHub issues：

- `#67`：P0-9，harden analyze-only against stale or incomplete IBS stage data。
- `#68`：P1-13，truncate or segment sampling history after production catastrophe rollback。
- `#69`：P1-14，repair molecules across PBC before first minimization or pre-equilibration。
- `#70`：P1-15，preserve convergence, coverage, ESS, and rescue evidence in stage checkpoints。
- `#71`：P1-16，wire traditional-mode resume through run_full and run_leg。
- `#72`：P2-15，correct single-stage lambda endpoint diagnostics semantics。

Project board 自定义字段未在本轮自动更新；issue 本体已经按 `docs/TODO.md` 同步。

## 2026-07-27 GitHub issue / TODO 同步（二）

来源文件：`docs/TODO.md`

执行脚本：`sync_github_issues_20260727_b.ps1`（一次性临时脚本，执行后删除）。

本轮同步内容：

- 关闭 `#51` ATT-11：Boresch anchor neighbor discovery 已改用拓扑真实成键关系，`0.22 nm` 几何阈值降为显式 fallback。
- 关闭 `#68` P1-13：production catastrophe rollback 已同步截断 energy/bias/base 三份 history。
- 关闭 `#69` P1-14：整分子 PBC 修复已提前到首次 Context/minimization/pre-equilibration 之前。
- 关闭 `#70` P1-15：stage checkpoint 已保存并复检 convergence/coverage/ESS/rescue 证据。
- 关闭 `#71` P1-16：traditional 模式 `--resume` 已从 CLI/config 贯穿到 `run_full -> run_leg`。
- 关闭 `#72` P2-15：单阶段 endpoint diagnostics 已按 Stage 1/Stage 2 各自物理端点修正。
- 更新 `#62`：记录 `run_offline_tests.sh` 入口 bug 已修、CPU/OpenMM/PyMBAR 全套 367 passed； broader CI/CD 仍 open。
- 更新 `#64`：记录显式 config/torsion 文件 fail-closed 已修； broader input validation 与 DEXP 输入契约仍 open。

新建 GitHub issues：

- `#73`：P0-10，committed Boresch equilibrium reuse 已加当前 pose/identity gate；创建后作为已修复审计项。
- `#74`：V-06，归档 endpoint-sigma diagnosis 与 VDW rescue 结果证据。
- `#75`：低优先，跨进程 production resume/rebuild 边界应作为独立 trajectory segments 处理。
- `#76`：低优先，锁定 `pymbar-core` 版本以稳定 uncertainty semantics。

Project board 自定义字段未在本轮自动更新；issue 本体已经按 `docs/TODO.md` 同步。
