# 变更记录（缩略）

[项目入口](../README.md) · [文档导航](README.md) · [当前科学状态](STATUS.md) · [历史材料索引](HISTORY_LOG.md)

> **本文是全程缩略时间线：一行一条，只记"改了什么、哪天、破不破缓存"。**
>
> 详细版本说明**不在这里**，在三个地方，本文只指路：
>
> | 要查 | 去哪 |
> |---|---|
> | 某个协议版本号为什么跳（逐版全文） | 源码常量上方的注释块（`ibs_engine.py` / `abfe_preoptimizer.py`） |
> | 某份开发期材料写过什么 | [HISTORY_LOG.md](HISTORY_LOG.md)（148 份逐份登记） |
> | 某个数字能不能引用 | [STATUS.md](STATUS.md)（唯一的科学结论声明处） |
>
> **⚠️ 本文不是科学结论。** 条目写的是"代码/协议变了"，不是"结果对了"。

## 工作区与体系

本项目在三个体系、四个工作区上推进过。看日期先看体系：

| 体系 / 工作区 | 时间 | 现状 |
|---|---|---|
| **Atenolol-rank11**（β 阻滞剂，第一个体系） | 2026-06 ~ 08-31 | 结果全部 `INVALIDATED`；8.1G 原始材料保留，不再是真源 |
| **4W53**（T4 lysozyme L99A + toluene，**当前主线**） | 2026-08-27 ~ | 当前唯一有效体系 |
| **ABFE_IBS**（本仓库，工程区分支） | 2026-08-31 ~ | 只留生产代码 + 回归测试 + 使用文档 |
| **ABFE_IBS_CUDA**（EXP-031 沙箱） | 2026-09 | GPU 优化实验，正文与证据不在主线 |

---

## 2026-09（最近三天）

> 📌 **09-14 及更早的全部条目**（含 09-14 一天 15 条的导读、2026-08 / 2026-07 / 2026-06 三节）
> 已移进 [archive/CHANGELOG_before_2026-09-16.md](archive/CHANGELOG_before_2026-09-16.md)。
> 本文只留最近三天。

| 日期 | 变更 | 破缓存 |
|---|---|---|
| 09-18 | 📄 **README 整理：根目录 4 份 → 3 份，落地页改成英文。** 原来 `README.md`（简明中文）/ `README_cn.md`（完整中文）/ `README_en.md`（完整英文）三份并存，而 `README.md` 是个**把真正的说明藏在链接后面的存根** —— GitHub 首页恰好渲染的就是它，信息量却最少；三份之间 clone-and-run / 工程区分支 / 许可三段逐字重复，**仓库目录树更是在两份 README 里各抄了一遍**，而权威在 `PROJECT_LAYOUT.md`（加起来 3 份，加个模块要改三处 —— 又是「同一不变量 N 份实现」）。现在：**`README.md` = 完整英文（首页）**、**`README_cn.md` = 完整中文**，`README_en.md` 并入首页后删除；两份都把目录树换成指向 `PROJECT_LAYOUT.md` 的一句话、把文档表压到最常用 4 行并指向 `docs/README.md`。引用已改完（`docs/README.md` 导航、`docs/TODO_P3.md` 的判定条目）；残留的 `README_en` 字样全是描述 2026-08-24 那次事故的**过去式叙述**，不是链接。⚠️ **`docs/TODO_P3.md`「英文文档追平中文 = 明确不做」这条判定没有被推翻** —— 只是首页语言换了，两份 README 仍然**不做逐字对等**，`docs/` 下的教程仍然以中文为准。代码零改动。 | 否 |
| 09-18 | 📄 **文档整理：`docs/` 顶层 31 → 24 份，索引 203 → 约 150 行。** 六份已结案文档移进 `archive/` 并各自加页首归档告示（`STAGE2_CONTROLLER_WAVE_2026-09-17` 10/11 已修、`CONTROLLER_BUDGET_AUDIT_2026-09-14` 65 条全落地、`STAGE2_AUTONOMOUS_LOOP_STATUS_2026-09-11` / `STAGE2_THREE_AXES_COUPLING_2026-09-11` 被 09-12 控制器设计取代、`AUTONOMOUS_STAGE2_LOOP_SPEC_2026-09-11` §1/§4 已过期、`EXP-031_GPU_OPTIMIZATION_2026-09-09` 迁移已执行完），**同一次改动里改完全部 43 处引用** （`abfe_core.py` / `ibs_engine.py` 各 1 处注释 + 3 个测试 + 13 份文档），`test_docs_internal_links_resolve` 0 坏链。`docs/README.md` 按「手册 / 状态与待办 / stage-2 控制器 / 其它活文档 / 子目录」重排，**删掉表格单元格里的整段论证与按日期记的归档流水账** —— 那些信息在各文档页首都有，索引里重复一遍就是第二份会过期的实现（并删掉一行完全重复的 `STAGE2_CONTROLLER_DESIGN`）。⚠️ 顺带修掉一处**真的过期的科学状态**：`STATUS.md` 仍写着 09-14 审计「44 条已修，18 条仍 `OPEN`」并点名 5 条「会炸整跑 / 会静默产出错误结果」，而 09-16 的逐条回源码对账早已证明 **65 条全部落地** —— 按那份审计自己留下的规矩，「修没修」的权威只有源码。归档那两份唯一记载核心验收指标的文档之前，先把那句指标抄进 `STAGE2_CONTROLLER_DESIGN_2026-09-12.md` 新的 §0。**代码零逻辑改动**（只动注释与 docstring 里的路径）。 | 否 |
| 09-18 | 🔴 **Stage-2 控制器：09-18 benchmark 批次分诊，`S2-N`/`S2-O` 两条已修。全文 → [STAGE2_BENCHMARK_CRASH_TRIAGE_2026-09-18.md](archive/STAGE2_BENCHMARK_CRASH_TRIAGE_2026-09-18.md)，逐条 → `TODO.md` 的 `S2-N`/`S2-O`。⚠️ 这批跑的是 09-17 整波修复**之后**的代码（工作树 mtime 09-17 19:51/20:15 早于首崩 23:00），那 10 条没挡住这两条链。** ① **`S2-N`：块账写侧记 4 个动作、读侧只拦 1 个。** `_BLOCK_CHARGING_ACTIONS` 声明 `RUN_PRODUCTION`/`PROBE_REANCHOR_EPOCH`/`RECALIBRATE_FK`/`PROVISIONAL_PRODUCTION` 都消耗一个生产块，而 `plan()` 的块数硬上限闸只挂在 `RUN_PRODUCTION` 上 ⟹ 另外三个**扣配额却从不过闸**。真机 `cmet_ligand2/rep3` w4 = 2 块补帧 + 3 块 `RECALIBRATE_FK` = **5 块**（上限 4），且轮 13/14 各开一个新段把累计 500k 作废回 250k；`cyclod_ligand3/rep3` w1 同形。它们吃掉的正是插 λ 作废下游之后**重采**要用的配额。修法：闸条件改成 `if action in self._BLOCK_CHARGING_ACTIONS:`（写侧读侧合成同一张表，与 `S2-L`/`BUD-03` 同族）。⚠️ **三道闸不能整套套给它们**：第二道（边际增益刹车）与第三道（射程闸）问的都是「同分布加帧还有没有用」，而 `RECALIBRATE_FK` 恰恰是「加帧没用」时的**对症动作** —— 拿"加帧没用"否决它方向完全反了，会掐掉唯一对症的路。所以 `_frames_admission` 新增 `hard_cap_only`，非补帧动作**只过第一道**（块数硬上限是资源账，与动作无关）。② **`S2-O`：停下时零产出。** 退出前的全路径 `ANALYZE` 在 `stale_layout_evidence` 非空时被整个跳过 ⟹ 没有任何 stage 级产物落盘 ⟹ 人工接手连「哪个窗口坏在哪」都要重翻散文件（真机五个 run 全部如此；`cyclod_ligand3/rep3` 16 轮里一次 `ANALYZE` 都没发过）。**跳过的决定本身是对的**（`run_once` 对 stale 窗口就是会重采，那会绕过控制器刚做的停止决定），所以不推翻它，改成**跳过时落一份零采样的诊断**：`write_comparison_manifest()`（纯聚合、不跑求解），路径记进 `outcome.final_diagnostic_manifest`。⚠️ **范围别记大**：不解决 `S2-A`（`solver_n_frames_decorrelated` 只存在于 stage 求解结果里），也**不阻止** `_assert_stage_result_sane` 抛错 —— 路径确实不完整，那道 fail-closed 是对的，**不许放宽**。先前写在 TODO 里的候选修法②「别让 8407 炸」已作废并留墓碑。⚠️ **崩溃计数口径**：13 个崩溃 run 里 **4 个是历史归档目录**（09-11 ×2、09-16 ×2）被扫进来的误报，本批真崩 **9 次**；扫 `launch.log` 取签名前必须先按 mtime 切到本批。⚠️ **没有动任何协议版本号**，**尚未上机复验**。全量 **2736 passed / 0 failed**，两条改动均做过还原变异验证。 | 否 |
| 09-17 | 🔴🔴 **Stage-2 控制器一整波：真机跑出 11 条缺口，10 条已修。全文 → [STAGE2_CONTROLLER_WAVE_2026-09-17.md](archive/STAGE2_CONTROLLER_WAVE_2026-09-17.md)，逐条 → `TODO.md` 的 `S2-G/H/J/K/L/M`、`TODO_P2.md` 的 `S2-A/B/E/F/I`。** **这一波的共同性质：全部是「判据/信号算出来了却没人消费」或「动作在构造上不可能成功」，不是阈值调参。** 六条 P1（会让整跑崩掉或白烧）：① `S2-L` 补帧的**块数硬上限被换布局重置** ⟹ 插一次 λ 配额就清零重发 4 块 ⟹ **补帧实际没有有效上限**（真机 `cmet_ligand1/rep1` w4 旧口径 2 块、真实 **9 块**；`brd4_ligand1/rep2` w3 累计 1,250,000 步 = 5 块）。与 `BUD-03` 是**同一个 bug 换了一个维度**（那次是「换段退钱」），而这本账的定义就写着「硬上限 = **资源账**」。修法：硬上限那本不按 `path_version` 过滤；**边际增益那本仍然要过滤**（跨布局比 `min N_eff/g` 没有意义）。② `S2-M` 「布局过期 + 冻结验证批次打满」死锁：1d-0 无条件发 `RUN_PRODUCTION` → 预热门弹回 → no-op 又记不下（stale 窗口指纹无可比身份）→ 连发 3 次 → 降级被 stale 挡 → 终止；改为分流到 `PROVISIONAL_PRODUCTION`（它产**新布局下**的帧，一并解决过期）。③ `S2-K` `ANALYZE` 跨段合并求解撞过期证据 `ValueError` **炸穿主循环**（跨 run 统计里最常见的末行）—— 判据侧 `classify_layout_evidence()` 明明知道哪个 (段,窗口) 过期，执行器侧无过滤加载；改为降级为「本轮拿不到跨段证据」并记进 history，不吞成静默。④ `S2-G` 退役判定只扫**有产物**的窗口 ⟹ 对「整窗未采、预算分文未动」的窗口恒答「别处没活干」⟹ 整跑终止。⑤ `S2-H+F` **开新段的动作既没有次数预算、也没有事后有效性判定** —— `RECALIBRATE_FK` 每次都改盘面 ⟹ 停滞保护（同一动作 + 盘面未变）与 no-op 台账（指纹含段维度）对它**结构性免疫**；真机连发 4 次、每次丢掉上一段累计步数、验收量 9.645 → 4.182 → 0.837，盘上留下 7 个采样段。修法不发明阈值：次数用与 `max_path_insertions` 同性质的终身预算，效果复用 `marginal_gain_stalled()`；**效果闸必须排在次数闸前**，否则默认配置下永不可达。⑥ `S2-J/I/E` 末窗永不拆 —— `_legalize_tail_window()` 的「补 1 到 K=7 ⟹ 拆 4+4」**早就完整实现**，但 `hi=8` 让死区 `(hi, 2·lo−1) = (8,7)` 成为**空集**、且 `K ≤ 8` 直接 return ⟹ 整段永不执行。P2：`S2-B` 缺窗覆盖被**纯补帧**挡住（补帧不重锚、作废不了下游，挡它没有物理依据）；`S2-A` solver 侧证据在整个采样阶段恒空 ⟹ 三条判据不带电 —— **本次只消除静默**（`NO_CRITERION_INPUT_AT_ALL` 与 `NOT_ENOUGH_POINTS` 分开 + `plan()` 恒出 `inoperative_criteria`），**行为一字未改**，根因归 `AUDIT-S2-02`。⚠️ **三处我做错又改正的，都留了痕**：(a) 早期 `ANALYZE` 连打红 25 → 11 条后回退，原位注释「别再试图靠挪 `ANALYZE` 的位置来补它」；(b) 一度要加 `SPLIT_LAST_WINDOW_IN_TWO` 动作，写完才发现 `_legalize_tail_window` 早就实现了 —— **同一不变量的第二份实现**，全部撤回留墓碑；(c) 曾判定改窗口上限不作废缓存（只查了第 1 层），已更正。⚠️ **没有动任何协议版本号。** 全量 **2731 passed / 0 failed**。 | 否（代码部分） |
| 09-17 | ⚙️ **`abfe_config.json`：`stage2_window_max_states` 8 → 5**（用户拍板）。**这不是调参，是让一条早就写好、却因为这个数而永不执行的修复路径重新可达**（见上一条 `S2-J/I/E`）：`hi=8 ⟹ split_lo = 2·lo−1 = 7 ⟹ 死区 (8,7) 空集`，K=6/7/8 全是「合法单窗」⟹ 末窗永远不拆；`hi=5 ⟹ 死区 = {6}` ⟹ 末窗 K=6 → 补 1 → K=7 → 唯一拆法 **4+4**。⚠️ **原来那个 8 的理由已经失效**：它是 09-14（审计 #49）为了让 **∫g 均衡**的分窗跑得动才从 5 提到 8 的，而 **∫g 已于 09-17 退役**（`stage2_window_partition` 现为 `state_count`，理由是它数学上重复计数、实测 ρ(∫g, rawESS)=+0.196 **符号是反的**）。新目标 min-max K 本来就要把 maxK 压小。⚠️ **不改初始布局**：离线验证（n_states=18、`state_count`、win0 cap=4）hi=8 与 hi=5 都给 `K=[4,5,5,4,4]`、maxK=5、5 个窗口 —— **逐字相同**；`hi` 只在**修复路径**上起作用。旁证（跨 run 完成率快照）：maxK=5 → 4/4、maxK=6 → 2/5、maxK≥7 → **0/22**；⚠️ 那张表有**反向因果**（插 λ 是修复动作 ⟹ 高 maxK 部分是「出过问题」的结果），不能单独当依据。 | **是（仅 stage 结果缓存）** —— 本键在 `_PREOPT_DERIVED_PATH_KEYS`（**第 2 层**派生路径），按设计可从旧 pilot **离线重算、不重烧 GPU**；它**不在**窗口采样身份里（2026-09-10 从 `_stage_protocol_key` 摘掉）；加上布局逐字相同 ⟹ 窗口 λ 不变 ⟹ 轨迹可复用。失配的只有 stage **结果**缓存，而重解是零采样。 |
| 09-17 | 🟢 **补帧准入补上第三道：剩余配额的乐观上界够不着门 ⟹ 停**（`DEAD_LINES §3.1+§3.2` 的共同根因）。`n_eff_over_g_reachable_by_frames` 的 docstring 自己写着它是**上界**、「`True` 只表示值得一试」，并且「真正的刹车是事后的 `marginal_gain_stalled()` —— 两者一前一后，**缺一不可**」。而那个刹车的判据是「末点 < 前面各点中位数 × 0.9」⟹ **单调上升但渐近在门以下的序列永不触发**（真机 cyclod_ligand1_outer/rep1 win0 逐块 4.459/4.553/5.287，门 10，按 +0.41/块要 11 块而配额只有 4）⟹ **这一对少了一半**：进的时候乐观、出的时候没人管。补法**不引入任何新阈值、也不拟合斜率**（那个量单次噪声 34×，3 个点拟斜率同样在量噪声）——就是**同一个前向判据**，拿**现在的读数**和**剩下的配额**再问一次：`当前 ratio × 剩余 headroom < 门 ⟹ 连上界都够不着 ⟹ 停`；`frames_growth_headroom` 本就是 `(1+cap)/(1+used)`，「剩余」是它的原义。真机数代入：used=1 ⟹ 4.459×2.5=11.1 ≥ 10 放行（此刻确实还可能够得着）；used=2 ⟹ 4.553×1.67=7.6 < 10 ⟹ 停。🔑 **挂在 `_frames_admission`（全部 16 个 `RUN_PRODUCTION` 出口的唯一咽喉），不挂在归因上** ——只有「自检归因」那两条入口会问射程，缺窗/跳窗/旧布局/步数未达标一概不问（FLOW 的 D1）。⚠️ 只在明确判 `False` 时拦，`None`（缺数/门缺失/非有限）一律放行 —— 未知不是「够不着」。⚠️ 与 `support_failure_is_skew` **共用同一个函数** ⟹ 射程一旦够不着，归因同时从「样本量类」翻成「偏斜类」，分支侧会先发缩跨度（`INSERT_LAMBDA`/有界重窗）；准入层是给其余十几个不查归因的入口兜底。三条新测试：射程内必须放行（防「一律不补帧」作弊）、射程外转对症动作、跳窗入口也被咽喉拦住。 | 否 |
| 09-17 | 📌 **登记一条**判定**：`openmm` 两份环境文件都**故意停在 8.5.2，不升 8.6**（维护者判定：8.6 尚不成熟、有已知严重 bug，本项目**暂**不使用 —— 是"暂"不是"永不"，8.6 的官方 REMD 正是本项目想要的，**未来版本可能支持**）。与代码里 `free_energy_engine.OFFICIAL_REMD_MIN_OPENMM_VERSION = "8.6.0"` **不矛盾** —— 那是「官方 `ReplicaExchangeSampler` 要 8.6 才有」的**资格判据**（后端选得上选不上），环境说的是「今天不装」。**代码支持得了 ≠ 现在就该用。** ⚠️ 本条先前一度被我改成下限 `openmm>=8.5.2`（并把 CI 对账改成按运算符分流），**已全部还原**；理由写进了 `environment-ci.yml` 与 `GETTING_STARTED.md`，防止下次再被当成"环境文件禁止了代码支持的版本"重开一遍。要放开必须先复验 8.6 那些 bug，并连带重编 `plugins/LocalManyBodyResidual/*.so`（对着 8.5.2 编的，跨版本无 ABI 承诺）。 | 否 |
| 09-17 | ✅ **`REL-08` 关闭：基础环境本来就存在，欠的是口径。** `environment-ci.yml`（43 行手写）就是 CPU 基础环境，**不含** CUDA 工具链 / torch / MACE / JAX；`environment.yml`（333 行 export、全钉死）是 GPU 生产环境。从 export dump 里手术式删 CUDA 行删不干净，所以改的是文档定位：`GETTING_STARTED.md` 加「两个环境文件，装哪个」对照表 + 插件那节补上「基础环境的 openmm 是下限，装到 8.6 后预编 `.so` 不保证能载」。 | 否 |
| 09-17 | 🟢 **`LR-04` 关闭：新增 `--only-ligand-in-water` 调试入口**（跳过 Boresch 估算 / rebalance / 整条复合物腿，只跑溶剂腿）。**定位是调试不是生产：不产出 ΔG_bind、不覆盖主 `final_results.json`。** 三道 fail-closed：① 与 `--only-complex-charging` / `--only-boresch-attachment` 互斥；② 与 `--outer-lambda-local-residual-ibs` 互斥（残差运行时在复合物腿那段构造、再按溶剂腿拓扑重建，跳过复合物腿就拿不到 ⟹ 抛错而不是静默降级成 baseline 溶剂腿）；③ 溶剂腿跑完**直接 `return`**，不走到第 8 节 —— 比"补个 `None` 往下传"安全，后者每加一个下游分支就多一次把半份结果写成完整结果的机会。`tests/test_only_ligand_in_water_lr04.py` 5 passed（控制流契约）。⚠️ **真跑要 GPU，没跑过**，钉住的只是不跑也能错的那部分。 | 否（新入口，默认关；不改任何既有路径） |
| 09-17 | 🟢 **用户拍板的三件全部落地。** ① **删死分支**：`decide()` 里「`allow_untrusted` + 分析不完整 ⟹ `DONE_UNTRUSTED`」从未发出过 `DONE`（O4 必然改写，且走到那里 `analysis_status` 必为 `ANALYSIS_INCOMPLETE`）。**不给 O4 开豁免**的理由不止「O4 对」：`ANALYSIS_INCOMPLETE` 是缺窗/跳窗/求解失败这类**硬不变量**没满足，即使放开 O4，下游`analysis_status != ANALYSIS_COMPLETE ⟹ RuntimeError` 照样拦死 ⟹ 只会「先报完成再被打回」。`allow_untrusted` 本身**没被削弱**（仍改 `trust_level`、分析完整时仍给 `DONE_UNTRUSTED`、仍管跳过 rescue）。回归测试出口 `HALT_EVIDENCE_CONTRADICTS_DONE` → `NO_FEASIBLE_ACTION`，`evidence_status != CONVERGED` 一字未动。② **`_legalize_tail_window` 不再借 `first_untrusted`**：合法化用**自己的**切点=末窗首态（天然是与前一窗的共享节点）。原来「合法化」借用了「修复」的定位谓词 ⟹ `first_untrusted ∈ {0, None}` 时 anchor 恒 None、抛 RuntimeError，而非法布局**已落盘**（真机 cyclod_ligand3/rep1+rep3）。⚠️ 那**不是死区**：死区 `hi<K<2lo−1` 在 lo=4/hi=8 下是空集，K=9 落在可拆区间 [7,15]、9→5+5 完全合法。新增三道 fail-closed（单窗布局拒绝、共享节点自检、前缀逐字不变）+ 「所有新窗口 ∈[lo,hi]」断言；**不放宽** `tail_repartition_anchor()`（那会扩大 `SPLIT_TAIL_WINDOW` 这个**修复**动作的语义）。顺带补上第二个调用点缺失的 `partition_criterion`（原来吃默认 `arclength`，与另一处不一致）。③ **端点不确定度改门**：`max_endpoint_uncertainty_kJ_mol` 不再单独设 `results_untrusted` —— 它的文本自称「仅诊断，不拒绝」而 `results_untrusted` 会一路传到最终结果并打印「不得作为可发布结果引用」，所以「仅诊断」是假的。读数/阈值/消息照留，新增 `drives_action:false` + `affects_trust:false`；**不碰 `analysis_status`**，发布验收仍由硬不变量 + `precision_status` 把关（去掉 untrusted ≠ 可发布，单 run 精度恒 UNMEASURED）。⚠️ 顺带把另外三道**仍然**拒绝发布的门（min_overlap / min_absolute_ess / min_decorrelated_samples）的假措辞「**仅诊断**，不拒绝」改成真话并标 `affects_trust:true` —— **行为一字未改**，只是不留已证伪的陈述。⚠️ 旧 JSON 里已落盘的 `results_untrusted` 不会被本改动洗掉，也**不该**全局清除（那几份多半还有别的真实失败），重新求解该 stage 时自然重算。全量 **2658 passed / 0 failed**。 | 否 |
| 09-17 | 🧹 **删除 `abfe_pipeline.RunDirectoryLock`（输出目录独占锁）与它的 5 条测试，进 `archive/run_directory_lock_removed_2026-09-17.py`。** 生产侧零调用点 —— 它曾挂在 `runabfe.py::main` → `guard_run_directory()`，那句早已删掉（理由见 `guard_run_directory` docstring：SIGKILL 后锁文件残留、共享盘上 stale 判不出来 ⟹ 锁挡住的是 `--resume` 本身）。⚠️ 归档头部记了一条**复核确认的机制缺陷**：`timeout_s=0.0` ⟹ 父类 `_PipelineStateLock.__enter__` 的 `deadline` 就是当下 ⟹ 第一次 `FileExistsError` 后必抛 `TimeoutError`，**`os.open` 永不重试**；于是 `_break_stale_lock_if_needed()` 即便清掉了残留锁，**这一跑照样死**，清理成果只留给下一次启动（与实测「拦一次、几十秒后重启就过」吻合）。坑不在「不等待」（docstring 明写的设计），在于**「不等待」与「不重试」被写成同一件事**，stale 清理路径因此对它实际失效 —— 将来重新接线要先加 break 后的重试。📌 父类 `_PipelineStateLock`（`timeout_s=10.0`）**不受影响**，`pipeline_state.json` 照常用。同文件的 SIGTERM 与磁盘预检两节测试**保留**（10 passed）。 | 否（无调用点） |
| 09-17 | 🧹 **删除 EXP-010/011 的死 CLI 表面：9 个子命令 + 13 个转发壳，移入 `archive/outer_lambda_exp010_exp011_removed_2026-09-17.py`（`TODO_P2` 的 `LR-03`）。** 这些壳全部转发给 `archive.outer_lambda_exp010_exp011_legacy`，而**那个模块从来没进过本仓库** —— `archive/` 目录不存在、`git log --all -- archive/` 为空；壳是 2026-08-31 `169514e`（主线迁移）引入的，1718 行真实现留在旧工地 `Atenolol-rank11/archive/`。⟹ **这 9 个子命令从迁移那天起就是 `ImportError`，一次都没跑起来过**，`LR-03` 原文「要么补实现、要么删入口」里的「补实现」在本仓库不成立（等于把旧库搬回来重新纳管），而「legacy 读 `manifest['lambda_shield']` → `TypeError`」那半条**永远够不着**，`ImportError` 先炸。删掉：`exp011-coverage` / `exp011-fit-pmf` / `exp011-umbrella-sample` / `exp011-reweight-umbrella` / `exp010-label` / `exp010-fit` / `exp010-prepare-selection` / `freeze-slow-variable` / `sample-hard-window-scratch`，13 个壳，`__all__` 里 12 个名字，以及 `tests/test_outer_lambda_cli.py` 的 `--help` 清单对应行。⚠️ **同期的 `screen-slow-variables` / `compare-slow-variable-screens` / `wp0-select` 没删** —— 它们不碰这些壳，仍能跑。`outer_lambda_neural_basis.py` 9472 → 8109 行；`--help` 从 27 个子命令降到 18 个。13 个壳在主线与测试里的外部引用数是 **0**（删前逐个数过）。 | 否（纯死代码，运行时无任何调用点） |
| 09-17 | ✅ **`TEST-01` 关闭：198 个测试文件全部落进 `cpu_only` / `needs_gpu` 之一。** 22 个无标记文件加 `pytestmark = pytest.mark.cpu_only`（8 个顺带补 `import pytest`），外加 `test_exp019_softlift_loro.py` 里**漏网的单个测试** —— 该文件已有 `@pytest.mark.needs_gpu` 挂在另一个测试上，按文件 grep 看是「有标记的」，实际第一个测试两档都不属于。📌 **所以判据只能用收集数对账，不能用 grep 文件。** 实测（`CUDA_VISIBLE_DEVICES=''`）：`-m 'not cpu_only and not needs_gpu'` → **no tests collected (2666 deselected)**；22 个新标文件在**无 GPU 可见**下 **235 passed**（不是「本机跑过了」，卡被藏掉了）。判 `cpu_only` 的依据是它们只读源码 / AST / monkeypatch —— 两个名字带 CUDA 的（`test_lr06_…` 读 `.cpp` 文本、`test_nvcc_discovery_…` 全程 monkeypatch 环境变量）都不建 Context。 | 否 |
| 09-17 | 📋 **`REL-09` 关闭（**无代码改动**，逐项回源码核实后发现三项里两项早已落地）**：① SIGTERM/SIGINT ✅ `install_termination_handlers`（09-17 真机触发过：`cyclod_ligand1_outer/rep1` 02:29 的有序退出）；② 磁盘预检 ✅ `ensure_free_disk_for_stage`（stage 前 `free ≥ 2×预估`，fail-closed，ATT-23），原文「只有 doctor 里有 `disk_usage`」是它落地**之前**的状态；③ 输出目录独占锁 ⚖️ **已判定不要**（`guard_run_directory` docstring：SIGKILL 后锁文件残留、共享盘上 stale 判不出来 ⟹ 锁挡住的是 `--resume` 本身）。⚠️ **原条目引的「目录锁真机咬人 4 次」证据方向是反的** —— 那 4 次全在 `runs/cyclod_ligand1/rep1/launch.log` 的 **09-10** 段，traceback 指向后来被删掉的那把锁，咬的就是续跑。**别拿它重开这条。** | 否 |
| 09-17 | 📌 **`LR-06` 第 6 步真机通过（补记，当天没人写进 TODO）。** 同一份 `runs/cyclod_ligand1_outer/rep1/launch.log` 两次启动直接对照（判版本用**启动横幅**不用 mtime）：09-16 02:46（夹取前）行 303–357 窗口 0 热化 `fail-closed … pair distance below 0.1 A`、步长降到 0.008 fs 仍炸；**09-17 01:22（夹取后）行 358–1251 `fail-closed` 0 次**，窗口 0–4 全部跑出能量，走到 `[自治 15/40]`，02:29 终止于**人为 SIGTERM**。⟹「outer 臂在窗口 0 必死」这个失效模式已消除。第 5、7 步按维护者判定**押后到控制器改完**。同批补记：`EXP-033-P1-GPU` 真机首跑也过了，但拟合是空模型（B 离散度 2.9e-06 kT），只证明不炸。 | 否 |
| 09-17 | 🟢 **死线 D1 / D8 的「报告侧」改进（`archive/STAGE2_DEAD_LINES_2026-09-17.md` 点名的两条，都不改路由）。** ① **D1**：终态窗口的 `last_failure_reason` / `last_gate_error` 原来只出现在 D1 那条分支的 `reason` 字符串里 —— 换一条出口（退役换窗，或 TERMINAL 窗口根本不是 `earliest`）就整个看不见，而这是人工接手唯一需要的东西。现在恒常输出到 plan 顶层 `terminal_window_failures`，与 `earliest` 是谁无关。⚠️ 两个键**不在同一个文件**（前者在 ibs_state、后者在 convergence 的 `bias_warmup`），正是它们该被提到 run 级的理由。② **D8**：兜底原来把「确实没有对症动作」与「归因读到坏数」说成同一句话，而两者处置相反。新增 `_attribution_blind_spots()`：把归因读数分成 **corrupt（NaN/inf）** 与 **absent（None）**——NaN 的毒性是**每一个比较都是 False**（`nan>=x` 与 `nan<x` 同时为假）⟹ 上游静默一条都不命中，长得和「真的没动作」一模一样；absent 则是正常（很多窗口本来就还没有 `cum_fk_verdict`）。只改措辞与 `missing_evidence`，**不给 D8 配任何动作**（配了就是「再跑一次直到碰巧通过」）。⚠️ 刻意**不做**「全部读数都读不出来 ⟹ 全盲」那种判据：有产物的窗口 `phase` 恒可导出，全盲构造上不可达，写了就是死支。另：`segment_index_of_dir` 的 docstring 补了「别把 `rewindow_<sha12>` 简化成裸 sha12」——现在是**构造上**安全（字面量前缀让 `isdigit()` 恒 False），去掉前缀就降级成 0.34% 概率碰撞。全量 **2657 passed / 0 failed**。 | 否 |
| 09-17 | 🟢 **REWIND-01 已修：`IMMUTABLE_REWINDOW` 接回 `decide()`，四条死线一次全开。** 它曾是 13 个声明动作里唯一 `decide()` 发不出的（成本表/执行器/子窗调度/台账全齐，只差发出点）。① 子窗划分 `vanishing_rescue_ranges()` 搬到 `abfe_preoptimizer` 模块级，`abfe_pipeline._build_vanishing_rescue_ranges` 转成一行转发（控制器 import 不动 pipeline：循环依赖 + openmm），成本表里那份 `2 if (b-a)>2 else 1` 的复算删掉 ⟹ **切法全仓一份**；② 新增 `rewindow_feasible()` / `rewindow_children()`：可行 = K≥3 **且** 该父窗不在 `parents_done`（**一个父窗只切一层**，不设深度上限就是新的无限循环）；③ 发出点接在 **四条死线各自的兜底之前**（1e 预热耗尽 / 5a-2 held-out REJECT / 边际停滞 / 5b 偏斜类），**不是链尾加第 33 条分支**。它绕开挡住拆末窗/插 λ 的全部四个条件（tail anchor、dry-run 切不细、插完越过 `hi`、插点预算）⟹ **window 0（本方法的必然坏窗口）第一次有了够得着的缩跨度动作**。与同日 `state_count` 分窗正向耦合：布局全变 `[5,5,5,5,5]` ⟹ 每个窗都可行。顺带删掉静态审计报的死赋值 `_window_states`。⚠️ **别期待它改善 ΔG** —— 死线开了只是循环能走完；触发它的判定本身噪声 34×，会误触发在没问题的窗口上，「一层」的限制不许放宽。验证：3 条 strict-xfail 删除后全绿、`tools/audit/static_controller_2026-09-17.py` 死动作 1→0、全量 **2653 passed / 0 failed**。 | 否 |
| 09-17 | 🔴 **登记两条 P1 源码缺口，都是修陈旧测试时挖出来的，都**只差一个接入点**。① `COMBINE-01`：`ibs_engine.combine_ibs_and_independent_endpoint` 不产出**顶层** `analysis_status`（2026-09-15 改造时被注释显式排除在范围外），而 `runabfe.py:5096` 对 vdw 腿**强制**走这个拼接 ⟹ **`--analyze-only` 恢复 vanishing 腿今天必然抛错**；生产侧同一个洞在 `abfe_pipeline.py:6618`（默认关，低一档）。② `REWIND-01`：`IMMUTABLE_REWINDOW` 是 13 个声明动作里**唯一一个 `decide()` 发不出的**（AST 实测）—— 成本表（审计 #44）、执行器（`abfe_pipeline.py:12162` → `_immutable_rewindow_step`）、子窗调度台账、12 条测试全齐，唯独判断侧没有 `plan(...)` 发出点 ⟹ λ 表顶满 + 中间窗失败时本该退到有界重窗，现在直接 `NO_FEASIBLE_ACTION` 停机（形状同 `BM-04`/`DATA-02`）。两条都**未修**（修法都要先判「合并/兜底时用哪个量表达完整性」，需维护者拍板），各自的 strict-xfail 修好即变红。详见 [TODO.md](TODO.md) | 否 |
| 09-17 | 🟢 **测试套件从 74 failed 归零**：`pytest -m cpu_only` **2480 passed / 0 failed / 9 xfailed**（此前 74 failed / 2332 passed）。**没有一行生产代码被改**。那 74 条全部源自 2026-09-15 的 `converged` **删键**（不是改义）而测试没跟：fixture 仍写 `converged` ⟹ `_read_stage_result()` 的嗅探键已换成 `analysis_status`/`total_delta_G` ⟹ 整份 stage 结果读不到 ⟹ 归因判反（`rewindow` 那簇 10 条就是这一个 fixture 键）。同时修正三类**语义**陈旧：① 四道拟合阈值门 09-01/09-15 已降级为只报告，测试还在 `pytest.raises`（改成钉替代品：不 raise + `results_untrusted=True` + 证据进 `stage_quality_failures`）；② `DONE` 现在要 `analysis_status=COMPLETE` **且** `precision_status=MEETS_CROSS_REPEAT_TARGET`，单跑的终态是 `NO_ACTION`+`ANALYSIS_COMPLETE_PRECISION_UNMEASURED`；③ 支撑失败必须落在**窗口级自检**而不是阶段门。⚠️ 顺带修掉一个**假绿**（`test_failed_gate_still_blocks` 之前绿的理由是错的）。 | 否 |
| 09-17 | 📄 **补两道"守卫本身失明"的守卫**。① `decide()` 已是 23 行外壳（真身 `_decide_once`），全仓 **13 处**源码探针全指着它：断言"存在"的当场红，**断言"不存在"的静默假绿**；已全部改指 `_decide_once`（或两段拼接）。② 仓库有「死出口」守卫却**没有「死动作」守卫** —— 正是 `REWIND-01` 烂掉没人知道的原因，已补 `test_no_dead_actions_left_in_the_declaration`。③ `test_no_exit_contradicts_its_action` 把合法终态**写死成两个词**（第二份终态清单），改成直接对 `TERMINAL_EXITS`。④ 新登记 `TEST-01`：29 个测试文件无任何标记 ⟹ `-m cpu_only` 整份跳过（实测 168 个测试），漏跑的恰好是最容易静默烂掉的源码探针。 | 否 |
| 09-16 | 🔴 **`LR-06` 方案 A 落地：插件的 `0.1 Å` fail-closed 硬门改成训练支撑域下界 `1.5 Å` 处的夹取**（`EXP025_R_FLOOR_ANGSTROM`）。`bias_scale` 乘在 Group-1 **整个表达式**外面（内含配体↔环境软核），所以 `bias_scale=0` 不是"关掉偏置"而是**把配体关成鬼影**，`r→0` 必然；`CustomCVForce` 又会求值**每一个** CV，于是插件在鬼影几何上撞门。堵求值点行不通（一天找到三个）⟹ 方案 A **消除失效模式**。**改动**：CUDA 侧 `CudaLocalManyBodyResidualKernels.cpp` 能量侧 2 处夹取（`r_eff=max(r,floor)`，边照样计入）+ 力侧 2 处跳过（下限内 dE/dr≡0，`1/r` 因此不求值）；**Reference 侧 `g1_math_core.h` 同步**（原计划漏了这一步 —— Reference 平台经头文件用它，只改 CUDA 就是「同一个不变量两份实现」，G1/G2 钉的正是两者一致），复用同一常量不另抄数字。**下限取 1.5 而非 0.1 的实证**：径向基在小 r 处**并不衰减**（16 个中心均匀铺在 `[0,5] Å`、宽 0.333，0.1 Å 处基函数值仍 0.96），且支撑域以下那 5 个中心上的 pair weight 量级与训练充分的完全一样（max|w| 0.30–0.37 vs 0.32–0.39）——完全活跃却从未被数据约束。**真机 A/B**（RTX 2080 Ti，mixed，前后两份独立编译 `.so`、两个独立进程）：全局最小 lig–env 距离 **≥1.5 Å 时新旧能量逐位相同**，<1.5 Å 才变；**0.05/0.093 Å 由 fail-closed 变为有限值（0.093 正是真机崩点）**。⚠️ 判据是**全局**最小距离，不是被移动原子到锚点的距离。⚠️ **G1/G2/G3 三个一致性 harness 未跑**（缺 `yyjson`，套件按设计跳过）——改动最需要它们，已记为验证缺口。两处 `source_sha256` 已同步（`5c74365dffaa…`）。错误码 `EXP025_DEVICE_ERROR_MIN_DISTANCE=2` **保留**（`static_assert` 钉 ABI），只改触发策略。判据：`tests/test_lr06_residual_distance_clamp_2026_09_16.py`（5 passed）。⚠️ `r_floor` 目前是**编译期常量、绑定 shipped R1**，换支撑域不同的重训模型会静默沿用，升级路径写在 `r1_model_layout.h` 的 `ponytail:` 注释里。 | ✅ **是**（`B_φ` 在 `r<r_floor` 处定义变了 ⟹ `sampling_score_sha256` 变，与已冻结旧分数不可比；对 ΔG 正确性无影响，MBAR 对任意冻结偏置无偏，但 A/B 两臂必须都用新插件） |
| 09-16 | 🧪 **`Particle coordinate is NaN` 11 次的根因分清成两件事，新增 `tests/test_warmup_guard_asymmetry_2026_09_16.py`（4 passed，已做还原变异）。主线代码零改动。** ① **10 次是 2026-09-09 单批事故**（3 个体系 10 个 run，全部 09-09 14:28–16:49 启动），死在偏置爬坡第一个非零档 `scale=0.2`，当时那里是裸 `sim.step()`；守卫 `step_with_chunk_rollback` 于 **09-12 15:50 (`1769e71`)** 落地 ⟹ **已修**，旧 traceback 只是留在追加式 `launch.log` 里。⚠️ `ibs_engine.py` 那段注释的 `🔑 [2026-09-09]` 是**诊断日期不是落地日期**，照它推断会误判「09-11 之后还炸=没修好」。② **剩 1 次**（`cyclod_ligand2/rep3` 09-14，守卫之后）是**守卫不对称**：热化/爬坡用 `step_with_chunk_rollback`（回滚+减半+重做），紧随其后的偏置学习循环只有 `guarded_step`（docstring：**只改错误信息、不改动力学**）⟹ 学习阶段一炸整窗作废。**是已知设计不是遗漏**（`ibs_engine.py` 原注释：「warmup/learning 控制面没有 guard、也没有周期性 checkpoint」）。⚠️ **不是 `LR-06` 的鬼影期**（本轮先这么猜过，已证伪：学习循环跑在 `bias_scale=1.0`）。📌 要给它接回滚**必须连 `sampler.energy_buffer`/`energy_history`/`sampling_state_energy_history` 一起回滚**，否则偏置学习读到与构型对不上的样本 —— 不许当成「顺手补个守卫」。结论并入 [TODO_P3.md](TODO_P3.md) 的 `BM-A`。 | 否 |
| 09-16 | 🔴 **`stage2_first_window_max_states: 4` 进 `runabfe.py::PRESET_CONFIGS` 三个预设**（维护者拍板 / `BM-04`）。此前这个键**从未在任何一次真实运行里生效过** —— 3/3 预设、14/14 benchmark config、CLI 全都没有它，而 `abfe_config.json` 里那个 `4` **不会被自动加载**（`--config` 的 argparse 默认 `None`，`runabfe.py:3302` 的合并基底是 `PRESET_CONFIGS` 而非该文件）。缺席时 `first_cap is None` ⟹ 键不发出 ⟹ `first_window_max_states=None` ⟹ 无 cap，win0 可占 8 态；而 win0 一旦不可信，`tail_repartition_anchor` 恒 `None`（它之前没有可冻结的窗口）⟹ 缩跨度一族在**构造上**全不可行 ⟹ 真机 4 个 run 死在这里。⚠️ 本键在 `_PREOPT_DERIVED_PATH_KEYS`（**第 2 层**派生路径）：**全部现存 run 的 Stage-2 窗口缓存失配重采**；第 1 层（pilot 采样语义）不变 ⟹ **Stage 0/1 与 pilot/preopt 全部保留**，布局离线重算。📌 顺带定性：**`abfe_config.json` 不是"仓库默认配置"，只是一份样例** —— 判某键在某次运行里的值只能看该 run 的 `run_provenance.json`。 | ✅ **是**（Stage-2 窗口全量；Stage 0/1 + pilot 不破） |
| 09-16 | 📄 **维护者拍板关闭 `BM-04` / `DATA-02` / `DATA-03`**，覆盖 benchmark 7 个走不动的 run，全文进 [archive/TODO_closed.md](archive/TODO_closed.md)。**总体策略：4 个旧/非法 Stage-2 干净重建，3 个预算不足 run 用 `21/4` 有界续跑。** ① `BM-04` **不开放 `anchor=window 0`** —— win0 无前置共享态，把它塞进 tail-repartition 等于新增「整条路径重分」而非放宽判断（`tail_repartition_anchor` docstring：冻结的是 anchor **之前**的窗口）；② `DATA-02` 选**清理重建**，不回退 v3（`lambda_path_versions._publish()` 明文「只前进不后退」、回退直接 raise），也不许只删窗口文件保留 v4 指针（`load_current` 读指针，仍会读到非法布局）；③ `DATA-03` `max_path_insertions` **3→4**、`stage2_final_n_states` **保持 21** 原地 resume —— 该键已被 `ibs_engine.py:503` 从窗口采样身份里摘掉，λ 未变的窗口可复用；**不许再加到 5**，第 4 次仍不足就改走 23 干净重跑。成本补记：改该键会动 stage protocol key ⟹ **Stage 1 结果重新求解 ~28 min/指纹 ×3**（只重解、不重采样）。 | 否（决定本身；预设改动见上一条） |
| 09-16 | 🔑 **确立决策原则：「算对之前，沉没成本一律不计。」** 本轮代理**两次**把已有缓存的价值算高（一次建议「别为 2 个 run 作废 39 个 run 的缓存」、一次把预设改动半径估成「39 个 run 从头跑」），两次都被维护者纠正。实测那批缓存里**只有 2 个 run 出自修复后的代码**（`p38_ligand1/rep2,rep3`），其余 7 个完成的全是旧码、本来就不可引用，30 个未完成的无论如何要重做。**一个还没被证明算得对的结果，它的缓存没有保护价值。** ⟹ 今后同类权衡**先问「这条路径算得对吗」，再问「重算要多少 GPU」，顺序不能反**。 | 否 |
| 09-16 | 📄 **文档对账：`docs/TODO.md` 的 `- [ ]` 系统性落后于源码，13 条已修的缺陷还挂着未打勾**（`DECORR-01` / `AUDIT-S2-01` / `BUD-01`~`05` / `BUD-07` / `CTL-11`~`14`），连带 [CONTROLLER_BUDGET_AUDIT_2026-09-14.md](archive/CONTROLLER_BUDGET_AUDIT_2026-09-14.md) 状态列 20 条虚挂的 `OPEN`（实测 65 条**全部**落地，缺标记的 #37 已回源码确认缺陷不在）。逐条证据与对账用的 `grep` 进 [archive/TODO_closed.md](archive/TODO_closed.md)。**危害不是漏修，是重复排查** —— 09-14 第三轮复核开出的 `BUD-*` 里就有一部分是在已修的地方重新查出来的。规矩：**「某条缺陷修没修」的权威只有源码，`- [ ]` 与状态列都是会过期的缓存。** 代码零改动。 | 否 |
| 09-16 | 📄 **修掉 `docs/TODO.md` 里 4 条与源码不符的陈述**（不是笔误，是会误导决策的）：① 「不许回退的约定」写「**只有三个真终态**」而 `TERMINAL_EXITS` 实测 **9 个**（多出 `DONE_UNTRUSTED` / `HALT_INVALID_INPUT` / `HALT_EVIDENCE_CONTRADICTS_DONE` / `HALT_LAMBDA_BUDGET_INSUFFICIENT` / `HALT_FRAMES_ADMISSION_CAP` / `ANALYSIS_COMPLETE_PRECISION_UNMEASURED`）—— 这条最危险，它属于**用来否决别人改动**的约定，照旧清单去审会把 6 个合法终态当违规打回；改成指向 `TERMINAL_EXITS`，**不在文档里维护第二份清单**。② `AUDIT-S2-03`「控制器真机零验证」不成立（39 rep 上过 GPU），改成「**有真机但没有一次以 `DONE` 收口**」。③ `REL-03`「零 GPU 复验」不成立，其子项 ③（两条腿 `pipeline.log` 分离）**已关闭**、①② 仍欠。④ `REL-04`「无一上过 GPU」改成「**跑过但没有一处被单独复验**」。另重测 `REL-05` 的中文行数（4 天涨 19k 行，旧数字已过期）。 | 否 |
| 09-16 | 📄 **新增 `BM-B`：全量 benchmark（13 体系 ×3 = 39 rep）30 个未完成 run 的归属** —— **最后启动在修复（05:03）之后的是 0 个**，没有一条能证明缺陷今天还在。与 `abfe-benchmark-31` 会话两轮交叉核实，定下三条取证口径（都是栽过之后才有的）：① 判代码版本只能用**最后一次启动横幅**的时刻，**不许用 `launch.log` 的 mtime**（实测差到 13h47m，本条第一版就这么判错过）；② `launch.log` 跨重启追加（单份最多 **25** 次启动），「最后一条异常」可能来自早就修好的那次；③ 判跑完没有时，`final_binding_results.json` 与 `pipeline_state.json` 对**被人工归档过**的 run **两个都会漏**（前者漏在文件被搬走、后者漏在 state 被回退；归档目录 `<体系>/_backup_*` 与 rep 目录**同级**）。**撤回**一条据作废签名写下的「5 个 TypeError ⟹ 重跑即可」（撤的是取证，`BM-05`② 那条修复独立成立）。正面证据：`p38_ligand1/rep2`(06:29:45)、`rep3`(07:34:59) 用修复后代码跑完，同体系 `rep1`(04:09 旧码) 卡住。 | 否 |
| 09-16 | 📄 **`REL-09`（输出目录与长任务保护）拿到第一份真机证据**：全量 benchmark 历史异常里 `输出目录已被另一次运行独占（目录锁）` 出现 **4 次，全部出自 `cyclod_ligand1`**。此前该条只是"理论上欠"。 | 否 |

---

## 协议版本现值

以下由 [`tests/test_doc_staleness_contract.py`](../tests/test_doc_staleness_contract.py) 对着源码常量钉住。
**这张表只在 [STATUS.md](STATUS.md) 和这里各存一份，别再往第三处复制。**

| 协议 | 常量 | 值 | 最近一次跳变 |
|---|---|---|---|
| IBS 偏置 | `ibs_engine.IBS_BIAS_PROTOCOL_VERSION` | **33** | 2026-09-05 |
| 热力学路径 | `abfe_preoptimizer.THERMODYNAMIC_PATH_PROTOCOL_VERSION` | **22** | 2026-09-03 |
| ESS 门 | `ibs_engine.ESS_GATE_PROTOCOL_VERSION` | **5** | 2026-09-01 |
| WCA 记账 | `ibs_engine.WCA_ACCOUNTING_VERSION` | **3** | 2026-09-02 |
| LJ 长程修正 | `ibs_engine.TRADITIONAL_LJ_LRC_PROTOCOL_VERSION` | **3** | — |
| 配体 COM 约束 | `ibs_engine.LIGAND_COM_RESTRAINT_PROTOCOL_VERSION` | **2** | 2026-08-29 |

「破缓存」= 兼容集合收窄成新版本自己，既有 `dual_window_*` / `ibs_state_*` /
`convergence.json` 全部失配、必须重跑，**不要试图复用**。
它**不作废**已经登记在 [STATUS.md](STATUS.md) 的数字——那些是已完成运行的记录，
失配影响的只是 resume 与缓存复用。

## 往这里加条目的规则

1. 一行一条，写"改了什么"，不写"为什么这么改"——理由写在源码注释或专题文档里，这里只指路；
2. 协议版本跳变必须标破不破缓存；
3. **计划、实现、测试通过、科学验证是四种状态**，条目里别混成"成功"；
4. 数字要带单位和符号约定，或者不写数字；
5. 不原地重写旧条目来"修正历史"——写新条目、指明它取代了哪条。
