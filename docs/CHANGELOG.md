# 变更记录（缩略）

[项目入口](../README.md) · [文档导航](README.md) · [当前科学状态](STATUS.md) · [历史材料索引](HISTORY_LOG.md)

> **本文是全程缩略时间线：一行一条，只记"改了什么、哪天、破不破缓存"。**
>
> 详细版本说明**不在这里**，在三个地方，本文只指路：
>
> | 要查 | 去哪 |
> |---|---|
> | 某个协议版本号为什么跳（逐版全文） | 源码常量上方的注释块（`ibs_engine.py` / `abfe_preoptimizer.py`） |
> | 某份开发期材料写过什么 | [HISTORY_LOG.md](HISTORY_LOG.md)（148 份逐份登记） |
> | 某个数字能不能引用 | [STATUS.md](STATUS.md)（唯一的科学结论声明处） |
>
> **⚠️ 本文不是科学结论。** 条目写的是"代码/协议变了"，不是"结果对了"。

## 工作区与体系

本项目在三个体系、四个工作区上推进过。看日期先看体系：

| 体系 / 工作区 | 时间 | 现状 |
|---|---|---|
| **Atenolol-rank11**（β 阻滞剂，第一个体系） | 2026-06 ~ 08-31 | 结果全部 `INVALIDATED`；8.1G 原始材料保留，不再是真源 |
| **4W53**（T4 lysozyme L99A + toluene，**当前主线**） | 2026-08-27 ~ | 当前唯一有效体系 |
| **ABFE_IBS**（本仓库，工程区分支） | 2026-08-31 ~ | 只留生产代码 + 回归测试 + 使用文档 |
| **ABFE_IBS_CUDA**（EXP-031 沙箱） | 2026-09 | GPU 优化实验，正文与证据不在主线 |

---

## 2026-09

> **2026-09-14 一天 15 条**，全部围绕 Stage-2 自治控制器。按这个顺序读最省事：
>
> | 批次 | 起因 | 条目关键词 |
> |---|---|---|
> | ① 真机日志驱动 | 每次 resume 炸一个新地方 | 路由信号炸管线 / 拼接失败炸管线 / 分析用错 f_k 字段 / 四起 no-op 死循环 |
> | ② 复核第一轮 | 同僚静态复核（四项裁决） | 子窗成为采样单元 / 生产预算独立成账 / S2-A 逐段门 / DECORR 同输入对照 |
> | ③ 复核第二轮 | 同僚静态复核（`CTL-01`~`CTL-10`） | 读旧 stage 结果 / 过期预算账 / 调度状态≠失败归因 / 身份隔离 / 预算准入 |
> | ④ 六路并行全面审计 | [CONTROLLER_BUDGET_AUDIT_2026-09-14.md](CONTROLLER_BUDGET_AUDIT_2026-09-14.md)（**63 + 2 条**） | 三条贯穿性根因 / 逐条状态列 / 两条「别再犯」的规矩 |
>
> ⚠️ **全部只有离线验证**（每条都做过还原变异确认会红），**真机零验证** ——
> 三个 benchmark run 的 stage-2 产物已于当天清空重跑，那次 resume 是第一次上机。
> 当天**没有动任何协议版本号**，也不破缓存。

| 日期 | 变更 | 破缓存 |
|---|---|---|
| 09-17 | 🔴🔴 **Stage-2 控制器一整波：真机跑出 11 条缺口，10 条已修。全文 → [STAGE2_CONTROLLER_WAVE_2026-09-17.md](STAGE2_CONTROLLER_WAVE_2026-09-17.md)，逐条 → `TODO.md` 的 `S2-G/H/J/K/L/M`、`TODO_P2.md` 的 `S2-A/B/E/F/I`。** **这一波的共同性质：全部是「判据/信号算出来了却没人消费」或「动作在构造上不可能成功」，不是阈值调参。** 六条 P1（会让整跑崩掉或白烧）：① `S2-L` 补帧的**块数硬上限被换布局重置** ⟹ 插一次 λ 配额就清零重发 4 块 ⟹ **补帧实际没有有效上限**（真机 `cmet_ligand1/rep1` w4 旧口径 2 块、真实 **9 块**；`brd4_ligand1/rep2` w3 累计 1,250,000 步 = 5 块）。与 `BUD-03` 是**同一个 bug 换了一个维度**（那次是「换段退钱」），而这本账的定义就写着「硬上限 = **资源账**」。修法：硬上限那本不按 `path_version` 过滤；**边际增益那本仍然要过滤**（跨布局比 `min N_eff/g` 没有意义）。② `S2-M` 「布局过期 + 冻结验证批次打满」死锁：1d-0 无条件发 `RUN_PRODUCTION` → 预热门弹回 → no-op 又记不下（stale 窗口指纹无可比身份）→ 连发 3 次 → 降级被 stale 挡 → 终止；改为分流到 `PROVISIONAL_PRODUCTION`（它产**新布局下**的帧，一并解决过期）。③ `S2-K` `ANALYZE` 跨段合并求解撞过期证据 `ValueError` **炸穿主循环**（跨 run 统计里最常见的末行）—— 判据侧 `classify_layout_evidence()` 明明知道哪个 (段,窗口) 过期，执行器侧无过滤加载；改为降级为「本轮拿不到跨段证据」并记进 history，不吞成静默。④ `S2-G` 退役判定只扫**有产物**的窗口 ⟹ 对「整窗未采、预算分文未动」的窗口恒答「别处没活干」⟹ 整跑终止。⑤ `S2-H+F` **开新段的动作既没有次数预算、也没有事后有效性判定** —— `RECALIBRATE_FK` 每次都改盘面 ⟹ 停滞保护（同一动作 + 盘面未变）与 no-op 台账（指纹含段维度）对它**结构性免疫**；真机连发 4 次、每次丢掉上一段累计步数、验收量 9.645 → 4.182 → 0.837，盘上留下 7 个采样段。修法不发明阈值：次数用与 `max_path_insertions` 同性质的终身预算，效果复用 `marginal_gain_stalled()`；**效果闸必须排在次数闸前**，否则默认配置下永不可达。⑥ `S2-J/I/E` 末窗永不拆 —— `_legalize_tail_window()` 的「补 1 到 K=7 ⟹ 拆 4+4」**早就完整实现**，但 `hi=8` 让死区 `(hi, 2·lo−1) = (8,7)` 成为**空集**、且 `K ≤ 8` 直接 return ⟹ 整段永不执行。P2：`S2-B` 缺窗覆盖被**纯补帧**挡住（补帧不重锚、作废不了下游，挡它没有物理依据）；`S2-A` solver 侧证据在整个采样阶段恒空 ⟹ 三条判据不带电 —— **本次只消除静默**（`NO_CRITERION_INPUT_AT_ALL` 与 `NOT_ENOUGH_POINTS` 分开 + `plan()` 恒出 `inoperative_criteria`），**行为一字未改**，根因归 `AUDIT-S2-02`。⚠️ **三处我做错又改正的，都留了痕**：(a) 早期 `ANALYZE` 连打红 25 → 11 条后回退，原位注释「别再试图靠挪 `ANALYZE` 的位置来补它」；(b) 一度要加 `SPLIT_LAST_WINDOW_IN_TWO` 动作，写完才发现 `_legalize_tail_window` 早就实现了 —— **同一不变量的第二份实现**，全部撤回留墓碑；(c) 曾判定改窗口上限不作废缓存（只查了第 1 层），已更正。⚠️ **没有动任何协议版本号。** 全量 **2731 passed / 0 failed**。 | 否（代码部分） |
| 09-17 | ⚙️ **`abfe_config.json`：`stage2_window_max_states` 8 → 5**（用户拍板）。**这不是调参，是让一条早就写好、却因为这个数而永不执行的修复路径重新可达**（见上一条 `S2-J/I/E`）：`hi=8 ⟹ split_lo = 2·lo−1 = 7 ⟹ 死区 (8,7) 空集`，K=6/7/8 全是「合法单窗」⟹ 末窗永远不拆；`hi=5 ⟹ 死区 = {6}` ⟹ 末窗 K=6 → 补 1 → K=7 → 唯一拆法 **4+4**。⚠️ **原来那个 8 的理由已经失效**：它是 09-14（审计 #49）为了让 **∫g 均衡**的分窗跑得动才从 5 提到 8 的，而 **∫g 已于 09-17 退役**（`stage2_window_partition` 现为 `state_count`，理由是它数学上重复计数、实测 ρ(∫g, rawESS)=+0.196 **符号是反的**）。新目标 min-max K 本来就要把 maxK 压小。⚠️ **不改初始布局**：离线验证（n_states=18、`state_count`、win0 cap=4）hi=8 与 hi=5 都给 `K=[4,5,5,4,4]`、maxK=5、5 个窗口 —— **逐字相同**；`hi` 只在**修复路径**上起作用。旁证（跨 run 完成率快照）：maxK=5 → 4/4、maxK=6 → 2/5、maxK≥7 → **0/22**；⚠️ 那张表有**反向因果**（插 λ 是修复动作 ⟹ 高 maxK 部分是「出过问题」的结果），不能单独当依据。 | **是（仅 stage 结果缓存）** —— 本键在 `_PREOPT_DERIVED_PATH_KEYS`（**第 2 层**派生路径），按设计可从旧 pilot **离线重算、不重烧 GPU**；它**不在**窗口采样身份里（2026-09-10 从 `_stage_protocol_key` 摘掉）；加上布局逐字相同 ⟹ 窗口 λ 不变 ⟹ 轨迹可复用。失配的只有 stage **结果**缓存，而重解是零采样。 |
| 09-17 | 🟢 **补帧准入补上第三道：剩余配额的乐观上界够不着门 ⟹ 停**（`DEAD_LINES §3.1+§3.2` 的共同根因）。`n_eff_over_g_reachable_by_frames` 的 docstring 自己写着它是**上界**、「`True` 只表示值得一试」，并且「真正的刹车是事后的 `marginal_gain_stalled()` —— 两者一前一后，**缺一不可**」。而那个刹车的判据是「末点 < 前面各点中位数 × 0.9」⟹ **单调上升但渐近在门以下的序列永不触发**（真机 cyclod_ligand1_outer/rep1 win0 逐块 4.459/4.553/5.287，门 10，按 +0.41/块要 11 块而配额只有 4）⟹ **这一对少了一半**：进的时候乐观、出的时候没人管。补法**不引入任何新阈值、也不拟合斜率**（那个量单次噪声 34×，3 个点拟斜率同样在量噪声）——就是**同一个前向判据**，拿**现在的读数**和**剩下的配额**再问一次：`当前 ratio × 剩余 headroom < 门 ⟹ 连上界都够不着 ⟹ 停`；`frames_growth_headroom` 本就是 `(1+cap)/(1+used)`，「剩余」是它的原义。真机数代入：used=1 ⟹ 4.459×2.5=11.1 ≥ 10 放行（此刻确实还可能够得着）；used=2 ⟹ 4.553×1.67=7.6 < 10 ⟹ 停。🔑 **挂在 `_frames_admission`（全部 16 个 `RUN_PRODUCTION` 出口的唯一咽喉），不挂在归因上** ——只有「自检归因」那两条入口会问射程，缺窗/跳窗/旧布局/步数未达标一概不问（FLOW 的 D1）。⚠️ 只在明确判 `False` 时拦，`None`（缺数/门缺失/非有限）一律放行 —— 未知不是「够不着」。⚠️ 与 `support_failure_is_skew` **共用同一个函数** ⟹ 射程一旦够不着，归因同时从「样本量类」翻成「偏斜类」，分支侧会先发缩跨度（`INSERT_LAMBDA`/有界重窗）；准入层是给其余十几个不查归因的入口兜底。三条新测试：射程内必须放行（防「一律不补帧」作弊）、射程外转对症动作、跳窗入口也被咽喉拦住。 | 否 |
| 09-17 | 📌 **登记一条**判定**：`openmm` 两份环境文件都**故意停在 8.5.2，不升 8.6**（维护者判定：8.6 尚不成熟、有已知严重 bug，本项目**暂**不使用 —— 是"暂"不是"永不"，8.6 的官方 REMD 正是本项目想要的，**未来版本可能支持**）。与代码里 `free_energy_engine.OFFICIAL_REMD_MIN_OPENMM_VERSION = "8.6.0"` **不矛盾** —— 那是「官方 `ReplicaExchangeSampler` 要 8.6 才有」的**资格判据**（后端选得上选不上），环境说的是「今天不装」。**代码支持得了 ≠ 现在就该用。** ⚠️ 本条先前一度被我改成下限 `openmm>=8.5.2`（并把 CI 对账改成按运算符分流），**已全部还原**；理由写进了 `environment-ci.yml` 与 `GETTING_STARTED.md`，防止下次再被当成"环境文件禁止了代码支持的版本"重开一遍。要放开必须先复验 8.6 那些 bug，并连带重编 `plugins/LocalManyBodyResidual/*.so`（对着 8.5.2 编的，跨版本无 ABI 承诺）。 | 否 |
| 09-17 | ✅ **`REL-08` 关闭：基础环境本来就存在，欠的是口径。** `environment-ci.yml`（43 行手写）就是 CPU 基础环境，**不含** CUDA 工具链 / torch / MACE / JAX；`environment.yml`（333 行 export、全钉死）是 GPU 生产环境。从 export dump 里手术式删 CUDA 行删不干净，所以改的是文档定位：`GETTING_STARTED.md` 加「两个环境文件，装哪个」对照表 + 插件那节补上「基础环境的 openmm 是下限，装到 8.6 后预编 `.so` 不保证能载」。 | 否 |
| 09-17 | 🟢 **`LR-04` 关闭：新增 `--only-ligand-in-water` 调试入口**（跳过 Boresch 估算 / rebalance / 整条复合物腿，只跑溶剂腿）。**定位是调试不是生产：不产出 ΔG_bind、不覆盖主 `final_results.json`。** 三道 fail-closed：① 与 `--only-complex-charging` / `--only-boresch-attachment` 互斥；② 与 `--outer-lambda-local-residual-ibs` 互斥（残差运行时在复合物腿那段构造、再按溶剂腿拓扑重建，跳过复合物腿就拿不到 ⟹ 抛错而不是静默降级成 baseline 溶剂腿）；③ 溶剂腿跑完**直接 `return`**，不走到第 8 节 —— 比"补个 `None` 往下传"安全，后者每加一个下游分支就多一次把半份结果写成完整结果的机会。`tests/test_only_ligand_in_water_lr04.py` 5 passed（控制流契约）。⚠️ **真跑要 GPU，没跑过**，钉住的只是不跑也能错的那部分。 | 否（新入口，默认关；不改任何既有路径） |
| 09-17 | 🟢 **用户拍板的三件全部落地。** ① **删死分支**：`decide()` 里「`allow_untrusted` + 分析不完整 ⟹ `DONE_UNTRUSTED`」从未发出过 `DONE`（O4 必然改写，且走到那里 `analysis_status` 必为 `ANALYSIS_INCOMPLETE`）。**不给 O4 开豁免**的理由不止「O4 对」：`ANALYSIS_INCOMPLETE` 是缺窗/跳窗/求解失败这类**硬不变量**没满足，即使放开 O4，下游`analysis_status != ANALYSIS_COMPLETE ⟹ RuntimeError` 照样拦死 ⟹ 只会「先报完成再被打回」。`allow_untrusted` 本身**没被削弱**（仍改 `trust_level`、分析完整时仍给 `DONE_UNTRUSTED`、仍管跳过 rescue）。回归测试出口 `HALT_EVIDENCE_CONTRADICTS_DONE` → `NO_FEASIBLE_ACTION`，`evidence_status != CONVERGED` 一字未动。② **`_legalize_tail_window` 不再借 `first_untrusted`**：合法化用**自己的**切点=末窗首态（天然是与前一窗的共享节点）。原来「合法化」借用了「修复」的定位谓词 ⟹ `first_untrusted ∈ {0, None}` 时 anchor 恒 None、抛 RuntimeError，而非法布局**已落盘**（真机 cyclod_ligand3/rep1+rep3）。⚠️ 那**不是死区**：死区 `hi<K<2lo−1` 在 lo=4/hi=8 下是空集，K=9 落在可拆区间 [7,15]、9→5+5 完全合法。新增三道 fail-closed（单窗布局拒绝、共享节点自检、前缀逐字不变）+ 「所有新窗口 ∈[lo,hi]」断言；**不放宽** `tail_repartition_anchor()`（那会扩大 `SPLIT_TAIL_WINDOW` 这个**修复**动作的语义）。顺带补上第二个调用点缺失的 `partition_criterion`（原来吃默认 `arclength`，与另一处不一致）。③ **端点不确定度改门**：`max_endpoint_uncertainty_kJ_mol` 不再单独设 `results_untrusted` —— 它的文本自称「仅诊断，不拒绝」而 `results_untrusted` 会一路传到最终结果并打印「不得作为可发布结果引用」，所以「仅诊断」是假的。读数/阈值/消息照留，新增 `drives_action:false` + `affects_trust:false`；**不碰 `analysis_status`**，发布验收仍由硬不变量 + `precision_status` 把关（去掉 untrusted ≠ 可发布，单 run 精度恒 UNMEASURED）。⚠️ 顺带把另外三道**仍然**拒绝发布的门（min_overlap / min_absolute_ess / min_decorrelated_samples）的假措辞「**仅诊断**，不拒绝」改成真话并标 `affects_trust:true` —— **行为一字未改**，只是不留已证伪的陈述。⚠️ 旧 JSON 里已落盘的 `results_untrusted` 不会被本改动洗掉，也**不该**全局清除（那几份多半还有别的真实失败），重新求解该 stage 时自然重算。全量 **2658 passed / 0 failed**。 | 否 |
| 09-17 | 🧹 **删除 `abfe_pipeline.RunDirectoryLock`（输出目录独占锁）与它的 5 条测试，进 `archive/run_directory_lock_removed_2026-09-17.py`。** 生产侧零调用点 —— 它曾挂在 `runabfe.py::main` → `guard_run_directory()`，那句早已删掉（理由见 `guard_run_directory` docstring：SIGKILL 后锁文件残留、共享盘上 stale 判不出来 ⟹ 锁挡住的是 `--resume` 本身）。⚠️ 归档头部记了一条**复核确认的机制缺陷**：`timeout_s=0.0` ⟹ 父类 `_PipelineStateLock.__enter__` 的 `deadline` 就是当下 ⟹ 第一次 `FileExistsError` 后必抛 `TimeoutError`，**`os.open` 永不重试**；于是 `_break_stale_lock_if_needed()` 即便清掉了残留锁，**这一跑照样死**，清理成果只留给下一次启动（与实测「拦一次、几十秒后重启就过」吻合）。坑不在「不等待」（docstring 明写的设计），在于**「不等待」与「不重试」被写成同一件事**，stale 清理路径因此对它实际失效 —— 将来重新接线要先加 break 后的重试。📌 父类 `_PipelineStateLock`（`timeout_s=10.0`）**不受影响**，`pipeline_state.json` 照常用。同文件的 SIGTERM 与磁盘预检两节测试**保留**（10 passed）。 | 否（无调用点） |
| 09-17 | 🧹 **删除 EXP-010/011 的死 CLI 表面：9 个子命令 + 13 个转发壳，移入 `archive/outer_lambda_exp010_exp011_removed_2026-09-17.py`（`TODO_P2` 的 `LR-03`）。** 这些壳全部转发给 `archive.outer_lambda_exp010_exp011_legacy`，而**那个模块从来没进过本仓库** —— `archive/` 目录不存在、`git log --all -- archive/` 为空；壳是 2026-08-31 `169514e`（主线迁移）引入的，1718 行真实现留在旧工地 `Atenolol-rank11/archive/`。⟹ **这 9 个子命令从迁移那天起就是 `ImportError`，一次都没跑起来过**，`LR-03` 原文「要么补实现、要么删入口」里的「补实现」在本仓库不成立（等于把旧库搬回来重新纳管），而「legacy 读 `manifest['lambda_shield']` → `TypeError`」那半条**永远够不着**，`ImportError` 先炸。删掉：`exp011-coverage` / `exp011-fit-pmf` / `exp011-umbrella-sample` / `exp011-reweight-umbrella` / `exp010-label` / `exp010-fit` / `exp010-prepare-selection` / `freeze-slow-variable` / `sample-hard-window-scratch`，13 个壳，`__all__` 里 12 个名字，以及 `tests/test_outer_lambda_cli.py` 的 `--help` 清单对应行。⚠️ **同期的 `screen-slow-variables` / `compare-slow-variable-screens` / `wp0-select` 没删** —— 它们不碰这些壳，仍能跑。`outer_lambda_neural_basis.py` 9472 → 8109 行；`--help` 从 27 个子命令降到 18 个。13 个壳在主线与测试里的外部引用数是 **0**（删前逐个数过）。 | 否（纯死代码，运行时无任何调用点） |
| 09-17 | ✅ **`TEST-01` 关闭：198 个测试文件全部落进 `cpu_only` / `needs_gpu` 之一。** 22 个无标记文件加 `pytestmark = pytest.mark.cpu_only`（8 个顺带补 `import pytest`），外加 `test_exp019_softlift_loro.py` 里**漏网的单个测试** —— 该文件已有 `@pytest.mark.needs_gpu` 挂在另一个测试上，按文件 grep 看是「有标记的」，实际第一个测试两档都不属于。📌 **所以判据只能用收集数对账，不能用 grep 文件。** 实测（`CUDA_VISIBLE_DEVICES=''`）：`-m 'not cpu_only and not needs_gpu'` → **no tests collected (2666 deselected)**；22 个新标文件在**无 GPU 可见**下 **235 passed**（不是「本机跑过了」，卡被藏掉了）。判 `cpu_only` 的依据是它们只读源码 / AST / monkeypatch —— 两个名字带 CUDA 的（`test_lr06_…` 读 `.cpp` 文本、`test_nvcc_discovery_…` 全程 monkeypatch 环境变量）都不建 Context。 | 否 |
| 09-17 | 📋 **`REL-09` 关闭（**无代码改动**，逐项回源码核实后发现三项里两项早已落地）**：① SIGTERM/SIGINT ✅ `install_termination_handlers`（09-17 真机触发过：`cyclod_ligand1_outer/rep1` 02:29 的有序退出）；② 磁盘预检 ✅ `ensure_free_disk_for_stage`（stage 前 `free ≥ 2×预估`，fail-closed，ATT-23），原文「只有 doctor 里有 `disk_usage`」是它落地**之前**的状态；③ 输出目录独占锁 ⚖️ **已判定不要**（`guard_run_directory` docstring：SIGKILL 后锁文件残留、共享盘上 stale 判不出来 ⟹ 锁挡住的是 `--resume` 本身）。⚠️ **原条目引的「目录锁真机咬人 4 次」证据方向是反的** —— 那 4 次全在 `runs/cyclod_ligand1/rep1/launch.log` 的 **09-10** 段，traceback 指向后来被删掉的那把锁，咬的就是续跑。**别拿它重开这条。** | 否 |
| 09-17 | 📌 **`LR-06` 第 6 步真机通过（补记，当天没人写进 TODO）。** 同一份 `runs/cyclod_ligand1_outer/rep1/launch.log` 两次启动直接对照（判版本用**启动横幅**不用 mtime）：09-16 02:46（夹取前）行 303–357 窗口 0 热化 `fail-closed … pair distance below 0.1 A`、步长降到 0.008 fs 仍炸；**09-17 01:22（夹取后）行 358–1251 `fail-closed` 0 次**，窗口 0–4 全部跑出能量，走到 `[自治 15/40]`，02:29 终止于**人为 SIGTERM**。⟹「outer 臂在窗口 0 必死」这个失效模式已消除。第 5、7 步按维护者判定**押后到控制器改完**。同批补记：`EXP-033-P1-GPU` 真机首跑也过了，但拟合是空模型（B 离散度 2.9e-06 kT），只证明不炸。 | 否 |
| 09-17 | 🟢 **死线 D1 / D8 的「报告侧」改进（`archive/STAGE2_DEAD_LINES_2026-09-17.md` 点名的两条，都不改路由）。** ① **D1**：终态窗口的 `last_failure_reason` / `last_gate_error` 原来只出现在 D1 那条分支的 `reason` 字符串里 —— 换一条出口（退役换窗，或 TERMINAL 窗口根本不是 `earliest`）就整个看不见，而这是人工接手唯一需要的东西。现在恒常输出到 plan 顶层 `terminal_window_failures`，与 `earliest` 是谁无关。⚠️ 两个键**不在同一个文件**（前者在 ibs_state、后者在 convergence 的 `bias_warmup`），正是它们该被提到 run 级的理由。② **D8**：兜底原来把「确实没有对症动作」与「归因读到坏数」说成同一句话，而两者处置相反。新增 `_attribution_blind_spots()`：把归因读数分成 **corrupt（NaN/inf）** 与 **absent（None）**——NaN 的毒性是**每一个比较都是 False**（`nan>=x` 与 `nan<x` 同时为假）⟹ 上游静默一条都不命中，长得和「真的没动作」一模一样；absent 则是正常（很多窗口本来就还没有 `cum_fk_verdict`）。只改措辞与 `missing_evidence`，**不给 D8 配任何动作**（配了就是「再跑一次直到碰巧通过」）。⚠️ 刻意**不做**「全部读数都读不出来 ⟹ 全盲」那种判据：有产物的窗口 `phase` 恒可导出，全盲构造上不可达，写了就是死支。另：`segment_index_of_dir` 的 docstring 补了「别把 `rewindow_<sha12>` 简化成裸 sha12」——现在是**构造上**安全（字面量前缀让 `isdigit()` 恒 False），去掉前缀就降级成 0.34% 概率碰撞。全量 **2657 passed / 0 failed**。 | 否 |
| 09-17 | 🟢 **REWIND-01 已修：`IMMUTABLE_REWINDOW` 接回 `decide()`，四条死线一次全开。** 它曾是 13 个声明动作里唯一 `decide()` 发不出的（成本表/执行器/子窗调度/台账全齐，只差发出点）。① 子窗划分 `vanishing_rescue_ranges()` 搬到 `abfe_preoptimizer` 模块级，`abfe_pipeline._build_vanishing_rescue_ranges` 转成一行转发（控制器 import 不动 pipeline：循环依赖 + openmm），成本表里那份 `2 if (b-a)>2 else 1` 的复算删掉 ⟹ **切法全仓一份**；② 新增 `rewindow_feasible()` / `rewindow_children()`：可行 = K≥3 **且** 该父窗不在 `parents_done`（**一个父窗只切一层**，不设深度上限就是新的无限循环）；③ 发出点接在 **四条死线各自的兜底之前**（1e 预热耗尽 / 5a-2 held-out REJECT / 边际停滞 / 5b 偏斜类），**不是链尾加第 33 条分支**。它绕开挡住拆末窗/插 λ 的全部四个条件（tail anchor、dry-run 切不细、插完越过 `hi`、插点预算）⟹ **window 0（本方法的必然坏窗口）第一次有了够得着的缩跨度动作**。与同日 `state_count` 分窗正向耦合：布局全变 `[5,5,5,5,5]` ⟹ 每个窗都可行。顺带删掉静态审计报的死赋值 `_window_states`。⚠️ **别期待它改善 ΔG** —— 死线开了只是循环能走完；触发它的判定本身噪声 34×，会误触发在没问题的窗口上，「一层」的限制不许放宽。验证：3 条 strict-xfail 删除后全绿、`tools/audit/static_controller_2026-09-17.py` 死动作 1→0、全量 **2653 passed / 0 failed**。 | 否 |
| 09-17 | 🔴 **登记两条 P1 源码缺口，都是修陈旧测试时挖出来的，都**只差一个接入点**。① `COMBINE-01`：`ibs_engine.combine_ibs_and_independent_endpoint` 不产出**顶层** `analysis_status`（2026-09-15 改造时被注释显式排除在范围外），而 `runabfe.py:5096` 对 vdw 腿**强制**走这个拼接 ⟹ **`--analyze-only` 恢复 vanishing 腿今天必然抛错**；生产侧同一个洞在 `abfe_pipeline.py:6618`（默认关，低一档）。② `REWIND-01`：`IMMUTABLE_REWINDOW` 是 13 个声明动作里**唯一一个 `decide()` 发不出的**（AST 实测）—— 成本表（审计 #44）、执行器（`abfe_pipeline.py:12162` → `_immutable_rewindow_step`）、子窗调度台账、12 条测试全齐，唯独判断侧没有 `plan(...)` 发出点 ⟹ λ 表顶满 + 中间窗失败时本该退到有界重窗，现在直接 `NO_FEASIBLE_ACTION` 停机（形状同 `BM-04`/`DATA-02`）。两条都**未修**（修法都要先判「合并/兜底时用哪个量表达完整性」，需维护者拍板），各自的 strict-xfail 修好即变红。详见 [TODO.md](TODO.md) | 否 |
| 09-17 | 🟢 **测试套件从 74 failed 归零**：`pytest -m cpu_only` **2480 passed / 0 failed / 9 xfailed**（此前 74 failed / 2332 passed）。**没有一行生产代码被改**。那 74 条全部源自 2026-09-15 的 `converged` **删键**（不是改义）而测试没跟：fixture 仍写 `converged` ⟹ `_read_stage_result()` 的嗅探键已换成 `analysis_status`/`total_delta_G` ⟹ 整份 stage 结果读不到 ⟹ 归因判反（`rewindow` 那簇 10 条就是这一个 fixture 键）。同时修正三类**语义**陈旧：① 四道拟合阈值门 09-01/09-15 已降级为只报告，测试还在 `pytest.raises`（改成钉替代品：不 raise + `results_untrusted=True` + 证据进 `stage_quality_failures`）；② `DONE` 现在要 `analysis_status=COMPLETE` **且** `precision_status=MEETS_CROSS_REPEAT_TARGET`，单跑的终态是 `NO_ACTION`+`ANALYSIS_COMPLETE_PRECISION_UNMEASURED`；③ 支撑失败必须落在**窗口级自检**而不是阶段门。⚠️ 顺带修掉一个**假绿**（`test_failed_gate_still_blocks` 之前绿的理由是错的）。 | 否 |
| 09-17 | 📄 **补两道"守卫本身失明"的守卫**。① `decide()` 已是 23 行外壳（真身 `_decide_once`），全仓 **13 处**源码探针全指着它：断言"存在"的当场红，**断言"不存在"的静默假绿**；已全部改指 `_decide_once`（或两段拼接）。② 仓库有「死出口」守卫却**没有「死动作」守卫** —— 正是 `REWIND-01` 烂掉没人知道的原因，已补 `test_no_dead_actions_left_in_the_declaration`。③ `test_no_exit_contradicts_its_action` 把合法终态**写死成两个词**（第二份终态清单），改成直接对 `TERMINAL_EXITS`。④ 新登记 `TEST-01`：29 个测试文件无任何标记 ⟹ `-m cpu_only` 整份跳过（实测 168 个测试），漏跑的恰好是最容易静默烂掉的源码探针。 | 否 |
| 09-16 | 🔴 **`LR-06` 方案 A 落地：插件的 `0.1 Å` fail-closed 硬门改成训练支撑域下界 `1.5 Å` 处的夹取**（`EXP025_R_FLOOR_ANGSTROM`）。`bias_scale` 乘在 Group-1 **整个表达式**外面（内含配体↔环境软核），所以 `bias_scale=0` 不是"关掉偏置"而是**把配体关成鬼影**，`r→0` 必然；`CustomCVForce` 又会求值**每一个** CV，于是插件在鬼影几何上撞门。堵求值点行不通（一天找到三个）⟹ 方案 A **消除失效模式**。**改动**：CUDA 侧 `CudaLocalManyBodyResidualKernels.cpp` 能量侧 2 处夹取（`r_eff=max(r,floor)`，边照样计入）+ 力侧 2 处跳过（下限内 dE/dr≡0，`1/r` 因此不求值）；**Reference 侧 `g1_math_core.h` 同步**（原计划漏了这一步 —— Reference 平台经头文件用它，只改 CUDA 就是「同一个不变量两份实现」，G1/G2 钉的正是两者一致），复用同一常量不另抄数字。**下限取 1.5 而非 0.1 的实证**：径向基在小 r 处**并不衰减**（16 个中心均匀铺在 `[0,5] Å`、宽 0.333，0.1 Å 处基函数值仍 0.96），且支撑域以下那 5 个中心上的 pair weight 量级与训练充分的完全一样（max|w| 0.30–0.37 vs 0.32–0.39）——完全活跃却从未被数据约束。**真机 A/B**（RTX 2080 Ti，mixed，前后两份独立编译 `.so`、两个独立进程）：全局最小 lig–env 距离 **≥1.5 Å 时新旧能量逐位相同**，<1.5 Å 才变；**0.05/0.093 Å 由 fail-closed 变为有限值（0.093 正是真机崩点）**。⚠️ 判据是**全局**最小距离，不是被移动原子到锚点的距离。⚠️ **G1/G2/G3 三个一致性 harness 未跑**（缺 `yyjson`，套件按设计跳过）——改动最需要它们，已记为验证缺口。两处 `source_sha256` 已同步（`5c74365dffaa…`）。错误码 `EXP025_DEVICE_ERROR_MIN_DISTANCE=2` **保留**（`static_assert` 钉 ABI），只改触发策略。判据：`tests/test_lr06_residual_distance_clamp_2026_09_16.py`（5 passed）。⚠️ `r_floor` 目前是**编译期常量、绑定 shipped R1**，换支撑域不同的重训模型会静默沿用，升级路径写在 `r1_model_layout.h` 的 `ponytail:` 注释里。 | ✅ **是**（`B_φ` 在 `r<r_floor` 处定义变了 ⟹ `sampling_score_sha256` 变，与已冻结旧分数不可比；对 ΔG 正确性无影响，MBAR 对任意冻结偏置无偏，但 A/B 两臂必须都用新插件） |
| 09-16 | 🧪 **`Particle coordinate is NaN` 11 次的根因分清成两件事，新增 `tests/test_warmup_guard_asymmetry_2026_09_16.py`（4 passed，已做还原变异）。主线代码零改动。** ① **10 次是 2026-09-09 单批事故**（3 个体系 10 个 run，全部 09-09 14:28–16:49 启动），死在偏置爬坡第一个非零档 `scale=0.2`，当时那里是裸 `sim.step()`；守卫 `step_with_chunk_rollback` 于 **09-12 15:50 (`1769e71`)** 落地 ⟹ **已修**，旧 traceback 只是留在追加式 `launch.log` 里。⚠️ `ibs_engine.py` 那段注释的 `🔑 [2026-09-09]` 是**诊断日期不是落地日期**，照它推断会误判「09-11 之后还炸=没修好」。② **剩 1 次**（`cyclod_ligand2/rep3` 09-14，守卫之后）是**守卫不对称**：热化/爬坡用 `step_with_chunk_rollback`（回滚+减半+重做），紧随其后的偏置学习循环只有 `guarded_step`（docstring：**只改错误信息、不改动力学**）⟹ 学习阶段一炸整窗作废。**是已知设计不是遗漏**（`ibs_engine.py` 原注释：「warmup/learning 控制面没有 guard、也没有周期性 checkpoint」）。⚠️ **不是 `LR-06` 的鬼影期**（本轮先这么猜过，已证伪：学习循环跑在 `bias_scale=1.0`）。📌 要给它接回滚**必须连 `sampler.energy_buffer`/`energy_history`/`sampling_state_energy_history` 一起回滚**，否则偏置学习读到与构型对不上的样本 —— 不许当成「顺手补个守卫」。结论并入 [TODO_P3.md](TODO_P3.md) 的 `BM-A`。 | 否 |
| 09-16 | 🔴 **`stage2_first_window_max_states: 4` 进 `runabfe.py::PRESET_CONFIGS` 三个预设**（维护者拍板 / `BM-04`）。此前这个键**从未在任何一次真实运行里生效过** —— 3/3 预设、14/14 benchmark config、CLI 全都没有它，而 `abfe_config.json` 里那个 `4` **不会被自动加载**（`--config` 的 argparse 默认 `None`，`runabfe.py:3302` 的合并基底是 `PRESET_CONFIGS` 而非该文件）。缺席时 `first_cap is None` ⟹ 键不发出 ⟹ `first_window_max_states=None` ⟹ 无 cap，win0 可占 8 态；而 win0 一旦不可信，`tail_repartition_anchor` 恒 `None`（它之前没有可冻结的窗口）⟹ 缩跨度一族在**构造上**全不可行 ⟹ 真机 4 个 run 死在这里。⚠️ 本键在 `_PREOPT_DERIVED_PATH_KEYS`（**第 2 层**派生路径）：**全部现存 run 的 Stage-2 窗口缓存失配重采**；第 1 层（pilot 采样语义）不变 ⟹ **Stage 0/1 与 pilot/preopt 全部保留**，布局离线重算。📌 顺带定性：**`abfe_config.json` 不是"仓库默认配置"，只是一份样例** —— 判某键在某次运行里的值只能看该 run 的 `run_provenance.json`。 | ✅ **是**（Stage-2 窗口全量；Stage 0/1 + pilot 不破） |
| 09-16 | 📄 **维护者拍板关闭 `BM-04` / `DATA-02` / `DATA-03`**，覆盖 benchmark 7 个走不动的 run，全文进 [archive/TODO_closed_2026-09-16.md](archive/TODO_closed_2026-09-16.md)。**总体策略：4 个旧/非法 Stage-2 干净重建，3 个预算不足 run 用 `21/4` 有界续跑。** ① `BM-04` **不开放 `anchor=window 0`** —— win0 无前置共享态，把它塞进 tail-repartition 等于新增「整条路径重分」而非放宽判断（`tail_repartition_anchor` docstring：冻结的是 anchor **之前**的窗口）；② `DATA-02` 选**清理重建**，不回退 v3（`lambda_path_versions._publish()` 明文「只前进不后退」、回退直接 raise），也不许只删窗口文件保留 v4 指针（`load_current` 读指针，仍会读到非法布局）；③ `DATA-03` `max_path_insertions` **3→4**、`stage2_final_n_states` **保持 21** 原地 resume —— 该键已被 `ibs_engine.py:503` 从窗口采样身份里摘掉，λ 未变的窗口可复用；**不许再加到 5**，第 4 次仍不足就改走 23 干净重跑。成本补记：改该键会动 stage protocol key ⟹ **Stage 1 结果重新求解 ~28 min/指纹 ×3**（只重解、不重采样）。 | 否（决定本身；预设改动见上一条） |
| 09-16 | 🔑 **确立决策原则：「算对之前，沉没成本一律不计。」** 本轮代理**两次**把已有缓存的价值算高（一次建议「别为 2 个 run 作废 39 个 run 的缓存」、一次把预设改动半径估成「39 个 run 从头跑」），两次都被维护者纠正。实测那批缓存里**只有 2 个 run 出自修复后的代码**（`p38_ligand1/rep2,rep3`），其余 7 个完成的全是旧码、本来就不可引用，30 个未完成的无论如何要重做。**一个还没被证明算得对的结果，它的缓存没有保护价值。** ⟹ 今后同类权衡**先问「这条路径算得对吗」，再问「重算要多少 GPU」，顺序不能反**。 | 否 |
| 09-16 | 📄 **文档对账：`docs/TODO.md` 的 `- [ ]` 系统性落后于源码，13 条已修的缺陷还挂着未打勾**（`DECORR-01` / `AUDIT-S2-01` / `BUD-01`~`05` / `BUD-07` / `CTL-11`~`14`），连带 [CONTROLLER_BUDGET_AUDIT_2026-09-14.md](CONTROLLER_BUDGET_AUDIT_2026-09-14.md) 状态列 20 条虚挂的 `OPEN`（实测 65 条**全部**落地，缺标记的 #37 已回源码确认缺陷不在）。逐条证据与对账用的 `grep` 进 [archive/TODO_closed_2026-09-16.md](archive/TODO_closed_2026-09-16.md)。**危害不是漏修，是重复排查** —— 09-14 第三轮复核开出的 `BUD-*` 里就有一部分是在已修的地方重新查出来的。规矩：**「某条缺陷修没修」的权威只有源码，`- [ ]` 与状态列都是会过期的缓存。** 代码零改动。 | 否 |
| 09-16 | 📄 **修掉 `docs/TODO.md` 里 4 条与源码不符的陈述**（不是笔误，是会误导决策的）：① 「不许回退的约定」写「**只有三个真终态**」而 `TERMINAL_EXITS` 实测 **9 个**（多出 `DONE_UNTRUSTED` / `HALT_INVALID_INPUT` / `HALT_EVIDENCE_CONTRADICTS_DONE` / `HALT_LAMBDA_BUDGET_INSUFFICIENT` / `HALT_FRAMES_ADMISSION_CAP` / `ANALYSIS_COMPLETE_PRECISION_UNMEASURED`）—— 这条最危险，它属于**用来否决别人改动**的约定，照旧清单去审会把 6 个合法终态当违规打回；改成指向 `TERMINAL_EXITS`，**不在文档里维护第二份清单**。② `AUDIT-S2-03`「控制器真机零验证」不成立（39 rep 上过 GPU），改成「**有真机但没有一次以 `DONE` 收口**」。③ `REL-03`「零 GPU 复验」不成立，其子项 ③（两条腿 `pipeline.log` 分离）**已关闭**、①② 仍欠。④ `REL-04`「无一上过 GPU」改成「**跑过但没有一处被单独复验**」。另重测 `REL-05` 的中文行数（4 天涨 19k 行，旧数字已过期）。 | 否 |
| 09-16 | 📄 **新增 `BM-B`：全量 benchmark（13 体系 ×3 = 39 rep）30 个未完成 run 的归属** —— **最后启动在修复（05:03）之后的是 0 个**，没有一条能证明缺陷今天还在。与 `abfe-benchmark-31` 会话两轮交叉核实，定下三条取证口径（都是栽过之后才有的）：① 判代码版本只能用**最后一次启动横幅**的时刻，**不许用 `launch.log` 的 mtime**（实测差到 13h47m，本条第一版就这么判错过）；② `launch.log` 跨重启追加（单份最多 **25** 次启动），「最后一条异常」可能来自早就修好的那次；③ 判跑完没有时，`final_binding_results.json` 与 `pipeline_state.json` 对**被人工归档过**的 run **两个都会漏**（前者漏在文件被搬走、后者漏在 state 被回退；归档目录 `<体系>/_backup_*` 与 rep 目录**同级**）。**撤回**一条据作废签名写下的「5 个 TypeError ⟹ 重跑即可」（撤的是取证，`BM-05`② 那条修复独立成立）。正面证据：`p38_ligand1/rep2`(06:29:45)、`rep3`(07:34:59) 用修复后代码跑完，同体系 `rep1`(04:09 旧码) 卡住。 | 否 |
| 09-16 | 📄 **`REL-09`（输出目录与长任务保护）拿到第一份真机证据**：全量 benchmark 历史异常里 `输出目录已被另一次运行独占（目录锁）` 出现 **4 次，全部出自 `cyclod_ligand1`**。此前该条只是"理论上欠"。 | 否 |
| 09-14 | 🔴 **Stage-2 分窗判据从「等边数」改成「按热力学长度 ∫g dλ 均衡」**（`stage2_window_partition` `arclength` → **`metric_integral`**，同时 `stage2_window_max_states` **5 → 8**）。先前那条默认路径（`vanishing_subdomain_ranges_from_lambdas`）**根本不看任何度量** —— 只把 `lambdas.size` 传给贪心分组，名为 arclength、实为等边数；它 docstring 声称「λ 位置已编码热力学度规」，而 λ 表来自 `blended_metric_vanishing_lambdas` 的混合坐标 `(1−β)·ŝ+β·(1−λ)`（刻意不等弧长），之后还被 `densify_lambdas_by_free_energy` 按 ΔF 绝对值再插点 ⟹ **等边数 ≠ 等 ∫g**。实测（4W53 cyclod，21 态）：五窗弧长 2.28~3.29 大致相等，∫g 却是 19/37/67/97/54 **差 5 倍，而失败的正是 ∫g 最大那个**。⚠️ `max` 必须同时放开：卡住均衡的是 max 不是 min —— max=5 会强制 `[5,5,5,5,5]`、退化回等边数，这个开关等于没开；max=8 得 `[8,5,4,4,4]`，峰值 ∫g 97.0→74.2、不均衡 5.08→2.36，**一个态都不用多采**。⚠️ 这条路径是 **fail-closed** 的：读不到 preopt 的 `pilot_lambdas`/`metric_g` 就**直接抛**，不退回等弧长（拒绝静默改判据）。回退就两行（partition 改回 `"arclength"`、max 改回 `5`），理由与实测数字写在 `abfe_config.json` 的 `_comment_stage2_window_partition` 里。 | **是** |
| 09-14 | 🟡 **补齐三个「全仓只有读侧」的预算/预算相关键**。`stage2_production_budget_steps` 与 `stage2_max_production_blocks_per_window` 此前**配置/preset/CLI 三处都没有** ⟹ `run_provenance.json` 里永远不出现 ⟹ 控制器 `cap_known=False`、`plan()` 那道生产预算闸**从未生效过**，唯一刹车是硬编码的 4 块。现已三处补齐，**默认值严格等于原硬编码兜底**（cap 默认 `null` = **未知不是零**、不拦；blocks 默认 4），行为逐位不变。⚠️ `max_path_insertions` **刻意只补 CLI**：它经 `_path_evolution_kwargs` 进 **stage 协议指纹**，而那个函数只在该键被**显式配置**时才发出它 —— 给它任何默认值都会让先前没配过的 run 的 Stage 1/2 **结果**缓存全部失配重跑（Stage 1 实测 ~28 分钟/指纹）。 | 否 |
| 09-14 | 🟡 **审计 67 条全部收口**（63 + #64 win0 空转 + #65 manifest 改名 + f_k 封存身份恒为空 + 二面角修正全套 fail-open）。**下游必须跟改的对外可见变更**：① `join_lambda_two_sided_support` **不再写 `tau_int`**，只落 `statistical_inefficiency_g` —— 历史产物的 `tau_int` 装的**一直是 g**（名字错值对），读点统一写 `d.get("statistical_inefficiency_g", d.get("tau_int"))`，新旧都拿到 g；**刻意不保留「名字不变、值改成真 τ」的别名**，那是三种改法里最难发现的一种。② `feasible_repair_actions` 的 `split_tail_window` 语义改成「从 anchor 起重分整个尾段」（= 那个动作真正做的事），「末窗能否一分为二」拆成新键 **`split_last_window_in_two`**；不传 `tail_repartition_start_state` 时前者返回「判不了」的**字符串**而非 `None` —— 消费者写 `is None` 会**静默永假**。③ `skipped_windows` 拆成 `skipped_windows`（物理窗口）+ `skipped_sampling_units`（子窗 `unit_id`）。④ 新出口词 **`HALT_FRAMES_ADMISSION_CAP`**（终态，补帧块数配额全满）。⑤ 块账变四个平键（同段/跨段 × 窗口/采样单元）。⑥ `action_noop_fingerprint` 形参 `window_record` → `record`（它同时吃两张表，旧名在说谎）。**「哪些键在哪张表上」的权威名单只有一份，在 `tests/test_audit_2026_09_14_controller_budget.py` 里，从真实 `read()` 打差集建出**，源码只指路 —— 源码里再写一份注释表就是同一不变量两份实现，而注释那份必然先过期。 | 否 |
| 09-14 | 🔴 **Stage-2 控制器 + 预算：六路并行全面审计，63 条缺陷 + 事后新增 2 条**，全文与**逐条修复状态列**在 [CONTROLLER_BUDGET_AUDIT_2026-09-14.md](CONTROLLER_BUDGET_AUDIT_2026-09-14.md)（**本条不逐条罗列，去那份查**）。**三条贯穿性根因**：① **路由在「知道这个窗口缺什么」之前就锁定了** —— `decide()` 先算 `earliest` 再 `_pick()` 限死到那一个窗口，而 `earliest` 之前还有若干**无条件 `return`**，任何一条早期 return 会把后面所有分支对该窗口静默删掉；② **「未知」在四处有四套语义** —— 同一个 `warmup_steps_left is None`，一处 fail-closed、一处当「有钱」、一处写 None、一处 `or 0` 当零（「未知不是零，**两个方向都不是**」这条规矩的第 4~7 次复发）；③ **子窗（sampling unit）是二等公民** —— 不进 history snapshot、不进块账、no-op key 写了没人读、no-op 闸显式排除、`status` 不参与过滤、f_k 驳回记到父窗头上。**审计后另新增 2 条**：**#64** `_resume_cached_window_gate_status` 缺「身份全对、只差步数 ⟹ 增量续采」第三档 ⟹ 补帧目标每涨 250k 就把 EM + dt 测试 + Boresch 爬坡 + 冻结 burn-in + 只读复验整条前置链白跑一遍再被 checkpoint 逐字覆盖（**唯一一条正在实时烧 GPU 的**；P1+P2 已落盘，**P2 是本批唯一一处主动移除既有保护**，理由与代价见审计正文）；**#65** `write_comparison_manifest` 改了落盘名而唯一消费者 `stage2_ab_report.py` 在另一个文件里没跟 ⟹ A/B 报告全空且不报错。**两条留给下一个人的规矩**：「**零调用点 ≠ 忘了接**」（#18）与「**改名/改值时消费者几乎总在另一个文件里**，尤其「键名不变、值变了」会静默差一倍且不抛异常」（#65）。⚠️ 状态列**只有改动真的落盘之后才准改**，每条都要回源码核实。 | 否 |
| 09-14 | 🔴🔴 **decharging 湮灭了配体分子内库仑：所有已有 ΔG 作废**（`PME_DECHARGE_MODEL_VERSION` v4 → **v5**，couple-intramol=no）。v4 让配体内部的普通 ≥1-5 L–L 对连同配体–环境对一起按 λ² 缩放，于是 stage 1 里夹了一个 ~500 kJ/mol 的分子内项；当时的理由「两腿配体内部 Hamiltonian 完全相同 ⟹ 严格相消」**是错的推理** —— 哈密顿量相同不等于自由能相同，结合态与自由态的配体构象系综不同。实测 `cyclod_ligand2`：⟨U_intra⟩ complex −539.91 vs solvent −451.25，**差 −88.66**，而 ΔG_bind 对实验的误差是 **−88.05**（1:1 吻合到 0.7%，该体系过度结合 21 kcal/mol 的全部来源）；ligand–environment 那一半两腿只差 +2.99，正常。**为什么一直没暴露**：4W53/toluene（唯一验过实验值的体系）Σq²=0.499、刚性，stage1 两腿只差 +1.56 kJ/mol —— 对这个失效模式免疫，**别再拿它当 stage 1 正确的证据**。所有质量门也看不见：88 kJ/mol 是两条**独立腿之间**的差，没有一道门跨腿。修法：`_freeze_ligand_internal_coulomb` 挂 (1-λ²) 前缀的 `CustomBondForce` 逐对补回，λ=1 时恒为 0（P0-01 端点不变量不受影响），vanishing 腿同一份对表、前缀取 1，接缝按构造恒等。残留只剩配体自周期镜像项（~1/L³，PME 下不可消除，GROMACS 同样留着）。连带作废 `STAGE2_CONTROLLER_DESIGN_2026-09-12` 的头条 −3.48 kcal/mol（1.19σ）—— 它是两个 ~90 kJ/mol 的错误反号抵消。**逐行改动记录**：[PME_DECHARGE_V5_CHANGE_RECORD_2026-09-14.md](archive/PME_DECHARGE_V5_CHANGE_RECORD_2026-09-14.md) | **是** |
| 09-14 | **控制器逐出口梳理（45 个出口 / 12 个动作全扫）**，抓到三条：① 🔴 **补帧准入门实际只挂住 1/16 个出口** —— 先前给 `1c`/`1d` 加的两处**根本没落地**：那个脚本在第三个 `assert` 上抛异常、**整份文件写入被中止**，前两处改动一起丢了，而当时"测试通过"照样绿（测的是别的路径）。**修法换了方向**：门从"逐个分支挂"改为**在 `plan()` 里挂一次** —— `decide()` 有 16 个发 `RUN_PRODUCTION` 的出口，逐个挂必然漏；新增 AST 守卫钉住「`_frames_admission` 在 `decide()` 里只许出现一次、且必须在 `plan()` 内」，再想逐个挂就红。② **`action=DONE` 配 `exit=NO_FEASIBLE_ACTION`**（f_k 可达性那条分支）：`plan()` 按 `action == "DONE"` 算 `execution_status=COMPLETE` ⟹ 一个"无路可走"的结局被记成**执行完毕**；09-13 修过一处同样的毛病，这一处漏了。改 `NO_ACTION`，并加 AST 守卫扫全部出口的动作/出口自洽。③ **`SPLIT_TAIL_WINDOW` 声明了、执行器也认，但 `decide()` 从不发** —— 所有发它的位置都被 `_tgt_is_tail` / `_can_split_skew` 收窄掉了，可能对也可能收过头，**登记为 `AUDIT-S2-01` 待维护者拍板，不自行放宽**（放宽的方向正是 `CTL-07` 刚修掉的错）。梳理同时登记 `AUDIT-S2-02`（`decide()` 已 1319 行 / 45 个出口，是当天所有"漏改一处"的共同成因；把判据往 `plan()` 收挡得住"漏挂"、挡不住"分支顺序错"，真要治需单独立项重构）与 `AUDIT-S2-03`（当天约 30 处修复**全部只有离线验证**，三个 benchmark run 仍在另一节点跑旧代码）。`./tests/run_offline_tests.sh` **2383 passed / 3 skipped / 0 failed** | 否 |
| 09-14 | **补帧的准入与停止条件**（同僚复核：150 万步那条）：真机可以一路 25→50→…→**150 万步**，成因三条叠加 —— ① 早期分支（求解器跳窗 ⟹ 补帧）**走不到**后面的边际收益刹车；② 停滞保护把"多跑了一块"算成盘面进展、**重复计数清零**；③ 生产 cap 缺省是 unknown，拦不住。**「又产生了帧」不等于「获得了有用证据」。** 现在每个发补帧的分支（1c 子窗 / 1d 求解器跳窗 / 5b 物理窗口自检）都要过**同一道准入门** `_frames_admission()`：**①块数硬上限**（`stage2_max_production_blocks_per_window`，缺省 **4** 块 —— 缺省绝不能是无限，那正是要修的行为）；**②上一块必须有实质增益**，判据量是**求解器侧**的去相关帧数（不是自检那份，两者实测差 2–8 倍；更不是步数），没涨就 `NO_ACTION`+`NO_FEASIBLE_ACTION` 并报出逐块轨迹。配套：逐窗口/逐块的账 `production_blocks_by_window` 从自治历史快照建（同 `path_version` **且**同段才可比 —— 换段=换 f_k 不是同一条曲线），快照新增 `solver_n_decorrelated`，视图给物理窗口也补上 `solver_n_frames_decorrelated`（先前只有子窗有）。solver 跳窗因此最多先拿**一个有界诊断块** | 否 |
| 09-14 | 🔴🔴 **部分和冒充 ΔG：`cyclod_ligand2/rep1` 的已发布结果作废** —— 本项目迄今最严重的一类缺陷。`stage2_vanishing.json` 记着 `ΔG=-49.04 / converged=True`，而它的 `coverage_diagnostics` 写着 `input/solved_window_indices=[5]`、`covered_lambda_indices=[17…24]` ——**那是只解了末窗一个窗口的部分和**，只覆盖 λ 17 之后，却带着 `converged=True` 写进 stage 缓存完成标记、被 `final_results.json` 直接采信。与同体系 rep2（完整 21 态路径，`+44.09`）差 **90.36 kJ/mol、连符号都不同**；而 rep1 **自己**的全路径中间结果是 `+43.00`，与 rep2 只差 1.1 kJ/mol —— 所以那 90 **不是重复间离散、不是拼接失败、不是插 λ 改变了路径**（λ 表实测端点始终 1.0→0.0、单调、三次插点都落在中间），是**部分和冒充了 ΔG**。成因：09-12 那次只给「子集求解**失败**」开了 carve-out，子集求解**成功**时结果照样一路流下去。现在两道**互相独立**的门：① 上游 `_run_dual_lambda_stage`：只要 `only_window_indices is not None` 就一律标 `window_subset_no_stage_verdict` + `window_subset_indices` 并把 `converged` **钉成 False**（成功也不例外，它没有资格声称路径收敛）；② 下游 `_assert_stage_result_sane`：新增**覆盖度硬门**，只看 `coverage_diagnostics.covered_lambda_indices` 是否等于 `range(n_states)`，**与上游标记无关** —— 上游漏标、或结果来自别的路径，这里照样拦。仓库铁律「缺窗口的和不是 ΔG，是另一个量」第一次真正落成可执行判据。⚠️ **`cyclod_ligand2/rep1` 的 `final_results.json`（`total_delta_G_complex=541.94`）必须作废**，它的 vanishing 项是部分和 | 否 |
| 09-14 | **两条补强（同僚复核后续）**：① **现有 run 的采样身份可证明恢复** —— 只修写侧不够，既有 `ibs_state_*.json` 里全是 None，现有 run 还要再烧一轮预热才干净。新增 `_recover_stage_identity_from_convergence()`：从**同窗口 `convergence.json`**（同一次生产写下的独立记录，`_resume_cached_window_gate_status` 本来就拿它比身份）取回 `stage_protocol_key`，**前提是两份记录的 λ 逐位相等**（否则属另一套布局，借过来等于用别的系综给旧 PASS 背书）；取不到就保持 None、照旧重走预热，**绝不凭空合成身份**，并记 `loaded_stage_protocol_key_source` 供审计。四个真机 run 实测全部可恢复。② **物理窗口也要分「缺帧 / 偏斜」** —— 子窗那侧 `CTL-02` 已拆开，物理窗口的 `self_sufficient=False` 分支仍是一律补帧（同一个毛病漏了一处）。现在 `top1pct_veto` / `HARD_INSUFFICIENT` 归**偏斜**走缩跨度、不可行则 `NO_FEASIBLE_ACTION`，其余归样本量照常补帧。`./tests/run_offline_tests.sh` **2375 passed / 3 skipped / 0 failed** | 否 |
| 09-14 | **两条真机根因（`CTL` 之后又挖出来的）**：① **采样身份从来没被写进 `ibs_state`** —— `save_ibs_state` 属于 `IBSSampler`，写的是 `getattr(self, "stage_protocol_key", None)`，而该字段只存在于**管理器**；全仓 AST 扫描**零个赋值点**（`sampling_score_sha256` / `sampling_repair_policy` 都在建完 sampler 之后被逐个盖上，唯独漏了它和 `coion_identity`）。后果不是少一条元数据，是 2026-09-11 加的那道「旧 PASS 只在同一采样身份下有效」的闸**从落地那天起就永久关闭**：save 写 None → load 读 None → `_identity_matches` 恒 False → `skip_warmup_entirely` 恒 False ⟹ **每个 resume 的窗口都重走一遍完整预热收敛判定**、白烧预热预算；而预热预算耗尽正是窗口"再也进不去"、自治循环空转到迭代上限的**上游根因**（今天 `CTL-10` / 分支 1e / no-op 检测修的全是它的下游症状）。五个 benchmark run 实测：state 里两个字段全是 None，同窗口 `convergence.json` 里都有。现在管理器把 `stage_protocol_key` / `coion_identity` 一并盖到 sampler，`save_ibs_state` 也补上 `coion_identity`（原来只有 convergence 记着它，对账只有一半）。⚠️ 既有 state 仍是 None ⟹ 第一次 resume 照旧重走一遍预热（那次是真的没身份），之后才干净。② **布局变过之后旧产物被拿去重解**（真机 `cyclod_ligand1/rep2` 17:57 崩溃）：`ValueError('窗口 3 状态数与 window_ranges 不符')` 炸出流水线。实测路径已演化到 v2（win3 6 态→7 态）、`ibs_state` 的 λ/f_k 已是 7，而该窗口的**生产产物**还是 6 态旧帧；控制器知道证据过期（`stale_layout_evidence`）却仍选了 `PROBE_REANCHOR_EPOCH` —— 那些帧描述的是**另一个窗口几何**，解出来必然维度不符。新增分支 **1d-0**（排在预算类分支之前）：产物与当前布局对不上的窗口**只能重采**。这是「一个在构造上不可能成功的动作被发了出去」的**第三例**（前两例：被求解器跳掉、预热预算耗尽）。⚠️ 同僚曾推测该崩溃由 12:05 那次删除重置 `path_version` 引起 —— **实测否定**：版本链 v1/v2 俱在、当前是 v2 且自洽，崩的是"产物落后于布局"，与删除无关。`./tests/run_offline_tests.sh` **2364 passed / 3 skipped / 0 failed** | 否 |
| 09-14 | **控制器 `CTL-01`~`CTL-10` 十条全部修掉**（第二轮静态复核的全部未收口项；`./tests/run_offline_tests.sh` **2353 passed / 3 skipped / 0 failed**，起点 2311）：① **`CTL-01` 读到旧 stage 结果**——`_read_stage_result()` 用 `sorted(glob("stage2_*.json"))` 取第一个带 `converged` 的，而 `.`(46) < `_`(95) ⟹ `stage2_vanishing.json` **永远排在** `..._autonomous_inprogress.json` 之前，自治循环每轮刚写出的新证据根本读不到。改为**显式候选顺序 + `path_version` 必须匹配当前布局**（带版本且不符的是**另一条布局**的结论，直接跳过；不带的老产物放行），兜底扫描也改按 mtime 而非文件名；写盘侧相应盖上当前版本号，否则隔离形同虚设。② **`CTL-10` `warmup_steps_left` 读过期账本**——`warm = conv.bias_warmup or fail...` **优先取 `convergence.json`**，而那是生产跑完那一刻写的；之后窗口还会继续消耗（续跑再进一次预热、跨段继承），最新值在 `*_warmup_failure.json`，引擎自己还算好了 `warmup_budget_remaining_steps`。实测 rep2 win4：convergence 说 `left=140000`、引擎说 `remaining=0` ⟹ 控制器以为"预算有余"一直发动作、引擎每次以「本次可用 0 步」弹回，**连发 40 轮盘面一字节没变**，分支 1e 的 `left<=0` 永不触发。改为引擎读数优先、两份 ledger 取**消耗更多**的那份。**这是「同一个量两份实现」的第五例**（前四例：去相关帧数、f_k 字段、跳窗清单、生产计账）。③ **`CTL-02` 调度状态 ≠ 失败归因**（这组里唯一的**思路**错误）——采样单元只有一个 `complete` 布尔，而它要求的 `self_sufficient` **混合**了帧数/`N_eff/g`/top1% 三项，分支 1c 对所有 `complete=False` 一律补帧 ⟹ **偏斜类**失败被反复加帧（§5.1 实测越加越差）。拆成正交的 `needs_frames` 与 `support_failed`：前者加帧，后者缩跨度、没有有界动作就如实停在 `NO_FEASIBLE_ACTION`，**不拿加帧顶替**；9b 对子窗的补帧也先问"是不是真缺帧"。④ **`CTL-05`** ledger 按 `path_version` 隔离（旧布局的子窗不得参与当前求解，旧 `parents_done` 不得挡住新修补）。⑤ **`CTL-03`** 被 rewindow 取代的父窗不参与 `earliest` 排序 —— 它已退出求解覆盖，旧 warmup/f_k 状态不该挡住子窗自己的补帧。⑥ **`CTL-07`** held-out `REJECT` 也要先问"失败的是不是末窗"（与 9b 同一个毛病，这一处漏了）。⑦ **`CTL-06`** S2-C 的边际增长判据只看**当前段**，不再取"点数最多的段"（旧段点数多时会拿**旧 f_k** 的趋势裁决新段）。⑧ **`CTL-04`** 三条：非变异动作（只重解已有帧）不按生产块收费；**消耗未知不得记作零**（与"上限未知 ≠ 零"同一个错、方向相反，账不完整时余量与"耗尽"都必须是 unknown）；rewindow **采样前**持久登记 `SAMPLING_INTENT`、成功后推进 `SAMPLED`、恢复时按产物核销（`ABANDONED_NO_PRODUCT` 不删目录），合并求解只采信 `SAMPLED`。⑨ **`CTL-08`** 被求解器跳过的窗口在逐段残差前 `continue` ⟹ 补一条显式 `UNMEASURED`（缺证据 ≠ 没这回事）。⑩ **`CTL-09`** DECORR-01 provenance 加 energy/bias **内容指纹**（仅诊断、**不进任何缓存身份**），比较器改为按内容而非来源名称判输入同异，生产路径自动跑对照、输入一致才重放，结论落在 overlap 诊断与跳窗记录里。⚠️ **十条全部只有离线验证**（每条都做了还原变异确认会红），**真机零验证** | 否 |
| 09-14 | **静态复查 5 条（3×P0 + 2×P1）全部修掉**：① **S2-A 数据链**：求解器用 `sub_idx` 裁短了能量与 bias，却把**未裁短**的 `sampling_source_id` 传进逐段残差（长度必然不符）；而且那时 `bias_kj` 已被换成 collapse 后的 `bias_mix_kJ_mol`（**混合**偏置，不是任何一段生产时真正用的）。现在在 collapse **之前**留一份 `_fk_bias_production` 与同一 `sub_idx` 切片的 `_fk_source_ids`。② **rewindow 重解丢采样**：合并只加载基准段 + **当前**一组子窗 ⟹ 已有数字段整段丢；给第二个父窗建窗时第一个父窗的子窗也丢，而被取代的原始父窗反而重新进结果（台账却仍称它"已替代"）。改为**从台账整体重建完整覆盖**（所有采样段 + 所有有效 identity），并加"每个物理窗口要么在某段里、要么被取代"的 fail-closed；台账改为**求解之前**落盘，否则第一次求解看不见新子窗。③ **子窗 11/20 补不了帧**：完成条件只看"自检过 + 没被跳窗"，而跳窗门是**入场**下限（10）⟹ 过了入场就从待补列表消失，最终门（`min_decorrelated_samples`=20）却报 `worst_window=solver 索引`，9c 发出**不带 `unit_id`** 的普通补帧、执行器把越界下标交给原窗口路径。现在完成条件用**最终 solver 门**（读不到读数 ⟹ 不算完成），并加 `solver_index_to_unit` 反查表，9b/9c 把 solver 索引翻回 `unit_id`。原测试恰好把 11/20 断言成"已完成"，一并纠正。④ **预算准入**：聚合账只计胜出段 ⟹ 漏掉旧段真烧过的步数，改为**跨所有段累加**；动作门只问"已经耗尽" ⟹ 剩 100k 仍可发 250k 块，改为按**下一动作完整成本**准入；cap 范围定死 `production_steps_only`，建窗预留相应改成"每子窗一个生产首块"（预热走另一本账），准入与记账口径一致。⑤ **子窗身份串号**：所有 identity 的子窗共用 `10000/10001` ⟹ solver 跳窗记录会误分配给另一组。改为建窗时钉死 `solver_index_base` 写进台账、两侧同读；停滞 key 加 `unit_id`（否则同父窗的两个子窗共用计数）、盘面签名加逐子窗步数与完成态（否则真实补帧被记成 no-op）、no-op 记账按 unit 分开记。顺带两条：自检 `sufficient=False` 的措辞不再写死成"去相关帧数不足"（它同时含 N_eff/g 与 top1%）；S2-C 的逐块历史**按段分组、只在段内比较**（换段=换 f_k，混段会把"换了一份 f_k"伪装成"加帧的效果"） | 否 |
| 09-14 | **第四起：预热预算耗尽的窗口「进不去」也在空转 40 轮**（真机 `cyclod_ligand1/rep3` win0）：缓存门这次**正确**失效了（"当前目标步数 1500000 高于缓存产出时的目标 1250000 ⟹ 重新采样"），但重新进入窗口时入口的 warmup 门看到「上限 555000、已耗 555000，本次可用 0 步」直接抛 `LOCAL_VALIDATION_CAP` —— **补帧类动作在构造上推不动这个窗口**。而路由那条 `continue` 还**绕过了 09-14 早先加的 no-op 记账**，连"这条路走不通"都留不下，于是同一个动作连发 40 轮。两处修：① 新增 `decide()` 分支 **1e**（排在预算可行性分支**之前**，因为 1a 的"退而补生产帧"对这种窗口同样不成立）：`warmup_steps_left <= 0` 且 f_k 非 `verified` ⟹ 不发补帧/续预热，改发**缩跨度**（插 λ，不需要重进预热）；连缩跨度都不可行才 `NO_ACTION`+`NO_FEASIBLE_ACTION`，并明写「要继续必须**显式升档**该窗口的 warmup 预算 —— 那是调用方的决定，控制器不替它做」。② 路由信号的 `continue` 之前补上同一份 no-op 记账。顺带把 `test_no_budget_is_consumed_before_action_selection` 的 fixture 从「零余量」改成「有余量但不够开新 Epoch」（剩 35000 < 首档 50000）—— 零余量是**另一种**情形，两者不该共用一个分支 | 否 |
| 09-14 | **第三起 no-op 死循环 ⟹ 改成通用判据**（真机 `cyclod_ligand2/rep2`）：`CONTINUE_WARMUP[4]` 连发 **40 次**，`production_steps`（1,750,000）与 `warmup_steps_left`（140,000）一动不动。逐轮快照里 `repeat_count` 在 **1→2→3 之间循环** —— 停滞保护每到 3 就降级到探针，探针又改了一点盘面让 `last_sig` 变化，于是 `seen` 归 1、`escalated` 被 `pop` 掉，**永远走不到退出**。根因：窗口级 resume 缓存门在**走到预热之前**就 `continue` 掉整个窗口（"已有有效缓存能量 … resume 模式下跳过重新采样"）⟹ `CONTINUE_WARMUP` 对生产缓存完整的窗口是**结构性 no-op**；它真正缺的是生产帧（端点 σ）。① `decide()`：`CONTINUE_WARMUP` 被记为当前盘面 no-op 时改发 `RUN_PRODUCTION`。② **通用 no-op 检测**：前两起靠逐动作打补丁（重标定 / 探针），第三起换了个动作就漏 —— 现在在主循环里**执行前后各取一次盘面指纹**（就是停滞保护那一份：生产步数/自检结论/段/路径版本/求解器跳窗/rewindow 身份/预热余额），一样就记 `action_changed_nothing_on_disk`，**任何动作**都逃不掉。只记账不改执行，换不换动作仍由 `decide()` 定 | 否 |
| 09-14 | **裁决 3（S2-A 逐段门）+ 裁决 4（DECORR-01 同输入对照）**：③ **逐段门**：多段窗口的帧采自两份不同偏置，`_load_ibs_window_outputs_merged` 显式 `base.pop("f_k")`，先前直接记 `no_effective_f_k_for_these_frames` ⟹ 这项证据对多段窗口**永久缺失**（循环一旦用换 Epoch 修好一个窗口就再也拿不到）。新增 `_per_segment_cumulative_fk_residual()`：帧切片由 `sampling_source_id`（**有序帧映射**，不猜边界）给出，每段用 `sampling_segment_f_k[i]` 自己的冻结 f_k，在**自己的帧切片**上独立解一次 —— **绝不**拿合并后的 ΔF 作逐段望远镜相消（窗口级只给 `max_segment_span_kJ_mol_REPORT_ONLY`，不合成 span 去判门）。窗口级三态合成：任一**有效**段 FAIL 则 FAIL；否则有缺证据段为 `UNMEASURED`；全部段 PASS 才 PASS。每段身份（段号 + 来源目录）、帧范围、f_k 指纹、结论全部落在 `segments[]` 里可审计；控制器侧加 `cum_fk_per_segment` / `cum_fk_gate_is_per_segment`。④ **同输入对照**：新增 `decorrelation_provenance()`（输入身份 = 能量数组来源/观测量/帧数/分段/kT，输出 = g / worst_state / 去相关帧数 / **实际抽样索引**首尾），自检与求解器**两个调用点各记一份**（自检写进 `*_self_support.json`，求解器写进 `window_overlap_diagnostics` 与跳窗记录）；`compare_decorrelation_provenance()` **先比输入构造**，输入不同就把差异归到输入上并明写「不得改任一侧的数去追平另一侧」；只有输入一致才由 `replay_decorrelation()` 在**完全相同的输入**上重放共用的 `_decorrelate_by_worst_target_state`。**纯诊断装置，不改任何判定**。⚠️ rep2 win3 那个 2–8 倍分歧的**来源仍未证明** —— 这套装置是用来证明它的，不是用来消掉它的 | 否 |
| 09-14 | **裁决 2：生产预算独立成账**：控制器先前只有 `warmup_steps_left` 一本账（`per_window_budget_remaining` 也是它），于是「还能不能补帧」判不出来，只好拿预热余额去终止一个只花生产帧的动作。新增 `view["production_budget"]`：stage 上限（config `stage2_production_budget_steps`）、实际消耗、逐**物理窗**与逐**子窗**的生产已用量、新系综预留额度（一次预热 + 首个生产块）。**上限读不到就是 `unknown`，不是 0，也不许由预热余额代替** —— 上限未知时「预算耗尽」这个结论本身不成立。`GLOBAL_BUDGET_EXHAUSTED` 现在由 `decide()` 在 `plan()` 内的**一道统一闸**发出（放在 `plan()` 而不是逐个分支，因为逐个分支必然漏）：只有生产上限**已知且确实耗尽**、且动作会花 GPU（`ANALYZE`/`DONE`/`NO_ACTION` 之外）才改写成 `NO_ACTION`+终态，并把**原动作与理由**原样留在 reason 里。**停滞保护不再推断预算**（推不动 ≠ 没钱），一律 `NO_FEASIBLE_ACTION`。`IMMUTABLE_REWINDOW` 建新系综前先查预留额度（2 个子窗 × 预留），留不出来就不开半截系综；上限 unknown 时不拦。词汇表守卫的字面量抽取扩到「`plan()` 内改写 action/exit_ 的赋值」，否则活着的词会被误判成死词 | 否 |
| 09-14 | **执行器的 no-op 必须记账，控制器才不会原样再发一次**（第二起真机同形状事故）：`RECALIBRATE_FK[0]` 连发 4 次、降级到 `PROBE_REANCHOR_EPOCH` 仍是 no-op、盘面一字节未变即 `NO_FEASIBLE_ACTION` 退出 —— 而退出前**没有**去试真正对症的动作。这次与前一起的区别是**没有窗口被求解器跳掉**，所以分支 1d 不适用；真因是**同一个决定两套判据、且读的不是同一份数据**：f_k 探针按**节奏**（已采步数）推荐窗口，执行器按**相邻位移 > 0.5 kJ/mol** 放行，而执行器是从「有这个窗口数据的最新段」重解（真机那次是只含 win0 的 `vanishing_9`，日志明写「缺窗口 [1,2,3]」），探针读的却是全量 —— 于是探针报 w0=4.12/w1=4.86/w2=7.08/w3=3.60 全部远超阈值，执行器却报「没有窗口超过阈值」。执行器**知道**自己没做事，控制器无从得知。新增 `stage2_noop_actions.json` 记账：执行器在 `RECALIBRATE_FK` / `PROBE_REANCHOR_EPOCH` 返回 None 时写入，指纹由**两侧共用**的 `abfe_preoptimizer.action_noop_fingerprint()` 算（路径版本 + 该窗口生产步数 + 证据所在段）；`decide()` 的三处（探针建议重标定 / held-out 判不了走探针 / warmup 预算耗尽走重标定）都先查它。记的是「**在这个盘面上**没用」不是「这个动作永远没用」—— 补过一块帧指纹就变、动作自动重新可选 | 否 |
| 09-14 | **两条真机新 bug + 裁决 1（子窗成为可调度采样单元）**：① **分析用错了 f_k 字段**（`cyclod_ligand1/rep3` win0 炸 `窗口 0 f_k 与冻结生产入口不一致`）：loader 断言 `ibs_state["f_k"] == production_entry_f_k`，但两个字段**语义不同** —— `production_entry_f_k` 在生产开始时锁一次、生产期间从不再赋值；`f_k` 是学习器**活的**状态，续跑时窗口再进一次 warmup 就会动（实测 `was_resumed=True` / `no_budget_remaining_on_resume` / `final_mode=learning`，差 0.03~0.28 kJ/mol）。ESS 门自己的注释写的就是「该窗口**冻结进生产**的 f_k」。新增 `_resolve_analysis_f_k()` 直接取入口标记，并**新加一道原来没有的横向核对**：入口标记必须与 convergence 里独立记录的 `bias_warmup.frozen_f_k_at_last_freeze` 一致（实测两者逐位相同）；两份独立记录对不上、或缺标记，仍然 fail-closed。② **被求解器跳掉的窗口反复吃重标定 no-op**：`RECALIBRATE_FK[0]` 连发 4 次、降级到 `PROBE_REANCHOR_EPOCH` 还是同一个 no-op，盘面一字节未变即 `NO_FEASIBLE_ACTION` 退出。根因是重解类动作要**拿这个窗口已有的帧重解**，而"被跳掉"的意思正是"这些帧不够解"（9 帧 < 10）⟹ 整族动作在构造上是 no-op；而且 f_k 探针按**节奏**推荐、执行器按**相邻位移 > 0.5 kJ/mol** 放行 —— 同一个决定两套判据，且被跳的窗口**根本没有位移读数**。新增分支 1d：earliest 被求解器跳掉 ⟹ 直接 `RUN_PRODUCTION`（低支撑永远是「尚不可测」，动作是加预算）。③ **裁决 1 落地**：`view["windows"]` 仍是物理窗口，rewindow 子窗另立 `view["sampling_units"]`（稳定 `unit_id`、父窗、局部下标、区间、输出/checkpoint 目录、`solver_index`）；`decide()` 对未完成子窗发**带 `unit_id`** 的 `RUN_PRODUCTION`；执行器 `_topup_rewindow_child()` 续跑**该子窗自己的 checkpoint**，随后走共享的 `_solve_with_rewindow_children()` 重解并落盘 —— 建窗与补帧共用同一份合并求解，`_immutable_rewindow_step()` 不再是一次性动作。**没有**靠放宽 `_segment_stage_names()` 来做：子窗局部下标 0/1 与父窗索引同名不同义，`10000` 偏移只是求解器命名空间 | 否 |
| 09-14 | **DECORR-01 路由 + 有界 `IMMUTABLE_REWINDOW` + 9b 中间窗归因 + S2-A `UNMEASURED`（`STAGE2_CONTROLLER_PROTOCOL_VERSION` 不变，不进指纹）**：① **DECORR-01 按裁决接通** —— 求解器对「能否进入求解、是否跳窗」有**操作权威**：`_window_state` 在 `ANALYSIS_ELIGIBLE` **之前**判 `solver_skip`，被踢出协方差链的窗口一律当**未解决**（实测 win3 自检 56 帧 `sufficient=True`、求解器报 9/7 并跳窗，先前被判 ELIGIBLE、earliest 直接越过它）。**两侧数字不强行看齐**：新增 `evidence_decorrelation{solver, self_check, authority, do_not_reconcile}`，分别记输入帧集/采样段/观测量/`g`/阈值/协议版本；自检那一项显式命名为 `sufficient_INCLUDES_N_eff_AND_top1pct`，杜绝被读成「求解器帧数够」。② **新动作 `IMMUTABLE_REWINDOW`（有界）**：一次只修**一个**被归因的窗口，在**固定 λ 表**上建可完整覆盖其原区间的重叠子系综（复用 `_build_vanishing_rescue_ranges`），只采**一个**预算块，采完立刻读盘→求解→读回求解器帧数与跳窗→回 `decide()`。身份锁 = sha256(路径版本+父窗口+父区间+子区间+λ表)；覆盖完整性与接缝共享节点**双 fail-closed**；原生产数据不改写（写 `<stage>_rewindow_<identity>/`，后缀非纯数字 ⟹ 段发现与多段合并都不会误收）；独立端点段那条 fail-closed 保留；台账 `stage2_rewindow_ledger.json` 按身份分开记 `f_k_scope=own_frozen_f_k_per_child_ensemble`。**不是**已退役的 bridge（批量建系综→一次重解→直接撞门）。③ **9b 中间窗归因**：`_can_split` 先前只问「拆末窗结构上可不可行」、不问「失败的是不是末窗」⟹ 中间窗的支撑失败会换来一次**与它无关**的尾段重分（烧 GPU、改布局、作废下游证据，元凶一字节没动）。现在拆末窗只在最差窗口**就是末窗**时可选；λ 表上两个动作都不可行 ⟹ 落到有界 rewindow（一个父窗口只建一次）。④ **停滞签名补求解器侧证据**：原签名只有生产步数/自检结论/段/路径版本，全是自检与执行侧的量 ⟹「补一块帧后求解器从跳窗变不跳窗」这种真实推进看不见、被算成推不动。现在并入 `solver_skip.n_frames_after_decorrelation`、跨段跳窗并集、段清单与 rewindow 身份。⑤ **S2-A**：多段窗口 `base.pop("f_k")` ⟹ 残差记录只有 `error`、没有 `verdict`，先前落成 `None`，而 `None` 在 5a-1 的语义是「去 ANALYZE 把它算出来」—— 结构上算不出来 ⟹ 又一个无限 ANALYZE。现在显式记 `UNMEASURED`（5a-2 已按它分岔）；父窗口被 rewindow 取代后整个不在求解覆盖里、连记录都没有，同样记 `UNMEASURED`，理由 `replaced_by_immutable_rewindow_children_own_f_k` —— **子系综不能替父窗口宣称这道门通过** | 否 |
| 09-14 | **控制器五处（同僚审查后逐条核实，`STAGE2_CONTROLLER_PROTOCOL_VERSION` 不变）**：① **「已关闭 path_evolution」是一句假日志** —— `_sampling_repair_policy` 在静音块里只被读来拼字符串、**从没被赋值**，而且那段代码跑在 `_run_stage2_with_path_evolution` **之后 150 行**，路径早就被改过；另外两样都有真赋值+硬断言，只有这一样两者都没有。现在在 `_sampling_repair_policy` 定义处就降级成 `non_mutating_v1`（同属 `_NON_MUTATING_POLICY_CLASS`，**指纹中性**）并补硬断言。连带修：降级后走的是 `should_run_path_evolution` 的**早退**分支，而那两个 `return run_once(...)` **不在** 09-14 早先那个路由 handler 的 try 覆盖范围内 ⟹ 不包住等于路由修复在默认配置下整个失效。② **9b/9c 漏动作**：`_remedies` 是集合，9b 用 `in` 判、9c 用 `==` 判 ⟹ 支撑类与样本量类**同时**失败时 9c 永远不可达，9b 的布局动作一旦不可行就直接 `NO_ACTION` 终止，样本量那条需求被静默丢掉（真机：`rescue_window_1` 支撑 + `rescue_window_2` 11/20 帧，后者永远拿不到帧）。9b 不可行时改为**落到 9c**；9c 判据改 `in`，且最差窗口只在**样本量类**的失败里挑。③ **预热账本终止了只花生产帧的动作**：四处「新 Epoch 付不起验证额度 ⟹ 退而补生产帧」都发 `GLOBAL_BUDGET_EXHAUSTED`（在 `TERMINAL_EXITS` 里）⟹ 主循环在分发**之前**就 break，补帧根本不执行；而 `all_windows_budget_exhausted` 判的是所有窗口 `warmup_steps_left ≤ 0`，`RUN_PRODUCTION` 用已冻结 f_k、一步验证预算都不花。四处改为恒发路由 `HALT_BUDGET`；`GLOBAL_BUDGET_EXHAUSTED` 改由**主循环停滞保护**发出（"补过一轮、盘上仍没变，且预热预算全干"才成立），词汇表守卫的 `emitted_by_outer_loop` 相应登记。⚠️ 控制器里**仍然没有"生产补帧预算"这个量**（`per_window_budget_remaining` 也是 `warmup_steps_left`），双层预算是 PLAN 第 5 步 F、未实现。④ **退出原因落盘**：`_write_history()` 原来只写 `{iterations, final_ranges}`，撞迭代上限只剩一行日志，三个真终态也只在 `history[-1]["exit"]`，而停滞保护还会**改写**那个键 ⟹ 事后没法回答"这一跑是收敛了、没预算了、还是转够 40 轮了"。新增顶层 `outcome{status, exit, exit_emitted_by, reason, iterations_used}`，六个出口全部登记，迭代上限记 `ITERATION_CAP_NOT_CONVERGED`（**不是**终态）。⑤ **`S2-C` 关闭**：`support_history` 按**采样段**建 ⟹ 单段窗口永远只有 1 个点、`len(h)>=2` 从不成立 ⟹「加帧已被证伪就别再加」这道唯一的刹车对单段窗口**从不触发**（布局一变旧段被标 stale，多段也会退回 1 点）。改为叠加主循环**已经在写的**逐轮 `snapshot`（逐块），可比性由 `path_version` + `segment`（换段=换 f_k）双身份保证，按 `production_steps` 排序去重。⚠️ 核实中还发现**逐窗自检与全局求解器对"去相关帧数"是两份实现、口径差 2–8 倍**（rep2 实测 win3 自检 56 帧 `sufficient=True`、求解器报 9 和 7 帧并跳过它），谁是权威**尚未定**，登记进 TODO | 否 |
| 09-14 | **`skipped_windows` 在失败出口上被整段丢掉**（接上条，这才是控制器推不动的真因）：`solve_stage_integrated` 的成功返回带 `skipped_windows` / `path_is_complete`，但三个拼接失败出口走的是裸 `_fallback()` —— 只返回 `error` / `converged` / `total_delta_G` / `total_error` 四个键。于是控制器看到的是「路径不完整，但不知道缺谁」：它那条**「被踢出协方差链 ⟹ 补采」的分支读的正是 `stage["skipped_windows"]`**，空的 ⟹ 分支不可达（代码注释里写的"这条路径以前不可达"其实一直没修好），只能对同一个窗口反复 ANALYZE 到停滞保护退出。新增 `_incomplete_path_fallback()`，三个出口全部改走它，口径与成功路径一致；测试用 AST 钉住这三个 reason 不许再出现在裸 `_fallback` 上 | 否 |
| 09-14 | **控制器对「求解器用不了的窗口」空转 4 轮**（接上条真机）：路由生效后自治循环起来了，但 win0（去相关 9 帧 < 下限 10）连发 4 次 `ANALYZE` 且盘上零变化 ⟹ 停滞保护降级到 `PROBE_REANCHOR_EPOCH`（探针判「f_k 不是瓶颈」什么也没做）⟹ `NO_FEASIBLE_ACTION` 退出。根因在分支 **5a-1**：它认为「累计 f_k 偏差在现有帧上就能算、零额外采样」，**但那有前提——这批帧求解器得能用**。帧数低于下限的窗口在生成任何诊断之前就被 `continue` 掉，`cumulative_fk_residual_production` 永远不会出现 ⟹ 无限 ANALYZE，而它真正需要的补采分支（5b/6 的 `RUN_PRODUCTION`）就在下面几行、永远轮不到。5a-1 现在跳过 `self_sufficient is False` 与 `skipped_windows` 里的窗口。② 顺带修 `_assert_stage_result_sane` 的**误诊**：缺 `target_support_gate` 时一律报「来自升级前的求解路径，请重新求解（不用重采样）」——而这次是求解器压根没走到判门那一步，重解一万次也不会有。带 `stage_solve_error_not_fatal` / `routing_signal` 时改报真因（有窗口帧数不足被跳过、拼接断链）并指向加采样预算；两种情况都照旧**拒绝标记 completed** | 否 |
| 09-14 | **窗口被跳过 ⟹ 全局拼接失败也炸穿流水线（另一组体系真机）**：win0/win3 去相关后只剩 7~9 帧（门 10）被求解器跳过 ⟹ 拼接链上缺共享 λ 节点 ⟹ `window_overlap_broken` ⟹ `_run_dual_lambda_stage` 无条件 `RuntimeError`。但 `SKIPPED_WINDOW` / `INSUFFICIENT_DATA` 按约定**是路由信号**（「低支撑永远不是 FAIL，是『尚不可测』，动作是加预算」），而自治控制器有现成的补采分支。现在这三个 reason（`no_local_tmbAR_results` / `window_overlap_broken` / `window_overlap_broken_for_covariance_chain`，新常量 `STAGE_SOLVE_ROUTING_ERRORS`）改为标 `routing_signal=SKIPPED_WINDOW` + 原错误留档 `stage_solve_error_not_fatal`，**`converged` 保持 False**（下游默认拒绝的门照旧拦，不是把失败改成成功），交控制器补采；停滞保护三次后 NO_FEASIBLE_ACTION 带诊断退出。错误字符串由测试对着 `ibs_engine` 的 `_fallback()` 字面量钉住 —— `no_local_tmbAR_results` 的大小写就是那么怪，管线注释里一度写成全小写 | 否 |
| 09-14 | **`LOCAL_VALIDATION_CAP` 炸穿流水线（真机 `cyclod_ligand2/rep2` 窗口 4）**：`IBSValidationBudgetIndeterminateError`（单周期预算内 Δf−ΔF 没求出 ⟹ 对这份 f_k **无结论**）按约定是**路由信号**，`_run_stage2_autonomous` 有它的路由分支，但**第一次执行**走的 `_run_stage2_with_path_evolution` 一个 handler 都没有 ⟹ 信号在自治控制器接管**之前**直接炸到 `run_full_pipeline`。现在在那里接住、原样交回（不改 f_k、不插 λ、不拆窗、不动路径），由控制器重判；**仅当下游真有控制器**（`route_to_controller`，接 `stage2_autonomous_controller`）才咽下去，否则照旧抛 —— 没人去延长验证预算时，静默返回会把「没结论」伪装成执行完毕。顺带把 `test_validation_indeterminate_routing` 那条守卫从「不许有 handler」收紧成「handler 里不许有布局动作」：原判据拦的是拆窗/插点，却顺带禁掉了「接住并上交」，正是这次炸管线的原因 | 否 |
| 09-14 | **大修后首轮抓 bug（三条，均为 09-12/13 收拢期引入或暴露）**：① `_legalize_tail_window` 在 `S2-D` 收拢时被删掉函数内 `from abfe_preoptimizer import ...`，但函数体里 `insert_lambda_in_failed_ibs_window` / `repartition_tail_from_anchor` / `record_tail_repartition_version` / `_pre` **四个裸名字没跟着改** ⟹ 末窗超可拆上限那条路一走到就 `NameError`；早退那条路（`tail_k <= 2*hi−1`）不碰它们，所以离线测试全绿、只有 GPU 上插过 λ 之后才炸（真机 15:59 连插两次顶到 K=6 正是这条路）。同类 bug 本轮已是第二次（`_relearn_epoch_required_steps` 是第一次），而那次只留了一条 grep 字符串的测试 ⟹ 本次改用 `symtable` 全仓通用判据 `tests/test_no_undefined_globals.py`（函数里当全局读、模块顶层又没有 ⟹ 红），已用还原变异验证会红。② `stage_quality_gate_failures` 的 `target_support_gate` 归因**挑错了尺子**：最差窗口按 `absolute_ess`（去相关后的 mixture 覆盖度，`ESS_GATE_PROTOCOL_VERSION=2` 起已降级为纯诊断）挑，而这道门判的是 raw 单参考量 —— 实测同批窗口 mixture 0.4684 / raw 0.0196，排序可以完全相反 ⟹ λ 会插到不是元凶的窗口上（TODO「四个量不许混用」）。改为按判否的那一支挑（raw ESS ↔ `raw_min_absolute_ess`；top1% ↔ `top1pct_raw_weight` 取最大，`_worst_window_by(largest=True)` 此前是死参数），并从嵌套的 IBS 段门取回 stage 级缺失的 top1% 阈值。③ 多窗补采**按段分组、一段一次**：`segment_dirs_for_evidence` 对跨段窗口集合 fail-closed，而多窗补采天然跨段（真机 win0-3 在 `vanishing_2`、win4 在基准段），新增的分支 9c「端点 σ 无逐窗归因 ⟹ 全窗各补一块」必然触发 ⟹ `ValueError` 穿过 `except Exception` 被 re-raise、整条流水线死在一个本来能正常执行的动作上。分组逻辑抽成 `abfe_preoptimizer.windows_by_segment`（纯函数、与目标步数同源）。`./tests/run_offline_tests.sh` **2245 passed / 3 skipped / 0 failed**（基线 2239 + 6 条新测试）| 否 |
| 09-13 | **文档对账（无代码变更）**：① 设计文档 §9 三张符号表**删掉「行」这一列** —— 实测 §9.1 九个行号全错（`decide()` 标 2378、实际 2517）、§9.3 错两个、§9.2 还有**四个符号根本不在那个文件里**（被 `S2-D` 搬去 `abfe_preoptimizer`，与同文 §9.4 自相矛盾）。行号注定漂，而第一列本来就是可 grep 的符号名，删掉一整类过期源。②§7「已知缺口（未修）」改标题并逐条标注：五条已修两条（第 2 条 `S2-B`、第 4 条 `S2-D`），其中第 4 条原文「设计要求是包在一起」**是被否掉的口径**，收拢判据是「决策同源的进 preopt、写盘的留 pipeline」。③ TODO 优先级表计数与全绿基线数字更新（4 条→3 条、2223→2239），三条关闭说明归并到待办上方让剩余条目连成一片。④ `RELEASE_READINESS` 的三处「仍欠」补上指向 `REL-08/09/10` 的交叉引用；其中「`environment.yml` 个人 prefix」实测已过期（全文 grep 不到 `/home/`），就地标掉 | — |
| 09-13 | **`S2-B` 完整性要求与预算解耦**：`minimum_complete_validation_frames` 原式是 `max(统计目标, validation_attempt_budget_steps // check_chunk)`，而续验路径上 `validation_attempt_budget_steps = full_bias_step_budget` ⟹ **给的预算越多、要求的帧数越高**（实测 `max(200, 515000//250) = 2061`，是统计目标的 10 倍）。现只剩 `required_consecutive_bias_updates * 20`。⚠️ **原待办的后果描述是错的**：「可达性判据第 2 档因此失效」从没发生过 —— 这个量在本仓自第一个 commit 起就只有「赋值+写报告」两处、**零条件判断**，而可达性预检的 T 读的是 `decorrelated_frames_required`（去相关下限 10），控制器侧字段名早已是 `validation_completeness_frames_REPORT_ONLY`。所以修它**不改变任何判定**；真实危害是这个名叫「要求」实为「预算」的数会骗读它的人（T 一度就被错取成它、gcrit 算小 20 倍、把只差 26% 帧数的 win4 判成「差 7.5 倍不可达」），以及两次 run 的报告没法横向比。守卫按源码 AST 断言右侧不得引用任何预算量，已用旧公式变异验证会红 | 否 |
| 09-13 | **控制器词汇表清理 + AST 守卫**：`ACTIONS`/`EXITS`/`TERMINAL_EXITS` 是三份声明式清单、全仓没有任何代码校验它们，实测漂出三种形状 —— ①**死词**：`HALT_NO_FEASIBLE_ACTION` 从没被发过（真在用的是无前缀的 `NO_FEASIBLE_ACTION`），而且它**不在 `TERMINAL_EXITS` 里**，照着清单发它会得到一个"看起来像终止、实际不终止"的出口；`HALT_TRUNCATED_PATH` 同样零发出（缺窗口现在走分支 6b 的 `RUN_PRODUCTION`、是路由不是终止）。②**反向的洞**：`GLOBAL_BUDGET_EXHAUSTED` / `NO_FEASIBLE_ACTION` 是真终态却漏在 `EXITS` 之外，"每一个结局都在这里"这句话不成立。③ `ACTIONS` 把 `INSERT_LAMBDA` 列了两遍。三条全修；`HALT_NO_ATTRIBUTION` 经核实**仍然活着**（warmup 预算耗尽且 stage 结果没落盘时发），保留。新增 `tests/test_stage2_controller_vocabulary.py`：按**源码 AST** 抽出 `plan()` 每个 action/`exit_` 的字符串字面量（含条件表达式两支）与三份清单互相对账，并钉住 `NO_ACTION` 只能配终态出口。四种变异逐一验证守卫真的会红 | 否 |
| 09-13 | **控制器终态与设计契约归一（`STAGE2_CONTROLLER_PROTOCOL_VERSION` 1→2）**：`decide()` 分支 8 原本是 `"DONE" if has_stage_result else "ANALYZE"` —— **只要 stage 结果文件存在就返回 `DONE`+`exit=DONE`**，哪怕结果里明写 `converged=False`，与设计 §2「`DONE` = 全路径证据齐备且**达标**」直接冲突；而主循环只看 `plan["terminal"]` 就 break、**不看** `evidence_status`，所以判据没过时自治诊断会提前结束（最终结果没被错误发布——下游另有默认拒绝 `converged=False` 的门——但循环白停）。现在按**失败的那道门**归因分岔：新增纯函数 `stage_quality_gate_failures()` 逐条读出五个门的实测值/阈值/最差窗口；支撑偏斜类→缩跨度（§5.1 加帧治不了），样本量精度类→加采样（§3 尚不可测），归因不出来或动作都不可行→新动作 **`NO_ACTION`** + `exit=NO_FEASIBLE_ACTION`（借 `DONE` 会让 `execution_status` 算成 `COMPLETE`）。`allow_untrusted` 仍走 `DONE_UNTRUSTED`，但 `evidence_status` 不被改写。**刻意不改成 `ANALYZE`**：分析已经跑过，再跑是空转。协议号只盖在 read/decide/replay payload 上、不进任何缓存指纹（全仓 grep 确认） | 否 |
| 09-13 | **`S2-F` 关闭：10 条红测试对齐新语义**。其中**只有 3 条真的是断言旧了**（预算耗尽出口 `HALT_NO_ATTRIBUTION`→`HALT_BUDGET`；健康窗口 `frames_short_by` `0`→`None`；跨腿构象门 `raises(ValueError)`→`gate=="WARN"` 不阻断），另外 **7 条是造数据的 fixture 旧了** —— run 目录缺逐窗自检产物/缺 warmup 预算台账，于是 fail-closed 的前置门把请求兜住，测试想钉的分支一条都走不到。照实际输出改断言会让这 7 条退化成同一个测试、四条分支覆盖归零，所以修的是 fixture。顺带更正 `test_stage2_repair_controller.py` 模块 docstring 第 1 条（原写「缺窗口优先于一切」，正是被实测推翻的口径）。全量 `./tests/run_offline_tests.sh` 2231 passed / 3 skipped / 0 failed | 否 |
| 09-12 | **`BOR-01` / `S2-D` 两条待办关闭并归档**：TODO 的 §7（Boresch 几何）整段 + `S2-D`（控制器收拢）一并移进 [archive/TODO_closed_2026-09-12.md](archive/TODO_closed_2026-09-12.md)，按本仓规矩**不留 `[x]` 尸体**；优先级表从七档收回六档，红测试块从 8 条更新成 10 条（多出的 2 条来自 §7.5 跨腿构象门改 WARN，与 `decide()` 重构无关，先前没登记）。归档件各带一条仍然有效的规矩，并记下本轮走过的两处弯路（`_legalize_tail_window` 曾被误搬——**它写盘**；`_latest_segment_dirs` 删对了但最初的理由错了——不是"没人调"而是"语义被证伪、留着是陷阱"）。`docs/` 内 0 坏链 | — |
| 09-12 | **`BOR-01` Boresch 几何 minimum-image 归一 + `S2-D` 控制器收拢**：①（BOR-01）写进限制势的 `calc_boresch_from_last_frame` 原本用裸 `norm(a-b)`、不接盒，而做校验的 `_check_boresch_geometry_safe` 自己解缠 ⟹ 锚点对跨周期边界时提交的 r0 静默差一个盒矢量、校验函数**反而看不出分歧**。新增唯一解缠实现 `abfe_core.unwrap_boresch_anchors_nm`（平移取**整数格矢**，不跨边界时坐标逐位不变、既有平衡值不作废），两份实现共用它，5 个调用点全部传 box。②（S2-D）4 个决策同源的 helper 迁入 `abfe_preoptimizer` 模块级；`_solve_merged_segments_if_any` 留在 pipeline（求解不是决策）、`_latest_segment_dirs` **删除**（被 `segment_dirs_for_evidence` 取代，「段号最大的段」可能根本没有目标窗口的帧，原地留墓碑注释）。顺带修一个真 bug：`_relearn_epoch_required_steps` 是类级 `@staticmethod` 却被当裸名字调 ⟹ `RELEARN_FK_EPOCH` 分支一走到就 `NameError` | 否 |
| 09-12 | **Python 版本统一成 3.12**：README×2 + GETTING_STARTED 写的 `3.10+` 是**搬集群之前的遗留**，迁移后 `environment.yml` / `environment-ci.yml` / CI / `pyproject.toml` 的 `target-version` 全是 3.12，3.10/3.11 一次都没测过。停止承诺没测过的版本 | 否 |
| 09-12 | **三条末期收尾项进 TODO**（`REL-05`~`07`，都标了「现在不做」+ 为什么）：代码内中文切英文（实测 30,642 行含中文 / 190 文件，其中**用户可见**的异常与日志 **1,563 行 / 54 文件**，非切不可的是这批，注释可最后动；异常文本被大量 `match=` 断言，会与 `S2-D` 重构撞车）；主页挂流程图（三份 README 的 `![` 计数都是 0，但管线正重构、现在挂等于挂过期图）；「没有可引用结果」那句的口径（碰科学结论单一真源，须维护者本人拍板，代理不得自行改写）| — |
| 09-12 | **两条登记为「明确不做」**（用户拍板）：① `CITATION` —— 还在 dev、方法没做完，现在写引用就是承诺一个还不存在的东西；② 英文文档追平中文 —— **中文是主文档，为开发方便的既定选择**，`README_en.md` 覆盖完整流程即可、不做逐字对等。两条都进 TODO §3 防复发 | — |
| 09-12 | **预编译插件 `.so` 真的进仓库了**（09-12 早先只拍了板没执行）：`.gitignore` 加三条例外放行 `build` 符号链接 + `build_exp026_a2/*.so`，并单独排掉没有扩展名的 gtest 可执行文件（放开目录会把它们一起放进来）。`git add -An plugins/` 应当只有 4 条。GETTING_STARTED/TROUBLESHOOTING/docs/README 同步成「随仓库分发、按环境文件建环境不用编」 | 否 |
| 09-12 | **发布文档改口径**：`RELEASE_READINESS` 不再当「首发规划」读——项目在**开发最末期**，剩下是收尾。原文按维护规则 3 不重写，加告示 + 逐条标掉已办/已划掉的（打包、R1/R2、资源随发、文档同步）；过期的 fake-clone 清单（「abfe_scripts 跟踪数 0 / local_residual 只跟踪 4/20」）改写成状态表 | — |
| 09-12 | **文档整理统一**：`TODO.md` 改成唯一待办清单（六条已关闭缺陷整段进 `archive/`，不留 `[x]` 尸体），并把散在专题文档里的开放项收编成 `S2-` / `LR-` / `REL-` 四组编号；四份 README + `PROJECT_LAYOUT.md` 的仓库地图补全（漏了 RBFE 整条线和 9 个顶层模块），并翻掉两条与实际树相矛盾的旧维护规则（`exp0XX_*` 不进本仓、residual 不随首发）；`EXP-031_…_09-04` / `AUDIT_2026-09-09` / `STAGE2_WINDOW_AND_SEGMENT_REDESIGN` / `HANDOFF_LOCAL_RESIDUAL` 四份结案文档归档并带告示。`docs/` 内 228 条链接 0 坏链 | 否 |
| 09-12 | **EXP-033 P1 闭式重训接入主线**：配体指纹对不上冻结 manifest 时不再 fail，改为预平衡跑完后自动闭式重训 `B_φ`，两条 vdW 腿共用这份权重（`local_residual/refit.py` + `runabfe.py`）。取代旧四步重训链的 ①②。**离线测试全绿，真机一次没跑过** —— [EXP-033_P1_LANDED_2026-09-12.md](EXP-033_P1_LANDED_2026-09-12.md) | 否 |
| 09-12 | **EXP-033 P3（跨配体通用权重）划掉**（用户拍板）：P3 的前提是「逐配体重训很贵」，P1 把重训压成一次闭式求解后前提不成立。连带**不必改 CUDA kernel**（省掉 `KNOWN_PLUGIN_SOURCE_SHA256` 两处同步 + G0–G4 重验 + 成本门重跑）。EXP-033 只剩 P2 开着 | — |
| 09-12 | **`resources/outer_lambda_local_residual/` 改为随首发**（用户拍板，翻转 08-31 的「residual sampling 不随首发」）：当时不发是因为「只能跑 Atenolol、永远没法换体系」，P1 闭式重训之后这条前提没了 | — |
| 09-12 | **文档补上「CUDA 插件必须自己编译」**：仓库不发任何 `.so`（`.gitignore` 排除 `plugins/*/build*`），而 loader 默认路径正是 `build/`，新 clone 一开开关就炸。写进 GETTING_STARTED《CUDA 插件》+ TROUBLESHOOTING。支持的 SM 范围由 mamba 环境的 `cuda-version=12.9` 决定（PTX sm_75~120），插件本身不锁 SM | — |
| 09-12 | 诊断脚本里两个个人路径 argparse default 改 `required=True`，加 `tests/test_no_personal_paths_as_defaults.py` 全仓 AST 扫描防复发 | 否 |
| 09-12 | **新增 clone 可导入门** `tests/test_fresh_clone_imports.py`：AST 扫全部已跟踪 `.py`，模块级 import 未跟踪模块 → 硬红（clone 当场 `ModuleNotFoundError`），惰性 import → 软门登记。**这道门就是发布清单本身。** 实测捞出 `step_guard.py` 未跟踪导致四个入口模块在 fake clone 里全死 | 否 |
| 09-12 | **修好 `self-test` 唯一一条红**：`resume/cache invalidation system hash` 断的是「不同质量给出不同哈希」，但 `_system_xml_hash` 09-09 已退役恒返回 None ⟹ 它在比 `None != None`。改成断言**退役契约**（两个真 System 都必须是 None），比 `test_todo_verified_fixes.py` 的 `fn(None)` 更强，并在注释里写明「红了不要靠让哈希复活来弄绿」| 否 |
| 09-12 | **顶层 `scripts/` 改名 `abfe_scripts/`（并加 `__init__.py` 变正规包）**：`mace_torch` 0.3.16 装了顶层正规包也叫 `scripts`，而本仓的只是 PEP 420 namespace 目录 ⟹ **正规包永远赢**，`runabfe.py` 闭式重训里 `from scripts.write_local_residual_resource_manifest import main` **在任何装了 MACE 的环境里必炸**（EXP-033 P1「真机没跑过」漏掉的那段）。同批全仓扫了顶层重名：24 个名字里只有 `scripts` 和 `tests` 撞车，都来自 mace_torch；`tests` 是潜伏的（没有任何 `from tests.` 用法，pytest 不走包名），未改 | 否 |
| 09-12 | **预编译插件 `.so` 改为随仓库分发**（用户拍板）：对着 `openmm=8.5.2` 编的，而环境文件钉的就是这个版本 ⟹ 按 `environment.yml` 建环境**开箱可用，不用编**。只有不走环境文件用了别的 OpenMM 版本、或改了插件源码才需要重编 | — |
| 09-11 | **Stage-2 分窗与多采样段重构**：一次会话查出并修掉八个真 bug（预热失败与生产质量门混淆、f_k 加帧前从不重标定、第二段采样被 bridge rescue 悄悄丢掉、分窗判据用等弧长而非 ∫g dλ 等）。全量 2083 passed | 否 |
| 09-11 | **Stage-2 自治闭环接通**：`abfe_pipeline._run_stage2_autonomous()` 的 `decide → execute → reread`，默认开。剩余 4 件活见 [STAGE2_AUTONOMOUS_LOOP_STATUS](STAGE2_AUTONOMOUS_LOOP_STATUS_2026-09-11.md) | 否 |
| 09-11 | 三轴耦合（分窗口/分 λ/分采样量）现状快照 + 路径最小修补计划稿（**计划，未实现**） | — |
| 09-10 | **溶剂腿 5.5σ 残差结案**：100% 是参照臂的盒错了（一直跑建系盒 43.950 nm³，比 1 bar 平衡密度大 3.15%）。换盒重跑残差 −4.318(5.5σ) → **+0.592(0.72σ)**。09-09 写的「密度 20% + 单混合重加权 80%」两半都作废 | 否 |
| 09-10 | R1 残差模型**绑配体、不绑体系**这一事实落成文档与重训链（换蛋白/换膜/换腿都不用重训） | 否 |
| 09-09 | **PBC-01**：链数 > 26 时 `topology.cif` 往返造假键、把水撕开。归组改为只信 System，载入时删假键 | 否 |
| 09-09 | **全仓审计**：9 个只读代理覆盖 77,588 行，62 条候选 → 52 条已修、7 条有意不修（登记为 `AUDIT-01`~`07`）。**52 处改动无一上过 GPU** | 否 |
| 09-09 | `PYMBAR_DISABLE_JAX=1`：pymbar 的 JAX 后端慢 17 倍、胖 7 倍且不归还宿主内存 | 否 |
| 09-09 | **EXP-031 路线 A+B 并入主线**（融合内核不并）。端到端 1.09×，不是单步的 1.22× | — |
| 09-05 | `IBS_BIAS_PROTOCOL_VERSION` **32 → 33**：摘掉 K 个恒零的 `cv_k_rest` 占位 CV。数学上精确恒等（能量逐比特相同），但 system XML 变 ⟹ `system_xml_sha256` 变 | **是** |
| 09-03 | `THERMODYNAMIC_PATH_PROTOCOL_VERSION` **21 → 22**：λ 布点增加可选的自由能定向加密。默认关时与 v21 逐字节相同 | 否 |
| 09-03 | **RBFE 线 R1a/R1b/R2**：原子映射、hybrid builder、独立窗口 + MBAR 分析落地。A→A 自边 ΔG = 2e-8 | — |
| 09-03 | `warmup_profile` 四档开关**整套删除**：`no_ghost_ramp` 被判定热力学上错误，预热只剩默认一条路 | 否 |
| 09-02 | **λ-WCA 防护壳退役**（`WCA_SHIELD_RETIRED=True`，`WCA_ACCOUNTING_VERSION` → 3）。这是 4W53 的 ΔG_bind 从 **+12.75 变成 −21.36 kJ/mol** 的原因 | **是** |
| 09-02 | 撤销 `docs/status/`——它本身就是维护规则第 1 条不许有的「平行当前状态文档」 | — |
| 09-01 | `ESS_GATE_PROTOCOL_VERSION` **4 → 5**：raw 权重退化量改在**去相关之前**算。旧写法拿权重挑样本、再用挑出的样本量同一批权重的退化，是循环论证；实测判定被直接翻转（min 绝对 ESS 3.58 → 47.01，门槛 20） | 否（不进指纹） |
| 09-01 | checkpoint **跨 platform 迁移**：CPU 写的 checkpoint 此前载不进 CUDA、被当损坏，白烧 5M 步 | 否 |
| 09-01 | `0831issue` 第九轮审查的 13 条 P1 + 43 条 P2 收口 | — |

## 2026-08

| 日期 | 变更 | 破缓存 |
|---|---|---|
| 08-31 | **发布整理**：移出 200+ 文件，`docs/` 与 `curated_project/` 两套文档合并成一套，开发期材料压缩成 [HISTORY_LOG](HISTORY_LOG.md)。本仓库自此是**工程区分支** | — |
| 08-31 | **主线代码库迁移**到 `ABFE_IBS/`（新 git 库、大版本起点）。Atenolol-rank11 保留但不再是唯一真源 | — |
| 08-31 | `IBS_BIAS_PROTOCOL_VERSION` **31 → 32**：逐帧 `e_offset` 从 `energy_buffer` 泄漏进 `tmbar_history` 的 `u_kn`，等价于人为注入共模因子，把 raw 单参考 ESS 压到个位数 | **是** |
| 08-29 | `LIGAND_COM_RESTRAINT_PROTOCOL_VERSION` **1 → 2**：**移除** Group 5 配体 COM 约束。非周期绝对锚点在 CUDA 上与被折叠的 centroid 成像规则不一致，形成永久激活的错误外力；CPU 与静态测试都检不出 | **是** |
| 08-28 | **Stage-2 根因定案**：ΔG_bind 错 +32 kJ/mol 不是循环/Boresch/λ 布点/估计器的问题，是每窗口单轨迹重加权抓不到空腔重组熵项。**所有既有收敛门对这个失效模式失明** | — |
| 08-27 | 主线体系切到 **4W53**（T4 lysozyme L99A + toluene） | — |
| 08-26 | `IBS_BIAS_PROTOCOL_VERSION` **30 → 31**：f_k 在线学习喂的是纯物理能量、不含残差项，而实际驱动采样的偏置力含残差 —— 拿不含残差的信号去学一个含残差的分布 | **是** |
| 08-25 | `IBS_BIAS_PROTOCOL_VERSION` **29 → 30**：新增 `s_residual`，EM 阶段对 residual 臂临时关闭残差耦合（全强度残差力下的无温控最小化会让局部环境原子数雪崩） | **是** |
| 08-24 | **EXP-029 / EXP-030 接线**；P1-19 在线 split-half 的"window 0"是滑动列表位置不是物理窗口 | — |
| 08-13 ~ 14 | **EXP-025 ~ 028**：Local Many-Body Residual CUDA 插件全链（G0 ABI → G1 Reference → G2 brute-force → G3 local CSR → G4 成本门）。EXP-025 成本门**未过**（1.1234 vs ≤1.07）判 STOP；EXP-026 Patch A1/A2 优化后 O4 **过**（1.0414）；EXP-028 找到真凶：每步 `addArg()` 而非 bind-once，参数向量无界增长，30k 步内每步成本翻倍 | — |
| 08-04 ~ 09 | **膜体系 / charge-transfer 线** B3（复合物腿 charge transfer）、B4（溶剂腿 co-ion）、B5（离线测试全绿 1161 passed）、C1（水盒离子简化）、C2（密度 + NVT + co-ion 选点，五轮修复）、C3（端点验证 150 帧全过） | — |
| 08-06 | `VDW_NONBONDED_PROTOCOL_VERSION` 引入（MEM-00h 双边归一化） | — |
| 08-03 | MEM-17 帧数门删除：重复帧是真的，但根因在 DCD append，不该往质量门加帧数对账 | — |

## 2026-07

| 日期 | 变更 | 破缓存 |
|---|---|---|
| 07-29 | **Boresch 二面角符号反号事故**修复（BOR-01） | **是** |
| 07-29 | **DEXP 合并进 `abfe_core`**：它只是替代 LJ 的解析形式，只吃 alpha/beta；退役的拟合代码隔离进 `dexp_退役.py` | — |
| 07-27 | **移除四块不可达代码**：ensemble 变异自动修复循环、`--parallel-stages`、medium-probe λ 精修、重叠 vdW 调度设计。逐字存档在 `archive/removed_*.md`，由 `test_att27_dead_code_removed.py` 钉住防回归 | — |
| 07-20 ~ 26 | `THERMODYNAMIC_PATH_PROTOCOL_VERSION` **v8 ~ v21** 的长链演化：v8/v9/v10/v11 四版**全部撤回**；v12 改由热力学长度布点；v13~v16 逐次修 window 0 分组；v17 全 5 窗真机跑通；v18 纯 Fisher 等分**又失败**（解耦尾塌缩）；v19 退回二次调度；v20 插两个实测中点 → 23 态 / 6 系综；v21 改为弧长与几何进度的**混合**等分 | **是** |
| 07-19 ~ 26 | `IBS_BIAS_PROTOCOL_VERSION` **v13 ~ v29** 的长链演化：v15 收敛门换成论文的 LSE 自洽方程；v16 更新式与判据对齐；v17/v18 修时间平均估计器的重要性校正与配分函数；v19 换成 TMBAR；**v21/v22 的 f_k 符号翻转本身是错的、v27 已回退（永远不要再加那个负号）**；v24~v26 修预热预算与 best-effort 放行；v29 整体换成局部滑窗 MBAR loose gate | **是** |
| 07-16 ~ 18 | **IBS resume 一批修复**：mode-fallback、checkpoint 续跑、ladder 预算与终止态、累计步数重复计数、跨进程 ladder 状态丢失 | — |
| 07-10 | DEXP / MACE surrogate 工作线的第一份交接记录（本项目文档记录的起点） | — |

## 2026-06 及更早

`output/dexp_experiment_OLD/`（2026-06-05）是现存最早的产物：DEXP 核与 LJ / MACE 的对比实验，
当时还在 Atenolol-rank11 上、还没有 IBS。更早的材料没有留下可核对的记录。

---

## 协议版本现值

以下由 [`tests/test_doc_staleness_contract.py`](../tests/test_doc_staleness_contract.py) 对着源码常量钉住。
**这张表只在 [STATUS.md](STATUS.md) 和这里各存一份，别再往第三处复制。**

| 协议 | 常量 | 值 | 最近一次跳变 |
|---|---|---|---|
| IBS 偏置 | `ibs_engine.IBS_BIAS_PROTOCOL_VERSION` | **33** | 2026-09-05 |
| 热力学路径 | `abfe_preoptimizer.THERMODYNAMIC_PATH_PROTOCOL_VERSION` | **22** | 2026-09-03 |
| ESS 门 | `ibs_engine.ESS_GATE_PROTOCOL_VERSION` | **5** | 2026-09-01 |
| WCA 记账 | `ibs_engine.WCA_ACCOUNTING_VERSION` | **3** | 2026-09-02 |
| LJ 长程修正 | `ibs_engine.TRADITIONAL_LJ_LRC_PROTOCOL_VERSION` | **3** | — |
| 配体 COM 约束 | `ibs_engine.LIGAND_COM_RESTRAINT_PROTOCOL_VERSION` | **2** | 2026-08-29 |

「破缓存」= 兼容集合收窄成新版本自己，既有 `dual_window_*` / `ibs_state_*` /
`convergence.json` 全部失配、必须重跑，**不要试图复用**。
它**不作废**已经登记在 [STATUS.md](STATUS.md) 的数字——那些是已完成运行的记录，
失配影响的只是 resume 与缓存复用。

## 往这里加条目的规则

1. 一行一条，写"改了什么"，不写"为什么这么改"——理由写在源码注释或专题文档里，这里只指路；
2. 协议版本跳变必须标破不破缓存；
3. **计划、实现、测试通过、科学验证是四种状态**，条目里别混成"成功"；
4. 数字要带单位和符号约定，或者不写数字；
5. 不原地重写旧条目来"修正历史"——写新条目、指明它取代了哪条。
