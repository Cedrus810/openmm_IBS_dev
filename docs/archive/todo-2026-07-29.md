# TODO completed/retired archive

Date: 2026-07-29

Source: docs/TODO.md

This file contains completed, retired, merged, or no-longer-actionable TODO entries moved out of the active checklist on 2026-07-29. Ongoing `[→]` entries are intentionally kept in `docs/TODO.md`.

## P0 / P1 completed or closed

- [x] **P0-6 / P0-7 / ATT-06 / ATT-07: old DEXP fitter / production Hamiltonian contract split.**
  GitHub #47, closed 2026-07-28. Production no longer fits global `r0_vdw/A_fit/B_fit` or accepts `offset_c0/offset_c1`; pair-specific `r0_ij/epsilon_ij` are generated analytically from LJ parameters. Old JSON fields now fail closed.

- [x] **P0-11: solvent leg box was 3.000 nm because solute size was omitted.**
  Fixed 2026-07-28. `abfe_core.solvent_box_edge_nm()` now implements the intended padding semantics, builders pass explicit `boxSize=`, OpenMM box size is verified after construction, and `SOLVENT_CACHE_PROTOCOL_VERSION` moved from 3 to 4. Production default is `SOLVENT_PADDING_NM = 1.5`, yielding box edge 4.257 nm and explicit 0.15 M salt. Later P1-19 evidence supersedes the early statement that box size explained nothing: pad1.5 to pad2.4 differences are currently interpreted as vanishing-leg run-to-run scatter, not finite-size effect.

- [x] **P1-21: charging / stage1 primary estimator changed to adjacent BAR with FD-TI consistency gate.**
  Closed 2026-07-28. `adjacent_bar_chain()`, `reweighted_fd_ti()`, `stage1_estimator_crosschecks()`, and `stage1_ti_consistency_gate()` are wired into production charging analysis. Important discipline retained in active TODO: vdW/stage2 must not receive BAR/TI/all-frame/bootstrap changes opportunistically.

- [x] **P1-8 / ATT-14: DEXP cutoff/switch configuration was ignored.**
  GitHub #54, closed 2026-07-28. DEXP softcore force creation now reads normalized `cutoff_distance` and `switch_width`, giving the intended 0.70/0.50 nm defaults, while traditional softcore keeps 1.20/1.00 nm.

- [x] **P1-17: missing Boresch restraint-attachment leg.**
  Implemented and GPU-tested 2026-07-28. Added `ibs_engine.run_boresch_attachment_leg()` for lambda_boresch 0->1 with adjacent BAR primary estimator and TI consistency gate. The final result path now includes the measured attachment leg and rejects negative attachment free energies fail-closed.

- [x] **P1-18: residual charging discrepancy against `result.txt` closed.**
  Closed 2026-07-29 because the premise was withdrawn. `result.txt` is only comparable at the total level; per-component comparison double-counted coupled restraint / restrained-leg effects. After BOR-01 full rerun, total binding free energy is within about 0.98 sigma of `result.txt` total under the optimistic reported uncertainty. Remaining real physics concern is P1-19 vanishing-leg uncertainty / sampling.

- [x] **P1-20: Stage 2 preoptimization fingerprint fail-open bypass removed.**
  Completed 2026-07-28, TODO status corrected 2026-07-29. `ABFE_DEBUG_SKIP_STAGE2_FINGERPRINT` now raises `RuntimeError` instead of forcing `protocol_match = True`. Tests cover the removal. The remaining `protocol_match = True` schema migration paths occur only after explicit physical-input checks.

## Validation completed

- [x] **V-03: Stage 2 v21 blended-metric lambda schedule CUDA validation.**
  Closed 2026-07-29. Both legs recorded `thermodynamic_path_protocol_version = 21`; realized ranges `[[0,5],[4,8],[7,12],[11,16],[15,20],[19,23]]` match `docs/design/LAMBDA_SCHEDULE_CONTRACT.md`, share exactly one node between adjacent windows, and cover final state 22. `validate_single_shared_boundary_ranges()` is wired into production and covered by tests.

- [x] **V-07: offline test suite rerun passed.**
  Closed 2026-07-29. The offline entry is `./tests/run_offline_tests.sh`, equivalent to `pytest -m "not needs_gpu"`; `cpu_only` is a hardware-requirement marker, not a collection gate. The previous ESS gate assertion failure was fixed and the full offline suite passed.

- [x] **V-04: solvent leg rebuilt with explicit 0.15 M NaCl cache.**
  Closed 2026-07-29. `output_lrc_fix/solvent_cache_manifest.json` records `protocol_version = 4`, `ionic_strength_molar = 0.15`, `na_count = 7`, `cl_count = 7`, `box_edge_nm = 4.257`, and `padding_nm = 1.5`. The 3->4 protocol bump and changed box edge prove old cache reuse was rejected.

## Boresch completed or retired

- [x] **BOR-01: Boresch dihedral sign bug fixed.**
  Fixed and verified 2026-07-29. Four hand-written dihedral formulas returned `-phi`; all now use `abfe_core.boresch_dihedral_rad()`. Full record is in `docs/archive/BOR-01-boresch-dihedral-sign-fixed-2026-07-29.md` and `docs/handoffs/BORESCH_DIHEDRAL_SIGN_HANDOFF.md`.

- [x] **BOR-03: decided not to bump `BORESCH_GEOMETRY_CONVENTION_VERSION` from 2 to 3.**
  Closed 2026-07-29. v2 `simple` / `fluctuation` cached geometry values were already numerically correct; the contaminated path was the last-frame auto/orb overwrite, fixed by BOR-01.

## Prior archive index moved out of active TODO

The following older completed items remain in `docs/archive/TODO-2026-07-27-full.md` and `docs/archive/TODO-2026-07-28-completed.md`:

- P0-1 to P0-5, P0-8, P0-10.
- P1-9 to P1-16.
- ATT-01 to ATT-05, ATT-08 to ATT-13, ATT-15 to ATT-18.
- P2-13, P2-14, P2-15.
- V-01, V-05, and V-06 historical validation / diagnostics.
- 2026-07-26/27 local validation long record.
- 2026-07-28 completed and closed items plus #1-#27 review records.
